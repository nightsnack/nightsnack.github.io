<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>gradientdecent</title>
      <link href="/2020/01/05/gradientdecent/"/>
      <url>/2020/01/05/gradientdecent/</url>
      
        <content type="html"><![CDATA[]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>RESNET 阅读笔记</title>
      <link href="/2019/12/20/resnetreadnote/"/>
      <url>/2019/12/20/resnetreadnote/</url>
      
        <content type="html"><![CDATA[<p>由知乎几篇文章和论文图片整合而成，以备日后查询。</p><h5 id="原文摘要"><a href="#原文摘要" class="headerlink" title="原文摘要"></a>原文摘要</h5><p>　　随着人们对于神经网络技术的不断研究和尝试，每年都会诞生很多新的网络结构或模型。这些模型大都有着经典神经网络的特点，但是又会有所变化。你说它们是杂交也好，是变种也罢，总之针对神经网络的创新的各种办法那真叫大开脑洞。而这些变化通常影响的都是使得这些网络在某些分支领域或者场景下表现更为出色（虽然我们期望网络的泛化性能够在所有的领域都有好的表现）。深度残差网络（deep residual network）就是众多变种中的一个代表，而且在某些领域确实效果不错，例如目标检测（object detection）。　　<br><a id="more"></a></p><p><strong>1、引言</strong></p><p>　　2015年时，还在MSRA的何恺明祭出了ResNet这个“必杀技”，在ISLVRC和COCO上“横扫”了所有的对手，可以说是顶级高手用必杀技进行了一场杀戮。除了取得了辉煌的成绩之外，更重要的意义是启发了对神经网络的更多的思考。可以说深度残差网络（Deep residual network, ResNet）的提出是CNN图像史上的一件里程碑事件。</p><p>　　ResNet的作者何恺明获得了CVPR2016最佳论文奖。那么ResNet为什么会如此优异的表现呢？其实ResNet是解决了深度CNN模型难训练的问题，我们知道2014年的VGG才19层，而15年的ResNet多达152层，这在网络深度完全不是一个量级上，所以如果是第一眼看到这个层数的话，肯定会觉得ResNet是靠深度取胜。事实当然是这样，但是ResNet还有架构上的trick，这才使得网络的深度发挥出作用，这个trick就是残差学习（Residual learning）。接下来我们将详细分析ResNet的原理。</p><p><strong>2、为什么会提出ResNet残差网络呢？</strong></p><p>　　VGG网络试着探寻了一下深度学习网络的深度究竟可以深到何种程度还可以持续提高分类的准确率。对于传统的深度学习网络，我们普遍认为网络深度越深（参数越多）非线性的表达能力越强，该网络所能学习到的东西就越多。我们凭借这一基本规则，经典的CNN网络从LetNet-5（5层）和AlexNet（8层）发展到VGGNet(16-19)，再到后来GoogleNet（22层）。根据VGGNet的实验结果可知，在某种程度上网络的深度对模型的性能至关重要，当增加网络层数后，网络可以进行更加复杂的特征模式的提取，所以当模型更深时理论上可以取得更好的结果，从图1中也可以看出网络越深而效果越好的一个实践证据。</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwxrvaomj30ed0ekwiu.jpg" alt="img"></p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwxsl7aaj30ed06d0ty.jpg" alt="img"></p><p>​         图1：VGGNet网络结构和实验结果</p><p> 　　但是更深的网络其性能一定会更好吗？我们后来发现传统的CNN网络结构随着层数加深到一定程度之后，越深的网络反而效果更差，过深的网络竟然使分类的准确率下降了（相比于较浅的CNN而言）。比较结果如图2。</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwxvgybgj30g307ujvf.jpg" alt="img"></p><p>图2：常规的CNN网络过分加深网络层数会带来分类准确率的降低</p><p>　　</p><p>　　为什么CNN网络层数增加分类的准确率却下降了呢？难道是因为模型参数过多表达能力太强出现了过拟合？难道是因为数据集太小出现过拟合？显然都不是！！！我们来看，什么是过拟合呢？过拟合就是模型在训练数据上的损失不断减小，在测试数据上的损失先减小再增大，这才是过拟合现象。根据图2 的结果可以看出：56层的网络比20层网络在训练数据上的损失还要大。这可以肯定不会是过拟合问题。因此，我们把这种问题称之为网络退化问题（Degradation problem）。</p><p>　　我们知道深层网络存在着梯度消失或者爆炸的问题，这使得深度学习模型很难训练。但是现在已经存在一些技术手段如BatchNorm来缓解这个问题。因此，出现深度网络的退化问题是非常令人诧异的。</p><p>　　何恺明举了一个例子：考虑一个训练好的网络结构，如果加深层数的时候，不是单纯的堆叠更多的层，而是堆上去一层使得堆叠后的输出和堆叠前的输出相同，也就是恒等映射/单位映射（identity mapping），然后再继续训练。这种情况下，按理说训练得到的结果不应该更差，因为在训练开始之前已经将加层之前的水平作为初始了，然而实验结果结果表明在网络层数达到一定的深度之后，结果会变差，这就是退化问题。这里至少说明传统的多层网络结构的非线性表达很难去表示恒等映射（identity mapping），或者说你不得不承认目前的训练方法或许有点问题，才使得深层网络很难去找到一个好的参数去表示恒等映射（identity mapping）。</p><p><strong>3、深度残差网络结构学习（Deep Residual learning）</strong></p><p>　　<strong>（1）残差单元</strong></p><p>　　这个有趣的假设让何博士灵感爆发，他提出了残差学习来解决退化问题。对于一个堆积层结构（几层堆积而成）当输入为x时其学习到的特征记为H(x)，现在我们希望其可以学习到残差F(x) = H(x) - x，这样其实原始的学习特征是H(x)。之所以这样是因为残差学习相比原始特征直接学习更容易。当残差为F(x) = 0时，此时堆积层仅仅做了恒等映射，至少网络性能不会下降，实际上残差不会为0，这也会使得堆积层在输入特征基础上学习到新的特征，从而拥有更好的性能。残差学习的结构如图3所示。这有点类似与电路中的“短路”，所以是一种短路连接（shortcut connection）。</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwxy8jkbj30h609c75a.jpg" alt="img"></p><p>图3：残差学习单元</p><p>　　为什么残差学习相对更容易，从直观上看残差学习需要学习的内容少，因为残差一般会比较小，学习难度小点。不过我们可以从数学的角度来分析这个问题，首先残差单元可以表示为：</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwxzjtzkj307o020t8m.jpg" alt="img"></p><p>其中，XL和XL+1分别表示第L个残差单元的输入和输出，注意每个残差单元一般包含多层结构。F是残差函数，表示学习到的残差，而h(XL) = XL表示恒等映射，f 是ReLu激活函数。基于上式，我们求得从浅层 l 到深层 L 的学习特征。</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwy1k446j308d02fa9x.jpg" alt="img"></p><p> 我们可以知道，对于传统的CNN，直接堆叠的网络相当于一层层地做——仿射变换-非线性变换，而仿射变换这一步主要是矩阵乘法。所以总体来说直接堆叠的网络相当于是乘法性质的计算。而在ResNet中，相对于直接堆叠的网络，因为shortcut的出现，计算的性质从乘法变成了加法。计算变的更加稳定。当然这些是从前向计算的角度，从后向传播的角度，如果代价函数用Loss表示，则有</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwy3wag7j30gi02v0sv.jpg" alt="img"></p><p>也就是说，无论是哪层，更高层的梯度成分<img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwy75b70j301e01kmwy.jpg" alt="img">都可以直接传过去。小括号中的1表明短路机制（shortcut）可以无损地传播梯度，而另外一项残差梯度则需要经过带有weights的层，梯度不是直接传递过来的。残差梯度不会那么巧全为-1，而且就算其比较小，有1的存在也不会导致梯度消失。这样一来梯度的衰减得到进一步抑制，并且加法的计算让训练的稳定性和容易性也得到了提高。所以可训练的网络的层数也大大增加了。</p><p>　　<strong>（2）恒等映射/单位映射（identity mapping）</strong></p><p>　　我们知道残差单元通过 identity mapping 的引入在输入和输出之间建立了一条直接的关联通道，从而使得强大的有参层集中学习输入和输出之间的残差。一般我们用F(X, Wi)来表示残差映射，那么输出即为：Y = F(X, Wi) + X 。当输入和输出通道数相同时，我们自然可以如此直接使用X进行相加。而当它们之间的通道数目不同时，我们就需要考虑建立一种有效的 identity mapping 函数从而可以使得处理后的输入X与输出Y的通道数目相同即Y = F(X, Wi) + Ws*X。</p><p>　　当X与Y通道数目不同时，作者尝试了两种 identity mapping 的方式。一种即简单地将X相对Y缺失的通道直接补零从而使其能够相对齐的方式，另一种则是通过使用1x1的conv来表示Ws映射从而使得最终输入与输出的通道达到一致的方式。</p><p>　　<strong>（3）瓶颈（BottleNeck）模块</strong></p><p>　　如下图4所示，左图是一个很原始的常规模块（Residual block），实际使用的时候，残差模块和Inception模块一样希望能够降低计算消耗。所以论文中又进一步提出了“瓶颈（BottleNeck）”模块，思路和Inception一样，通过使用1x1 conv来巧妙地缩减或扩张feature map维度从而使得我们的3x3 conv的filters数目不受外界即上一层输入的影响，自然它的输出也不会影响到下一层module。不过它纯是为了节省计算时间进而缩小整个模型训练所需的时间而设计的，对最终的模型精度并无影响。</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwyctpt2j30g107qmzs.jpg" alt="img"></p><p>图4：BottleNeck模块</p><p>　　<strong>（4）ResNet的结构</strong></p><p>　　ResNet网络是参考了VGG19的网络，在其基础上进行了修改，并通过短路机制加入了残差单元，如图5所示。变化主要体现在ResNet直接使用stride=2的卷积做下采样，并且用global average pool层替换了全连接层。ResNet的一个重要设计原则是：当feature map大小降低一半时，featuremap的数量增加一倍，这保持了网络层的复杂度。从图5中可以看到，ResNet相比普通网络每两层间增加了短路机制，这就形成了残差学习，其中虚线表示featuremap数量发生了改变。图5展示的34-layer的ResNet，还可以构建更深的网络如表1所示。从表中可以看到，对于18-layer和34-layer的ResNet，其进行的两层间的残差学习，当网络更深时，其进行的是三层间的残差学习，三层卷积核分别是1x1，3x3和1x1，一个值得注意的是隐含层的feature map数量是比较小的，并且是输出feature map数量的1/4。</p><p><em><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwyeuy7ej30u01wv4qp.jpg" alt="img"></em></p><p>图5 ResNet网络结构图</p><p>表1 不同深度的ResNet</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwyk60gyj318v0jm41x.jpg" alt="0?wx_fmt=png"></p><p>　　下面我们再分析一下残差单元，ResNet使用两种残差单元，如图6所示。左图对应的是浅层网络，而右图对应的是深层网络。对于短路连接，当输入和输出维度一致时，可以直接将输入加到输出上。但是当维度不一致时（对应的是维度增加一倍），这就不能直接相加。有两种策略：（1）采用zero-padding增加维度，此时一般要先做一个downsamp，可以采用strde=2的pooling，这样不会增加参数；（2）采用新的映射（projection shortcut），一般采用1x1的卷积，这样会增加参数，也会增加计算量。短路连接除了直接使用恒等映射，当然都可以采用projection shortcut。<br><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwymi0j2j30qd09h0tl.jpg" alt="img"></p><p>图6 不同的残差单元</p><p>作者对比18-layer和34-layer的网络效果，如图7所示。可以看到普通的网络出现退化现象，但是ResNet很好的解决了退化问题。</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwynf7plj30v109mmy1.jpg" alt="0?wx_fmt=png"></p><p>图7 18-layer和34-layer的网络效果</p><p>　　最后展示一下ResNet网络与其他网络在ImageNet上的对比结果，如表2所示。可以看到ResNet-152其误差降到了4.49%，当采用集成模型后，误差可以降到3.57%。</p><p>表2 ResNet与其他网络的对比结果</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwyqb5mzj30fb09lwez.jpg" alt="0?wx_fmt=png"></p><p> 　　<strong>（5）ResNet的进一步改进</strong></p><p> 　　在2015年的ILSVRC比赛获得第一之后，何恺明对残差网络进行了改进，主要是把ReLu给移动到了conv之前，相应的shortcut不在经过ReLu，相当于输入输出直连。并且论文中对ReLu，BN和卷积层的顺序做了实验，最后确定了改进后的残差网络基本构造模块，如下图8所示，因为对于每个单元，激活函数放在了仿射变换之前，所以论文叫做预激活残差单元（pre-activation residual unit）。作者推荐在短路连接（shortcut）采用恒等映射（identity mapping）。</p><p> <img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galwyu4zpuj30oe0cmq4a.jpg" alt="img"></p><p>图8 改进后的残差单元及效果</p>]]></content>
      
      
      <categories>
          
          <category> 技术 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 深度学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>EM 算法的基本步骤</title>
      <link href="/2019/10/17/embasic/"/>
      <url>/2019/10/17/embasic/</url>
      
        <content type="html"><![CDATA[<h2 id="EM算法的步骤"><a href="#EM算法的步骤" class="headerlink" title="EM算法的步骤"></a><strong>EM算法的步骤</strong></h2><ol><li>选择合适的初始参数$\theta_0$，开始迭代（EM算法是初值敏感的）</li><li>E步：记$\theta_0$为第i次参数的估计值，计算第i+1次的Q函数的值$Q(\theta,\theta_i) = E_Z[log(Y,Z|\theta)|Y,\theta_i]=\Sigma_Z logP(Y,Z|\theta)P(Z|Y,\theta_i)$</li></ol><ol><li>M步：求使$Q(\theta,\theta_i)$极大化的参数$\theta$，确定新的参数估计值$\theta_{i+1} = argmax_{\theta}Q(\theta,\theta_i)$</li><li>重复（2），（3）直到收敛，收敛条件一般为$||\theta_{i+1}-\theta_i||\le \in_1$或者$||Q(\theta_{i+1},\theta_i)-Q(\theta_i,\theta_i)||\le \in_2$</li></ol><a id="more"></a>]]></content>
      
      
      <categories>
          
          <category> 知识 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 机器学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>MAP最大后验估计</title>
      <link href="/2019/10/05/map/"/>
      <url>/2019/10/05/map/</url>
      
        <content type="html"><![CDATA[<h2 id="为什么要引入最大后验？"><a href="#为什么要引入最大后验？" class="headerlink" title="为什么要引入最大后验？"></a>为什么要引入最大后验？</h2><p>最大似然估计仅仅是求得似然函数的极值点，考虑到实验结果数量较少，有的时候仅靠最大似然估计不足以求得较为准确参数估计值。<br>拿这枚硬币抛了10次，得到的数据（$x_0$）是：反正正正正反正正正反。我们想求的正面概率θθ是模型参数，而抛硬币模型我们可以假设是 二项分布。</p><a id="more"></a><p>那么，出现实验结果$x_0$（即反正正正正反正正正反）的似然函数是多少呢？</p><script type="math/tex; mode=display">\begin{split}f(x_0,\theta) &= (1−\theta)\times\theta\times\theta×\theta×\theta×(1−\theta)\times\theta×\theta×\theta×(1−\theta) \\&=\theta^7(1−\theta)^3\\&=f(θ)\\\end{split}</script><p>注意，这是个只关于$\theta$的函数。而最大似然估计，顾名思义，就是要最大化这个函数。我们可以画出$f(θ)$的图像：</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galj8v8jn8j31bk0qomyt.jpg" alt="likeli"></p><p>可以看出，在$θ=0.7$时，似然函数取得最大值。</p><p>这样，我们已经完成了对$θ$的最大似然估计。即，抛10次硬币，发现7次硬币正面向上，最大似然估计认为正面向上的概率是0.7。（ummm..这非常直观合理，对吧？）</p><p>且慢，一些人可能会说，硬币一般都是均匀的啊！ 就算你做实验发现结果是“反正正正正反正正正反”，我也不信$θ=0.7$。</p><p>这里就包含了贝叶斯学派的思想了——要考虑先验概率。 为此，引入了最大后验概率估计。</p><h2 id="最大后验估计"><a href="#最大后验估计" class="headerlink" title="最大后验估计"></a>最大后验估计</h2><p>最大似然估计是求参数$θ$, 使似然函数$P(x0|θ)$最大。最大后验概率估计则是想求$θ$使$P(x0|θ)P(θ)$最大。求得的$θ$不单单让似然函数大，$θ$自己出现的先验概率也得大。 （这有点像正则化里加惩罚项的思想，不过正则化里是利用加法，而MAP里是利用乘法）</p><p>MAP其实是在最大化$P(θ|x_0)=\frac{P(x_0|θ)P(θ)}{P(x_0)}$，不过因为$x_0$是确定的（即投出的“反正正正正反正正正反”），$P(x_0)$是一个已知值，所以去掉了分母$P(x0)$（假设“投10次硬币”是一次实验，实验做了1000次，“反正正正正反正正正反”出现了n次，则$P(x_0)=n/1000P(x_0)=n/1000$。总之，这是一个可以由数据集得到的值）。最大化$P(θ|x_0)$的意义也很明确，$x_0$已经出现了，要求$θ$取什么值使$P(θ|x_0)$最大。顺带一提，$P(θ|x_0)$即后验概率，这就是“最大后验概率估计”名字的由来。</p><p>对于投硬币的例子来看，我们认为（”先验地知道“）$θ$取0.5的概率很大，取其他值的概率小一些。我们用一个高斯分布来具体描述我们掌握的这个先验知识，例如假设$P(θ)$为均值0.5，方差0.1的高斯函数，如下图：</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galkg7t34cj30zk0qomyd.jpg" alt="ptheta"></p><p>则$P(x0|θ)P(θ)$的函数图像为：</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galkgdntyfj30zk0qodgz.jpg" alt="map1"></p><p>注意，此时函数取最大值时，$θ$取值已向左偏移，不再是0.7。实际上，在$θ=0.558$时函数取得了最大值。即，用最大后验概率估计，得到$θ=0.558$<br>最后，那要怎样才能说服一个贝叶斯派相信$θ=0.7$呢？你得多做点实验。。</p><p>如果做了1000次实验，其中700次都是正面向上，这时似然函数为:</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galkglnlzaj30zk0qomy1.jpg" alt="likeli2"></p><p>如果仍然假设$P(θ)$为均值0.5，方差0.1的高斯函数，$P(x0|θ)P(θ)$的函数图像为：</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galkgtqxixj30zk0qoaat.jpg" alt="map2"></p><p>在$θ=0.696$处，$P(x0|θ)P(θ)$取得最大值。</p><p>这样，就算一个考虑了先验概率的贝叶斯派，也不得不承认得把$θ$估计在0.7附近了。</p><p>PS. 要是遇上了顽固的贝叶斯派，认为$P(θ=0.5)=1$ ，那就没得玩了。。 无论怎么做实验，使用MAP估计出来都是$θ=0.5$。这也说明，一个合理的先验概率假设是很重要的。（通常，先验概率能从数据中直接分析得到）</p><h2 id="最大似然估计和最大后验概率估计的区别"><a href="#最大似然估计和最大后验概率估计的区别" class="headerlink" title="最大似然估计和最大后验概率估计的区别"></a>最大似然估计和最大后验概率估计的区别</h2><p>相信读完上文，MLE和MAP的区别应该是很清楚的了。MAP就是多个作为因子的先验概率P(θ)P(θ)。或者，也可以反过来，认为MLE是把先验概率$P(θ)$认为等于1，即认为$θ$是均匀分布。</p><h2 id="MAP-分类"><a href="#MAP-分类" class="headerlink" title="MAP 分类"></a>MAP 分类</h2><p>说实话我也不知道这是个啥东西，5644的一次作业题里有这样一个问题：</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1galkxp2e62j31go0u048z.jpg" alt="image-20200105123707614"></p><p>大体意思就是服从高斯分布的三个类，给了均值和方差，你可以生成出来三组数据，混合在一起的。然后希望你用MAP的方式把它分开，跟踪分对和分错的数量，给出confusion matrix。</p><p>操作方式就是：</p><p>对于所有的点来说，他们都有可能分属于3个类别，算出来对于每一个类别（有多大可能性属于它），然后选出来最大的那个类别，就是这个点最可能属于的类。</p><p>计算的公式我觉得这里叫做MAP其实不太合适，这只是个后验概率而已，公式是后验=条件概率x先验。当然，还要取对数。先验已知了。$ln(P(x|L_k))$ 这个条件概率（就是已知这个点属于第K类的时候，它的概率密度）由高斯分布pdf的公式可以求出来。</p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1gallc873upj30so0mg104.jpg" alt="image-20200105125105925"></p><p>Numbers of each sample:</p><p>sample 1: 1517 </p><p>sample 2: 3441 </p><p>sample 3: 5042 </p><p>  Classification Rule (MAP) Description:</p><p>  From the data generated, we could see this is a typical GMM model, with 3 class prior and each class’s $\Sigma$ and $\mu$，Let $P(L_k) = P(L=k),k ={1,2,3}$</p><script type="math/tex; mode=display">\begin{gather*}    g_k(x) = ln(P(x|L_k)) + ln(P(L_k)) \\    ln(P(x|L_k))  = -\frac{1}{2}ln(2\pi)-\frac{1}{2}ln|\Sigma_{k}|- \frac{1}{2}(x-\mu)\Sigma^{-1}(x-\mu)'   \\     \end{gather*}</script><p>  For each point, by using pdf we can calculate all the points for 3 class’s $arg_{C_k}maxg_k(x)$<br>  as a score. Then the maximum of 3 scores is the decision of coresponding class decision label. </p><script type="math/tex; mode=display">confusion\_matrix =               \left[        \begin{matrix}    1226 & 120& 22\\    218 &   3102  &     101\\    73   &  219   &  4919\end{matrix}     \right]</script><p>total misclassified samples: 753 </p><p>probability of error 0.075300 </p><p><img data-src="https://tva1.sinaimg.cn/large/006tNbRwly1gallcmnakrj30kq0ku459.jpg" alt="image-20200105125128320"></p><p>脚本链接：<br><a href="https://github.com/nightsnack/ce564_ml/blob/master/Midterm/Question1.m" target="_blank" rel="noopener">https://github.com/nightsnack/ce564_ml/blob/master/Midterm/Question1.m</a></p>]]></content>
      
      
      <categories>
          
          <category> 知识 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 机器学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>最大似然估计</title>
      <link href="/2019/09/28/maxlikelyhood/"/>
      <url>/2019/09/28/maxlikelyhood/</url>
      
        <content type="html"><![CDATA[<h2 id="最大似然估计学习笔记"><a href="#最大似然估计学习笔记" class="headerlink" title="最大似然估计学习笔记"></a>最大似然估计学习笔记</h2><h3 id="为什么需要极大似然？"><a href="#为什么需要极大似然？" class="headerlink" title="为什么需要极大似然？"></a>为什么需要极大似然？</h3><p>贝叶斯公式中先验概率的估计较简单，1、每个样本所属的自然状态都是已知的（有监督学习）；2、依靠经验；3、用训练样本中各类出现的频率估计。</p><a id="more"></a><p>类条件概率的估计（非常难），原因包括：概率密度函数包含了一个随机变量的全部信息；样本数据可能不多；特征向量x的维度可能很大等等。总之要直接估计类条件概率的密度函数很难。解决的办法就是，把估计完全未知的概率密度转化为估计参数。这里就将概率密度估计问题转化为参数估计问题，极大似然估计就是一种参数估计方法。当然了，概率密度函数的选取很重要，模型正确，在样本区域无穷时，我们会得到较准确的估计值，如果模型都错了，那估计半天的参数，肯定也没啥意义了。</p><p><strong>重要前提：训练样本的分布能代表样本的真实分布。每个样本集中的样本都是所谓独立同分布的随机变量 (iid条件)，且有充分的训练样本。</strong></p><h3 id="极大似然估计的原理"><a href="#极大似然估计的原理" class="headerlink" title="极大似然估计的原理"></a>极大似然估计的原理</h3><p> 极大似然估计的原理, 用一张图片来说明，如下图所示：</p><p><img data-src="/image/006tNbRwly1gakx16edfej30hu0cfac6.png" alt="img"></p><p>总结起来，最大似然估计的目的就是：利用已知的样本结果，反推最有可能（最大概率）导致这样结果的参数值。</p><p>原理：极大似然估计是建立在极大似然原理的基础上的一个统计方法，是概率论在统计学中的应用。极大似然估计提供了一种给定观察数据来评估模型参数的方法，即：“模型已定，参数未知”。通过若干次试验，观察其结果，利用试验结果得到某个参数值能够使样本出现的概率为最大，则称为极大似然估计。</p><p>由于样本集中的样本都是独立同分布，可以只考虑一类样本集D，来估计参数向量θ。记已知的样本集为：</p><p><img data-src="/image/20170528003138251.png" alt="img"></p><p>似然函数（linkehood function）：联合概率密度函数<img data-src="/image/20170528003212360.png" alt="img">称为相对于<img data-src="/image/20170528003218392.png" alt="img">的θ的似然函数。</p><p><img data-src="/image/20170528003223845.png" alt="img"></p><p>如果<img data-src="/image/20170528003231366.png" alt="img">是参数空间中能使似然函数<img data-src="/image/20170528003236220.png" alt="img">最大的θ值，则应该是“最可能”的参数值，那么就是θ的极大似然估计量。它是样本集的函数，记作：</p><p><img data-src="/image/20170528003244189.png" alt="img"></p><p>求解极大似然函数</p><p>ML估计：求使得出现该组样本的概率最大的θ值。</p><p><img data-src="/image/20170528003838359.png" alt="img"></p><p>实际中为了便于分析，定义了对数似然函数：</p><p><img data-src="/image/20170528003844453.png" alt="img"></p><p><img data-src="/image/2018060522232071.png" alt="img"></p><ol><li>未知参数只有一个（θ为标量）</li></ol><p>在似然函数满足连续、可微的正则条件下，极大似然估计量是下面微分方程的解：</p><p><img data-src="/image/20170528003855734.png" alt="img"></p><ol><li>未知参数有多个（θ为向量）</li></ol><p><img data-src="/image/20170528003901066.png" alt="img"></p><p>记梯度算子：</p><p><img data-src="/image/20170528003905766.png" alt="img"></p><p>若似然函数满足连续可导的条件，则最大似然估计量就是如下方程的解。</p><pre><code>    ![img](/image/20170528003911078-20200104231232865.png)</code></pre><p><strong>方程的解只是一个估计值，只有在样本数趋于无限多的时候，它才会接近于真实值。</strong></p><h3 id="极大似然估计的例子"><a href="#极大似然估计的例子" class="headerlink" title="极大似然估计的例子"></a>极大似然估计的例子</h3><p>例1：设样本服从正态分布<img data-src="/image/20170528003917176.png" alt="img">，则似然函数为：</p><p><img data-src="/image/20170528003922141.png" alt="img"><br>它的对数：</p><p><img data-src="/image/20170528003926973.png" alt="img"></p><p>求导，得方程组：</p><p><img data-src="/image/20170528004731774.png" alt="img"></p><p>联合解得：</p><p><img data-src="/image/20170528004738060.png" alt="img"></p><p>似然方程有唯一解<img data-src="/image/20170528004743185.png" alt="img">：，而且它一定是最大值点，这是因为当<img data-src="/image/20170528004747290.png" alt="img">或<img data-src="/image/20170528004751982.png" alt="img">时，非负函数<img data-src="/image/20170528004756951.png" alt="img">。于是U和<img data-src="/image/20170528004801165.png" alt="img">的极大似然估计为<img data-src="/image/20170528004743185-20200104231248193.png" alt="img">。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>求最大似然估计量<img data-src="/image/20170528003231366-20200104230335203.png" alt="img">的一般步骤：</p><ol><li><p>写出似然函数；</p></li><li><p>对似然函数取对数，并整理；</p></li><li><p>求导数；</p></li><li><p>解似然方程。</p></li></ol><p>最大似然估计的特点：</p><ol><li>比其他估计方法更加简单；</li><li>收敛性：无偏或者渐近无偏，当样本数目增加时，收敛性质会更好；</li><li>如果假设的类条件概率模型正确，则通常能获得较好的结果。但如果假设模型出现偏差，将导致非常差的估计结果。</li></ol>]]></content>
      
      
      <categories>
          
          <category> 知识 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 机器学习 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ffmpeg 文档翻译</title>
      <link href="/2018/05/03/ffmpeg_translate/"/>
      <url>/2018/05/03/ffmpeg_translate/</url>
      
        <content type="html"><![CDATA[<p><code>ffmpeg</code>是一个非常快的视频和音频转换器，还可以抓取实时的音频/视频流。它可以在任意的采样率之间的转换和调整视频，并同时使用高品质的多相滤波器。<br><a id="more"></a></p><h2 id="1-概要"><a href="#1-概要" class="headerlink" title="1. 概要"></a>1. 概要</h2><p><code>ffmpeg [global_options] {[input_file_options] -i INPUT_FILE} ... {[output_file_options] OUTPUT_FILE} ...</code></p><h2 id="2-说明"><a href="#2-说明" class="headerlink" title="2. 说明"></a>2. 说明</h2><p><code>ffmpeg</code>是一个非常快的视频和音频转换器，还可以抓取实时的音频/视频流。它可以在任意的采样率之间的转换和调整视频，并同时使用高品质的多相滤波器。</p><p><code>ffmpeg</code>从输入“文件”（其可以是常规文件，管道，网络流，录制装置等），由指定任意数量的读取<code>-i</code>选项，并写入到任意数量的输出“文件”，只需指定一个输出的文件名。任何一个命令行中不能被解释为选项的内容都被认为是一个输出文件名。</p><p>每个输入或输出文件可以在原则上，包含任意数量的不同类型（视频/音频/字幕/附件/数据）的流。输出文件中允许流的数量和类型是由输出格式容器限制决定的。输入流和输出流直接的映射可以自动完成也可以用<code>-map</code>选项给定（见流选择章节）。</p><p>引用输入文件的选项时，则必须使用他们的索引（从0开始）。例如：第一输入文件是<code>0</code> ，第二个是<code>1</code>等。类似地，一个文件中的流也通过其索引指定。例如<code>2:3</code>指的是在第三个输入文件中的第四数据流。参见流章节。</p><p>作为一般规则，选项作用于下一个指定的文件。因此，命令的顺序是重要，你可以在命令行上多次相同的选项。每次选项的出现都将作用于下一个输入或输出文件。这条规则若有例外将会提前声明（例如冗余级别）。</p><p>不要混合输入和输出文件。首先指定所有输入文件，那么所有的输出文件。也不要混用属于不同的文件的选项。所有选项仅适用于下一个输入或输出文件，之后选项将被重置。</p><p>设置输出文件以64千比特/秒的视频比特率：</p><p><code>ffmpeg -i input.avi -b：V 64K -bufsize 64K output.avi</code></p><p>要强制输出文件为24 fps的帧速率：</p><p><code>ffmpeg -i input.avi -r 24 output.avi</code></p><p>要强制输入文件的帧频（仅对原始格式有效），以1 FPS读入文件，以每秒24帧的帧速率输出：</p><p><code>ffmpeg -r 1 -i input.m2v -r 24 output.avi</code></p><p><code>format</code> 选项可能需要指定，对于原始输入文件。</p><h2 id="3-详细描述"><a href="#3-详细描述" class="headerlink" title="3. 详细描述"></a>3. 详细描述</h2><p>在转码过程ffmpeg每个输出可以由以下图描述：</p><pre><code> _______              ______________|       |            |              || input |  demuxer   | encoded data |   decoder| file  | ---------&gt; | packets      | -----+|_______|            |______________|      |                                           v                                       _________                                      |         |                                      | decoded |                                      | frames  |                                      |_________| ________             ______________       ||        |           |              |      || output | &lt;-------- | encoded data | &lt;----+| file   |   muxer   | packets      |   encoder|________|           |______________|</code></pre><p><code>ffmpeg</code>调用libavformat库（含分流器）来读取输入文件并获得含有他们编码信息的数据包。当有多个输入文件， <code>ffmpeg</code>将通过跟踪最小的时间戳来试图在所有活跃的输入流间同步。编码的数据包然后被传递到解码器（除非复制音频流被选择用于流，见进一步的说明）。解码器产生的未压缩的帧（原始视频/ PCM音频/ …），它可以进一步通过滤镜进行处理（见下一节）。通过滤镜后，这些帧被传递到编码器，编码器将其编码并输出编码后的数据包。最后，这些将被传输给混合器以将编码数据写入到输出文件。</p><h3 id="3-1-滤镜"><a href="#3-1-滤镜" class="headerlink" title="3.1 滤镜"></a>3.1 滤镜</h3><p>在编码之前， <code>ffmpeg</code>可以使用libavfilter库中的滤镜处理原始的音频和视频帧。几个连接的滤镜可以形成一个滤镜组（filtergraphs）。 <code>ffmpeg</code>有两种filtergraphs：简单和复杂。</p><h4 id="3-1-1-简单filtergraphs"><a href="#3-1-1-简单filtergraphs" class="headerlink" title="3.1.1 简单filtergraphs"></a>3.1.1 简单filtergraphs</h4><p>简单filtergraphs是那些具有相同的类型且正好一个输入和输出的滤镜组。另外，在上图中，他们可以由简单地在解码和编码之间插入附加步骤来表示：</p><pre><code> _________                        ______________|         |                      |              || decoded |                      | encoded data || frames  |\                   _ | packets      ||_________| \                  /||______________|             \   __________   /  simple     _\||          | /  encoder  filtergraph   | filtered |/                | frames   |                |__________|</code></pre><p>简单filtergraphs配置了每个流的筛选器选项（与视频和音频分别-vf和-af别名）。一个简单的FilterGraph动态视频可以看一下这样的例子：</p><pre><code> _______        _____________        _______        ________|       |      |             |      |       |      |        || input | ---&gt; | deinterlace | ---&gt; | scale | ---&gt; | output ||_______|      |_____________|      |_______|      |________|</code></pre><p>需要注意的是一些滤镜改变帧属性而不是画面的内容。例如，在上例中，<code>fps</code>改变帧的数量，但不触及帧的内容。又如<code>setpts</code>滤镜，其仅设置时间戳而保持帧不变。</p><h4 id="3-1-2-复杂filtergraphs"><a href="#3-1-2-复杂filtergraphs" class="headerlink" title="3.1.2 复杂filtergraphs"></a>3.1.2 复杂filtergraphs</h4><p>复杂filtergraphs是那些不能被描述为简单的线性处理链的滤镜组。例如，当滤镜组具有多个输入和/或输出，或当输出流的类型是不同于输入。它们可以被表示为以下图：</p><pre><code> _________|         || input 0 |\                    __________|_________| \                  |          |             \   _________    /| output 0 |              \ |         |  / |__________| _________     \| complex | /|         |     |         |/| input 1 |----&gt;| filter  |\|_________|     |         | \   __________               /| graph   |  \ |          |              / |         |   \| output 1 | _________   /  |_________|    |__________||         | /| input 2 |/|_________|</code></pre><p>复杂filtergraphs可使用<code>-filter_complex</code>选项配置。注意，此选项是全局性的，因为复杂FilterGraph，就其本质，不能明确地与单个流或文件相关联。</p><p><code>-lavfi</code>选项相当于<code>-filter_complex</code>。</p><p>一个复杂FilterGraph动态的简单的例子是在<code>overlay</code>滤镜，它具有两个视频输入和一个视频输出，含有一个视频重叠在另一个的上面。其对应的音频滤镜是<code>amix</code>。</p><h3 id="3-2-复制流"><a href="#3-2-复制流" class="headerlink" title="3.2 复制流"></a>3.2 复制流</h3><p>复制流是通过添加<code>copy</code>选项到<code>-codec</code>选项完成的。它使<code>ffmpeg</code>对指定的流忽略解码和编码步骤，所以它只能混合和拆包。它用于改变所述容器的格式或修改容器级别的元数据是有用的。在这种情况下，可以简化为这样：</p><pre><code> _______              ______________            ________|       |            |              |          |        || input |  demuxer   | encoded data |  muxer   | output || file  | ---------&gt; | packets      | -------&gt; | file   ||_______|            |______________|          |________|</code></pre><p>由于不存在解码或编码，它是非常快，没有质量损失。然而因为许多因素的工作，它可能无法在某些情况下使用。应用滤镜显然也是不可能的，因为滤镜仅能作用在未压缩的数据上。</p><h2 id="4-选择流"><a href="#4-选择流" class="headerlink" title="4 选择流"></a>4 选择流</h2><p>默认情况下， <code>ffmpeg</code>只包含输入文件中每个类型流各一个（视频，音频，字幕），并将它们添加到每个输出文件。它选择“最好”的每一个流基于以下标准：用于视频，它选择最高分辨率的流；对于音频，它使用最多声道的流；对于字幕，它是第一个字幕流。在相同类型中，参数相等的若干流中具有最低索引的流被选择。</p><p>您可以通过<code>-vn/-an/-sn</code>选项禁用其中的一些默认值。若需全手动控制，请使用-map选项，它将禁用刚才所描述的默认设置。</p><h2 id="5-选项"><a href="#5-选项" class="headerlink" title="5 选项"></a>5 选项</h2><p>所有的数值选项中，如果不另外指明，均表示接受数作为输入，其后可添加一个SI单位的字符串，例如：<code>K</code>，<code>M</code>，或<code>G</code>。</p><p>如果<code>i</code>被附加在SI单位，完整的前缀将被解释为一个单元前缀的二进制倍数，也即1024倍，而不是1000倍。追加<code>B</code>可使数值增加8倍。这允许使用，例如：<code>KB</code>，<code>MiB</code>，<code>G</code>和<code>B</code>的数量后缀。</p><p>选项不带参数是布尔选项，并设置相应的值设置为<code>true</code>。他们可以通过在选项前添加<code>no</code>来将选项设置为<code>false</code>。例如使用<code>-nofoo</code>将设置名称为<code>foo</code>为假。</p><h3 id="5-1-流标识符"><a href="#5-1-流标识符" class="headerlink" title="5.1 流标识符"></a>5.1 流标识符</h3><p>有些选项是按流的，例如比特率或编解码器。流标识符被用来精确地指定一个给定的选项作用于哪一个数据流（多个）。</p><p>一个标识符一般是选项名称加冒号分隔的字符串。例如<code>-codec:a:1 ac3</code>包含流标识符<code>a:1</code>，它匹配第二音频流。因此，将选择AC3编解码器的第二音频流。</p><p>一个标识符可以匹配多个流，这个选项将适用于所有流。比如，流标识符<code>-b:a 128k</code>标识了所有的音频流。</p><p>空标识符匹配所有的流。例如， <code>-codec copy</code>或<code>-codec: copy</code>会复制所有的数据流而不重新编码。</p><p>流标识符的可能形式有：</p><p><code>stream_index</code></p><p>匹配与该索引对应的流。例如<code>-threads:1 4</code>将设置第二个流的线程计数为4。</p><p><code>stream_type[:stream_index]</code></p><p>流类型是下列字母之一：<code>v</code>为视频，<code>a</code>为声音，<code>s</code>为字幕，<code>d</code>为数据，<code>t</code>为附件。如果<code>stream_index</code>给出，则它匹配该类型的索引为<code>stream_index</code>的流。否则，它匹配所有这种类型的流。</p><p><code>p:program_id[:stream_index]</code></p><p>如果给定<code>stream_index</code>，那其将与在与ID为 <code>program_id</code>的program的<code>stream_index</code>的流相匹配。否则，它将匹配在<code>program_id</code>中的所有流。</p><p><code>#stream_id</code> 或者 <code>i:stream_id</code></p><p>按流索引逐一匹配流（如在MPEG-TS容器中的PID）。</p><p><code>m:key[:value]</code></p><p>匹配流的元数据标签中具有指定<code>key</code>的流。如果<code>value</code> 没有给出，将匹配包含给定标签的所有流。</p><p>请注意，在<code>ffmpeg</code>中，按元数据匹配仅能用于输入文件。</p><h3 id="5-2-通用选项"><a href="#5-2-通用选项" class="headerlink" title="5.2 通用选项"></a>5.2 通用选项</h3><p>这些选项当中的FF *工具共享。</p><p><code>-L</code></p><p>显示许可证。</p><p><code>-h, -?, -help, --help [arg]</code></p><p>显示帮助。一个可选参数可以被指定为打印与特定项目相关的帮助。如果没有指定参数，则只显示基本的（非高级）工具选项。</p><p><code>arg</code>的可能值是：</p><pre><code>`long`除了基本的工具选项外，打印高级的工具选项。`full`打印所有选项，包括编码器，解码器，分流器，混合器，滤镜等的共享和私有选项。`decoder=decoder_name`打印有关的解码器`decoder_name`的详细信息。使用`-decoders`选项来获得所有的解码器的列表。`encoder=encoder_name`打印有关指定编码器`encoder_name`的详细信息。使用`-encoders`选项来获得所有编码器的列表。`demuxer=demuxer_name`打印有关的分流器`demuxer_name`的详细信息。使用`-formats`选项来获取所有分流器和混合器的列表。`muxer=muxer_name`打印有关混合器`muxer_name`的详细信息。使用`-formats`选项来获取所有混合器和分流器的列表。`filter=filter_name`打印有关滤镜`filter_name`的详细信息。使用`-filters`选项来获得所有滤镜的列表。</code></pre><p><code>-version</code></p><p>显示的版本。</p><p><code>-formats</code></p><p>显示可用的格式（包括设备）。</p><p><code>-devices</code></p><p>显示可用的设备。</p><p><code>-codecs</code></p><p>显示libavcodec已知的所有编解码器。</p><p>注意，整个文档中术语“解码器”更正确地称呼是比特流媒体格式（media bitstream format）。</p><p><code>-decoders</code></p><p>显示可用的解码器。</p><p><code>-encoders</code></p><p>显示所有可用的编码器。</p><p><code>-bsfs</code></p><p>显示可用的流滤镜。</p><p><code>-protocols</code></p><p>显示可用的协议。</p><p><code>-filters</code></p><p>显示可用的libavfilter滤镜。</p><p><code>-pix_fmts</code></p><p>显示可用的像素格式。</p><p><code>-sample_fmts</code></p><p>显示可用的采样格式。</p><p><code>-layouts</code></p><p>显示频道名称和标准的渠道布局。</p><p><code>-colors</code></p><p>显示公认的颜色名称。</p><p><code>-sources device[,opt1=val1[,opt2=val2]...]</code></p><p>显示自动检测到的输入设备的源。某些设备可提供不能自动检测系统相关的源名称。返回的列表不能被假定为总是完整的。</p><pre><code>`ffmpeg -sources pulse,server=192.168.0.4`</code></pre><p><code>-sinks device[,opt1=val1[,opt2=val2]...]</code></p><p>自动检测显示输出设备的接收器。某些设备可提供不能自动检测系统相关的接收器名称。返回的列表不能被假定为总是完整的。</p><pre><code>`ffmpeg -sinks pulse,server=192.168.0.4`</code></pre><p><code>-loglevel [repeat+]loglevel | -v [repeat+]loglevel</code></p><p>设置库使用的日志记录级别。加入<code>repeat+</code>表示重复日志输出不应该被压缩到所述第一条日志和“最后的日志重复n次”线将被省略。 <code>repeat</code>，也可以单独使用。如果<code>repeat</code>可以单独使用，并没有预设的记录级别，默认记录级将被使用。如果给定多个日志级别参数，使用<code>repeat</code>不会改变日志级别。 <code>loglevel</code>是一个字符串或数字，可为以下值之一：</p><pre><code>`quiet, -8`保持沉默。`panic, 0`只显示可能导致程序崩溃的致命错误。目前没有此类错误。`fatal, 8`只显示致命错误。这些错误会导致进程绝对无法继续。`error, 16`显示所有的错误，包括那些可以修复的。`warning, 24`显示所有警告和错误。将显示任何有关可能不正确或不正常事件的信息。`info, 32`显示处理过程中的信息。不单单是警告和错误。这是默认值。`verbose, 40`与info类似 ，但更详细。`debug, 48`显示一切信息，包括调试信息。默认情况下，程序日志输出到标准错误流，如果终端支持着色，颜色用来标记错误和警告。日志着色可以被环境变量AV_LOG_FORCE_NOCOLOR或NO_COLOR ，或者可以被强制设置环境变量AV_LOG_FORCE_COLOR 禁用。使用环境变量NO_COLOR已被弃用，并在之后的FFmpeg的版本将被丢弃。</code></pre><p><code>-report</code></p><p>转储完整的命令行和控制台输出到当前目录一个文件名 为<code>program - YYYYMMDD - HHMMSS .log</code>的文件。此文件对于错误报告非常有用。这也意味着<code>-loglevel verbose</code>。</p><p>将环境变量设置<code>FFREPORT</code>为任何值具有相同的效果。如果该值是一个’：’ - 分隔键=值序列，这些选项会影响报表;如果包含特殊字符则需要使用转义字符，或者“：”分隔（参见的<code>ffmpeg-utils</code>的手册中的“引用与转义”一节）。</p><p>下列选项也可使用：</p><pre><code>`file`设置报告使用的文件名; %p添加程序名， %t添加时间戳， %%添加一个普通的%`level`设置使用的数值（查看日志详细级别`-loglevel` ）。</code></pre><p>例如，要输出到名为<code>ffreport.log</code>使用的一个日志级别文件的报告32 （日志级别info的别称 ）：</p><p><code>FFREPORT=file=ffreport.log:level=32 ffmpeg -i input output</code></p><p>非致命的环境变量的解析错误不会出现在报告中。</p><p><code>-hide_banner</code></p><p>不打印横幅。</p><p>所有FFmpeg的工具通常会显示一个版权声明，构建选项和库版本。此选项可以用来抑制打印此信息。</p><p><code>-cpuflags flags (global)</code></p><p>允许设置和清除CPU标志。此选项用于测试。不要使用它，除非你知道自己在做什么。</p><pre><code>ffmpeg -cpuflags -sse+mmx ...ffmpeg -cpuflags mmx ...ffmpeg -cpuflags 0 ...</code></pre><p>可能选项有：</p><pre><code>`x86`    ‘mmx’    ‘mmxext’    ‘sse’    ‘sse2’    ‘sse2slow’    ‘sse3’    “sse3slow”    ‘ssse3’    ‘atom’    ‘sse4.1’    ‘sse4.2’    ‘avx’    ‘xop’    ‘fma4’    ‘3dnow’    ‘3dnowext’    ‘cmov’`ARM`    ‘armv5te’    ‘armv6’    ‘armv6t2’    ‘vfp’    ‘vfpv3’    ‘neon’    ‘PowerPC’    ‘altivec’`Specific Processors`    ‘pentium2’    ‘pentium3’    ‘pentium4’    ‘k6’    ‘k62’    ‘athlon’    ‘athlonxp’    ‘k8’</code></pre><p><code>-opencl_bench</code></p><p>测试所有可用的OpenCL设备并显示结果。此选项仅当FFmpeg含有–enable-opencl 编译时可用。</p><p><code>-opencl_options options (global)</code></p><p>设置的OpenCL环境选项。此选项仅当FFmpeg的已编译–enable-opencl 。</p><p>options必须是冒号分隔的key = value选项对。参见ffmpeg-utils的手册中的“OpenCL的选项”部分的内容。</p><h3 id="5-3-AVOption选项"><a href="#5-3-AVOption选项" class="headerlink" title="5.3 AVOption选项"></a>5.3 AVOption选项</h3><p>这些选项直接由<code>libavformat</code>，<code>libavdevice</code>和<code>libavcodec</code>库提供。要查看可用AVOption的列表，请使用<code>-help</code>选项。它们被分为两类：</p><p>generic 这些选项可以为任何容器，编解码器或设备进行设置。通用的选项都列在AVFormatContext选择容器/设备和AVCodecContext选择编解码器小节。</p><p>private 这些选项是作用于给定的容器，装置或编解码器。私有选项列在其相应的容器/设备/编解码器。</p><p>比如编辑ID3v2.3头而不是默认的ID3v2.4到MP3文件，使用MP3混合器的<code>id3v2_version</code>私有选项：</p><p><code>ffmpeg -i input.flac -id3v2_version 3 out.mp3</code></p><p>所有编解码器AVOption选项是按流指定的，并且因此应该指定相应的流标识符。</p><p>注： <code>-nooption</code>语法不能用于布尔AVOption选项，请使用<code>-option 0/-option 1</code> 。</p><p>注：老的前缀v/a/s的流标识记号已经过时，将被移除。</p><h3 id="5-4-主要选项"><a href="#5-4-主要选项" class="headerlink" title="5.4 主要选项"></a>5.4 主要选项</h3><p><code>-f fmt (input/output)</code></p><p>强制指定输入或输出的文件格式。输入文件的格式通常是自动检测的，输出文件的格式由该文件的扩展名猜测，所以在大多数情况下不需要此选项。</p><p><code>-i filename (input)</code></p><p>输入的文件名</p><p><code>-y (global)</code></p><p>直接覆盖输出文件。</p><p><code>-n (global)</code></p><p>如果指定的输出文件已经存在，不要覆盖输出文件，并立即退出。</p><p><code>-c[:stream_specifier] codec (input/output,per-stream)</code></p><p><code>-codec[:stream_specifier] codec (input/output,per-stream)</code></p><p>用于对一个或多个数据流指定一个编码器（一个输出文件之前使用时）或一个解码器（一个输入文件之前使用时）。 codec是一个解码器/编码器的名称或特殊值copy （仅输出），copy表示该流不是被重新编码。</p><p>例如</p><pre><code>`ffmpeg -i INPUT -map 0 -c:v libx264 -c:a copy OUTPUT`使用libx264编码所有视频流并拷贝所有音频流。</code></pre><p>对于每个数据流，最后匹配的<code>c</code>选项被应用，所以</p><pre><code>`ffmpeg -i INPUT -map 0 -c copy -c:v:1 libx264 -c:a:137 libvorbis OUTPUT`将复制的所有流除了第二视频流，并将用libx264进行编码，并且对于第138个音频流用libvorbis进行编码。</code></pre><p><code>-t duration (input/output)</code></p><p>当作为输入选项（在<code>-i</code>之前 ），限制从输入文件中读取数据的<code>duration</code>。</p><p>当作为输出选项（输出文件名之前）使用，在达到<code>duration</code>后停止写入输出文件。</p><p><code>duration</code>可能是以秒为单位，或以<code>hh:mm:ss[.xxx]</code>形式出现。</p><p><code>-to</code>和<code>-t</code>是相互排斥的，<code>-t</code>具有优先权。</p><p><code>-to position (output)</code></p><p>在<code>position</code>位置停止输出。 <code>position</code>可能是一个表示秒数的数，或<code>hh:mm:ss[.xxx]</code>形式。</p><p><code>-to</code>和<code>-t</code>是相互排斥的，<code>-t</code>具有优先权。</p><p><code>-fs limit_size (output)</code></p><p>设置文件大小限制，以字节表示。</p><p><code>-ss position (input/output)</code></p><p>当用作输入选项一起使用（在<code>-i</code> 以前），跳转到输入文件中<code>position</code> 位置。请注意，在大多数的格式是不可能确切定位，这样ffmpeg将寻求最接近的<code>position</code>位置点。当转码和<code>-accurate_seek</code>启用（默认设置），寻找点和<code>position</code>位置之间的附加段将被解码并丢弃。当进行流复制或当<code>-noaccurate_seek</code>被使用时，它都将被保留。</p><p>当用作输出选项（在输出文件名前），解码但丢弃输入直到时间戳到达的位置。</p><p><code>position</code>位置可以是秒数或hh:mm:ss[.xxx]形式。</p><p><code>-itsoffset offset (input)</code></p><p>设置输入时间偏移。</p><p><code>offset</code>必须是持续时间规范，请参阅（ffmpeg-utils）在FFmpeg-utils（1）手动的持续时间段的相关内容 。</p><p>偏移被添加到输入文件的时间戳。指定一个正偏移意味着相应流将延迟offset所指定的时间。</p><p><code>-timestamp date (output)</code></p><p>设置在容器内记录的时间戳。</p><p>date必须是一个规范的持续时间，请参阅（ffmpeg-utils）在FFmpeg-utils的（1）日期部分 。</p><p><code>-metadata[:metadata_specifier] key=value (output,per-metadata)</code></p><p>设置元数据的键/值对。</p><p>可选<code>metadata_specifier</code>可以被用于设置流或章节的元数据。见<code>-map_metadata</code>文档的详细信息。</p><p>此选项将覆盖<code>-map_metadata</code>设置的元数据。另外，也可以通过使用空值来删除元数据。</p><pre><code>例如，设置输出文件的标题：`ffmpeg -i in.avi -metadata title=&quot;my title&quot; out.flv`设置第一个音频流的语言：`ffmpeg -i INPUT -metadata:s:a:0 language=eng OUTPUT`</code></pre><p><code>-target type (output)</code></p><p>指定目标文件类型（ vcd ， svcd ， dvd ， dv ， dv50 ）。 类型可能与前缀<code>pal-</code> ， <code>ntsc-</code>或<code>film-</code>使用相应的标准。所有的格式选项（比特率，编解码器，缓冲大小）都将自动设置。你仅需键入：</p><pre><code>`ffmpeg -i myfile.avi -target vcd /tmp/vcd.mpg`不过，你可以指定其他选项，只需你知道他们与标准不冲突，如：ffmpeg -i myfile.avi -target vcd -bf 2 /tmp/vcd.mpg</code></pre><p><code>-dframes number (output)</code></p><p>设置输出数据的帧数。这是<code>-frames:d</code>的别名。</p><p><code>-frames[:stream_specifier] framecount (output,per-stream)</code></p><p>在<code>framecount</code>帧后停止写入流。</p><p><code>-q[:stream_specifier] q (output,per-stream)</code> <code>-qscale[:stream_specifier] q (output,per-stream)</code></p><p>使用固定编码率（VBR）。<code>q/qscale</code>的意思与编解码器定义相关。如果<code>qscale</code>不与<code>stream_specifier</code>联用，那么它仅适用于视频流，这是为了保持兼容性。另外将相同的编码器参数赋给两个不同的编解码器通常并不是用户想要的，因此若需要这样的功能，可以使用流标识符(<code>stream_specifier</code>)来指定。</p><p><code>-filter[:stream_specifier] filtergraph (output,per-stream)</code></p><p>创建由FilterGraph指定的滤镜组并使用它。</p><p>FilterGraph是作用于流的滤镜组的描述，而且必须有一个单一的输入和同一类型的数据流输出。</p><p>在滤镜组里，输入被关联到<code>in</code>标签 ，输出到<code>out</code>标签 。关于ffmpeg滤镜组的语法可参见ffmpeg-filters的手册。</p><p>如果你想创建具有多个输入或输出的滤镜组，参见<code>-filter_complex</code>的相关选项。</p><p><code>-filter_script[:stream_specifier] filename (output,per-stream)</code></p><p>这个选项类似于<code>-filter</code>，唯一的区别是，它的参数是滤镜组所在的文件名。</p><p><code>-pre[:stream_specifier] preset_name (output,per-stream)</code></p><p>指定匹配流（S）的预设。</p><p><code>-stats (global)</code></p><p>打印编码进度/统计数据。这是默认值，你可通过指定<code>-nostats</code>禁用 。</p><p><code>-progress url (global)</code></p><p>发送程序友好的进展信息到<code>url</code></p><p>进度信息大约每秒和编码过程结束后写入。它是由<code>key = value</code>行组成。 key只能包含字母和数字字符。最后一个关键字序列始终是<code>progress</code>。</p><p><code>-stdin</code></p><p>启用标准输入交互。这是默认设置，除非标准输入被作为输入。要明确禁用互动，你需要指定<code>-nostdin</code>。</p><p>在标准输入禁用相互作用是很有用的，例如，如果ffmpeg在后台进程组中。大致相同的结果可以用<code>ffmpeg ... &lt; /dev/null</code>实现，但它需要一个终端。</p><p><code>-debug_ts (global)</code></p><p>打印时间戳信息。它默认是关闭的。此选项主要是用于测试和调试目的，输出格式可从一个版本切换到另一个，所以它不应该在可移植脚本中使用。</p><p>参见<code>-fdebug ts</code>选项。</p><p><code>-attach filename (output)</code></p><p>添加一个附件到输出文件。这仅由几个格式支持。例如Matroska格式，这个文件可以是用来渲染字幕使用的字体。附件被实现为数据流的一个特定类型的，因此该选项将增加一个新的流的文件。因此，可以以通常的方式在此流使用每个流的选项。使用此选项创建的附件流将所有的其他流之后创建（也即那些由<code>-map</code>或自动映射创建的流）。</p><p>请注意，对于Matroska，你还必须设置<code>mimetype</code>元数据标签：</p><pre><code>`ffmpeg -i INPUT -attach DejaVuSans.ttf -metadata:s:2 mimetype=application/x-truetype-font out.mkv`（假设该附件流将是输出文件的第三个流）。</code></pre><p><code>-dump_attachment[:stream_specifier] filename (input,per-stream)</code></p><p>提取匹配的附件流成一个名为<code>filename</code>的文件。如果<code>filename</code>是空的，那么元数据标签中的<code>filename</code>标签的值将被使用。</p><pre><code>例如，提取第一附件到“out.ttf”：`ffmpeg -dump_attachment:t:0 out.ttf -i INPUT`提取所有附件，并按照filename 标签命名文件：`ffmpeg -dump_attachment:t &quot;&quot; -i INPUT`</code></pre><p>技术说明 - 附件是通过编解码器额外数据实现的，所以这个选项实际上可以用来提取任意额外数据，而不仅仅是附件。</p><h3 id="5-5-视频选项"><a href="#5-5-视频选项" class="headerlink" title="5.5 视频选项"></a>5.5 视频选项</h3><p><code>-vframes number (output)</code></p><p>设置视频帧的输出数量。这是的<code>-frames:v</code>别名 。</p><p><code>-r[:stream_specifier] fps (input/output,per-stream)</code></p><p>设置帧速率（Hz值，分数或缩写）。</p><p>作为输入选项，忽略存储在文件中的任何时间戳，而是产生时间戳假设恒定的帧速率fps这与一些输入格式，诸如image2或者v4l2，中的<code>-framerate</code>选项是不同的（在老版本的ffmpeg中是一样的）。如有疑问，使用的输入选项<code>-framerate</code>代替<code>-r</code>。</p><p>作为输出选项，复制或删除输入帧以达到恒定的输出帧速率fps。</p><p><code>-s[:stream_specifier] size (input/output,per-stream)</code></p><p>设置帧尺寸。</p><p>作为输入的选项，这是私有选项<code>video_size</code>的快捷方式，部分分流器可以识别该参数，这时帧大小或者未存储在文件中或不可配置,例如原始视频或视频采集卡。</p><p>作为输出的选择，这将插入<code>scale</code>视频滤镜到相应滤镜组的末端 。如需改变滤镜位置，请将<code>scale</code>滤镜直接插入到开头或其他地方。</p><p>格式是<code>wxh</code>（默认值是使用与源相同的尺寸）。</p><p><code>-aspect[:stream_specifier] aspect (output,per-stream)</code></p><p>设置指定视频显示的宽高比<code>aspect</code>。</p><p><code>aspect</code>可以是一个浮点数，或使用形如<code>num: den</code>的形式，其中<code>num</code>是分子，<code>den</code>是分母。例如，“4:3”，“16:9”，“1.3333”，和“1.7777”都是有效的参数值。</p><p>如果与<code>-vcodec copy</code>一起使用，这会影响储存在容器级别的宽高比，而不是存储在编码帧的宽高比，如果有的话。</p><p><code>-vn (output)</code></p><p>禁用视频录制。</p><p><code>-vcodec codec (output)</code></p><p>设置视频编解码器。这是-codec:v的别名。</p><p><code>-pass[:stream_specifier] n (output,per-stream)</code></p><p>选择通过次数（1或2）。它是用来做两遍视频编码。视频的统计信息记录在第一次编码时写入到日志文件中（也参见选项<code>-passlogfile</code>），在第二次编码时，该日志文件被用于按要求生成准确比特率的视频。在第一次编码中，你可以通过禁用音频并将输出设置为null，下面为Windows和Unix的例子：</p><pre><code>`ffmpeg -i foo.mov -c:v libxvid -pass 1 -an -f rawvideo -y NUL``ffmpeg -i foo.mov -c:v libxvid -pass 1 -an -f rawvideo -y /dev/null`</code></pre><p><code>-passlogfile[:stream_specifier] prefix (output,per-stream)</code></p><p>设置二次编码的日志文件名 前缀为<code>prefix</code> ，默认的文件名 前缀是<code>ffmpeg2pass</code>。完整的文件名 将是<code>PREFIX-N.log</code>，其中<code>N</code>是一个输出流的特定数字</p><p><code>-vf filtergraph (output)</code></p><p>创建由<code>filtergraph</code>指定的滤镜组，并使用它。</p><p>这是<code>-filter:v</code>的别名 ，参见<code>-filter</code>选项。</p><h3 id="5-6-高级视频选项"><a href="#5-6-高级视频选项" class="headerlink" title="5.6 高级视频选项"></a>5.6 高级视频选项</h3><p><code>-pix_fmt[:stream_specifier] format (input/output,per-stream)</code></p><p>设置的像素格式。使用<code>-pix_fmts</code>显示所有支持的像素格式。如果所选择的像素格式不能被选择，ffmpeg将打印警告，并选择由编码器所支持的最好的像素格式。如果<code>pix_fmt</code>前缀<code>+</code> ，如果请求的像素格式不能被选中，ffmpeg会出现错误退出，同时滤镜组中的自动转换将被禁用。如果<code>pix_fmt</code>是一个单一的<code>+</code> ，ffmpeg的选择与输入或者滤镜输出相同的像素格式，并将禁用自动转换。</p><p><code>-sws_flags flags (input/output)</code></p><p>设置软件缩放的标志。</p><p><code>-vdt n</code></p><p>丢弃的阈值。</p><p><code>-rc_override[:stream_specifier] override (output,per-stream)</code></p><p>覆盖特定的时间间隔内的帧率控制，格式为用斜杠分隔的“整型，整型，整型”列表。前两个值是在开始和结束帧编号，最后一个如果是正数，则为量化器；负数则为品质因数。</p><p><code>-ilme</code></p><p>支持编码器force interlacing（仅对MPEG-2和MPEG-4有效）。如果你的输入文件是隔行并要保持隔行格式最小损失，请使用此选项。另一种方法是使用<code>-deinterlace</code>反交错输入流，但这会引入损失。</p><p><code>-psnr</code></p><p>计算压缩帧的PSNR（伪信噪比）。</p><p><code>-vstats</code></p><p>输出视频编码统计到<code>vstats_HHMMSS.log</code>。</p><p><code>-vstats_file file</code></p><p>输出频编码统计到<code>file</code>。</p><p><code>-top[:stream_specifier] n (output,per-stream)</code></p><p>top=1/bottom=0/auto=-1 field first</p><p><code>-dc precision</code></p><p>Intra_dc_precision.</p><p><code>-vtag fourcc/tag (output)</code></p><p>强制视频 tag/ fourcc。这是<code>-tag:v</code>的别名。</p><p><code>-qphist (global)</code></p><p>显示QP直方图</p><p><code>-vbsf bitstream_filter</code></p><p>已抛弃，见<code>-bsf</code></p><p><code>-force_key_frames[:stream_specifier] time[,time...] (output,per-stream)</code></p><p><code>-force_key_frames[:stream_specifier] expr:expr (output,per-stream)</code></p><p>强制关键帧在指定的时间戳，更精确地在每个指定的时间之后的第一帧。</p><p>如果参数的前缀<code>expr:</code>，<code>expr</code>将解释为一个表达式，并在每一帧执行。如果运行结果非零，一个关键帧被强制加入。</p><p>如果时间之一是<code>chapters [ delta ]</code>，它被扩展成的文件中的所有章节开始通过<code>delta</code>偏移的时间（时间以秒为单位）。这个选项可能是有用的，以确保存在一个搜索点位于章节标记或者在输出文件中的任何其它指定的地方。</p><p>例如，在5分钟插入一个关键帧，同时在每章开始前0.1秒插入一个关键帧：</p><pre><code>`-force_key_frames 0:05:00,chapters-0.1`</code></pre><p><code>expr</code>中可以包含以下常量：</p><pre><code>`n`当前处理的帧的数量，从0开始`n_forced`强制帧的数量`prev_forced_n`先前强制帧的数目，当没有强制的关键帧时，它是NAN`prev_forced_t`先前强制帧的时间，当没有强制关键帧时，它是NAN`t`当前处理的帧的时间例如每5秒强制插入一个关键帧，你可以使用：`-force_key_frames expr:gte(t,n_forced*5)`自13秒开始，强制插入一个关键帧在上一强制关键帧后5秒：`-1-force_key_frames expr:if(isnan(prev_forced_t),gte(t,13),gte(t,prev_forced_t+5))`</code></pre><p>需要注意的是太多强制的关键帧对某些编码器的超前算法是非常有害的：使用固定的GOP选项或类似选项会更有效。</p><p><code>-copyinkf[:stream_specifier] (output,per-stream)</code></p><p>当复制流时，也复制起始处的非关键帧。</p><p><code>-hwaccel[:stream_specifier] hwaccel (input,per-stream)</code></p><p>使用硬件加速解码匹配流。<code>hwaccel</code>的允许值包括：</p><pre><code>`none`不要使用任何硬件加速（默认）。`auto`自动选择硬件加速的方法。`vda`使用苹果VDA硬件加速。`vdpau`使用VDPAU（视频解码和演示API对于Unix）硬件加速。`dxva2`使用DXVA2（DirectX视频加速）硬件加速。</code></pre><p>如果选择的hwaccel不可用或不支持选择的解码器，此选项没有效果。</p><p>注意，大多数加速方法适用于播放，但并不会比现代CPU软件解码更快。此外， ffmpeg通常需要解码帧从GPU存储器复制到系统存储器，从而导致进一步的性能损失。因此此选项主要用于测试。</p><p><code>-hwaccel_device[:stream_specifier] hwaccel_device (input,per-stream)</code></p><p>选择一个设备使用硬件加速。</p><p>此选项仅使得同时与<code>-hwaccel</code>选项使用。它的确切含义取决于所选择的具体硬件加速方法。</p><pre><code>`vdpau`对于VDPAU，此选项在X11中使用。如果没有指定这个选项， DISPLAY环境变量的值被使用`dxva2`对于DXVA2，这个选项应包含在显示适配器使用的数量。如果未指定此选项，默认的适配器将被使用。</code></pre><h3 id="5-7-音频选项"><a href="#5-7-音频选项" class="headerlink" title="5.7 音频选项"></a>5.7 音频选项</h3><p><code>-aframes number (output)</code></p><p>设定的音频帧输出的数目。这是<code>-frames:a</code> 的别名。</p><p><code>-ar[:stream_specifier] freq (input/output,per-stream)</code></p><p>设置音频采样频率。对于输出流，将默认设置为相应的输入流的采样频率。对于输入流该选项仅对音频抓取设备和raw格式分流器和映射到相应分流器上的选项有效。</p><p><code>-aq q (output)</code></p><p>设置音频质量（与编解码器有关，VBR）。这是<code>-q:a</code>的别名。</p><p><code>-ac[:stream_specifier] channels (input/output,per-stream)</code></p><p>设置音频通道的数目。输出流默认将设置为输入音频信道的数目。对于输入流该选项仅对音频抓取设备和raw格式分流器和映射到相应分流器上的选项有效。</p><p><code>-an (output)</code></p><p>禁用录音。</p><p><code>-acodec codec (input/output)</code></p><p>设置音频解码器。这是<code>-codec:a</code>的别名。</p><p><code>-sample_fmt[:stream_specifier] sample_fmt (output,per-stream)</code></p><p>设置音频样本格式。使用<code>-sample_fmts</code>得到支持的采样格式的列表。</p><p><code>-af filtergraph (output)</code></p><p>创建由FilterGraph指定的滤镜组并使用它。</p><p>这是<code>-filter:a</code>的别名 ，参见<code>-filter</code>选项。</p><h3 id="5-8-高级音频选项"><a href="#5-8-高级音频选项" class="headerlink" title="5.8 高级音频选项"></a>5.8 高级音频选项</h3><p><code>-atag fourcc/tag (output)</code></p><p>强制音频<code>tag/fourcc</code>值。这是<code>-tag:a</code>的别名。</p><p><code>-absf bitstream_filter</code></p><p>已过时，参见<code>-bsf</code></p><p><code>-guess_layout_max channels (input,per-stream)</code></p><p>如果一些输入通道布局是未知的，试图猜测它最多的声道数量。例如，2要求ffmpeg识别1个通道为单声道和2声道立体声，6声道作为5.1。默认是总是试图去猜测。用0来禁用所有的猜测。</p><h3 id="5-9-字幕选项"><a href="#5-9-字幕选项" class="headerlink" title="5.9 字幕选项"></a>5.9 字幕选项</h3><p><code>-scodec codec (input/output)</code></p><p>设置字幕的编解码器。这是<code>-codec:s</code>的别名。</p><p><code>-sn (output)</code></p><p>关闭字幕记录。</p><p><code>-sbsf bitstream_filter</code></p><p>已过时，参见<code>-bsf</code></p><h3 id="5-10-高级字幕选项"><a href="#5-10-高级字幕选项" class="headerlink" title="5.10 高级字幕选项"></a>5.10 高级字幕选项</h3><p><code>-fix_sub_duration</code></p><p>调整字幕的持续时间。对于每个字幕，等待在相同的流中的下一个分组，并调节第一持续时间，以避免重叠。这对某些字幕编解码器是必要的，特别是数字电视广播字幕(DVB)，因为在原来的分组的持续时间仅仅是一个粗略的估计，结束标记是通过一个空的字幕帧完成的。没有使用这个选项在必要时可导致夸张的持续时间，或由于非单调时间戳混流故障。</p><p>注意，此选项将延迟所有数据的输出直到下一个字幕分组被解码：它可能会增加内存消耗和延迟。</p><p><code>-canvas_size size</code></p><p>设置用于呈现字幕的画布的大小。</p><h3 id="5-11-高级选项"><a href="#5-11-高级选项" class="headerlink" title="5.11 高级选项"></a>5.11 高级选项</h3><p><code>-map [-]input_file_id[:stream_specifier][,sync_file_id[:stream_specifier]] | [linklabel] (output)</code></p><p>指定一个或多个输入流作为用于输出文件的来源。每个输入流由输入文件索引<code>input_file_id</code>和输入流索引<code>input_stream_id</code>标识。这两个指标都是从0开始。<code>sync_file_id：stream_specifier</code>可用于指定输入流作为同步参考。</p><p>在命令行上第一个<code>-map</code>选项指定的输出流0，第二<code>-map</code>选项指定的源输出流1等</p><p>一个连字符<code>-</code>创建一个“否定的”的映射。它禁用从已经建立映射中匹配流。</p><p>另一种<code>LinkLabel</code>的形式将映射从复杂滤波器组输出到输出文件（见<code>-filter_complex</code>选项）。 <code>LinkLabel</code>必须对应于一个链路中已定义的输出标签。</p><pre><code>例如，映射第一输入文件的所有流到输出ffmpeg -i INPUT -map 0 output例如，如果在第一输入文件中的两个音频流，这些流记为“0：0”，“0：1”。您可以使用-map选择哪个流输出到输出文件。例如：ffmpeg -i INPUT -map 0:1 out.wav将映射INPUT的输入数据流“0:1”到在（单）输出流out.wav。例如，从输入文件a.mov选择索引2流（由识别符“0：2”指定的）与从输入b.mov索引6（由识别符“1：6”指定）流，复制到输出文件out.mov：ffmpeg -i a.mov -i b.mov -c copy -map 0:2 -map 1:6 out.mov选择所有的视频和输入文件中的第三音频流：ffmpeg -i INPUT -map 0:v -map 0:a:2 OUTPUT映射所有的数据流，除了第二音频，使用否定的映射ffmpeg -i INPUT -map 0 -map -0:a:1 OUTPUT选择英语音频流：ffmpeg -i INPUT -map 0:m:language:eng OUTPUT</code></pre><p>请注意，使用此选项将禁用此输出文件的默认映射。</p><p><code>-map_channel [input_file_id.stream_specifier.channel_id|-1][:output_file_id.stream_specifier]</code></p><p>映射从一个给定的输入音频声道到输出。如果<code>output_file_id.stream_specifier</code>未设置时，声道将被映射的所有音频流。</p><p>使用<code>-1</code>替代<code>input_file_id.stream_specifier.channel_id</code>将映射一个静音声道。</p><pre><code>例如，假设INPUT是一个立体声音频文件，你可以切换两个音频通道与下面的命令：ffmpeg -i INPUT -map_channel 0.0.1 -map_channel 0.0.0 OUTPUT如果您想要静音的第一声道，并保留第二个：ffmpeg -i INPUT -map_channel -1 -map_channel 0.0.1 OUTPUT</code></pre><p><code>-map_channel</code>选项的顺序指定在输出流中的声道的顺序。所述输出信道的布局是从映射信道数猜测的（如果只有一个<code>-map_channel</code>，则使用单声道，如果是2，则使用立体声，如此等等）。如果输入和输出信道的布局不匹配（例如2个<code>-map_channel</code>和<code>-ac 6</code>），联合使用<code>-map_channel</code>和<code>-ac</code>将更新声道的增益水平。</p><p>您也可以提取各个输入通道到特定的输出；以下命令中提取的两个通道的INPUT音频流（文件0，流0）到相应的<code>OUTPUT_CH0</code>和<code>OUTPUT_CH1</code>输出：</p><pre><code>ffmpeg -i INPUT -map_channel 0.0.0 OUTPUT_CH0 -map_channel 0.0.1 OUTPUT_CH1</code></pre><p>以下示例拆分立体声输入的通道成两个独立的数据流，其被放入同一个输出文件：</p><pre><code>ffmpeg -i stereo.wav -map 0:0 -map 0:1 -map_channel 0.0.0:0.0 -map_channel 0.0.1:0.1 -y out.ogg</code></pre><p>注意，目前每个输出流只能包含来自单个输入流的声道；不能使用<code>-map_channel</code>从不同的数据流提取的多个输入音频声道（来自相同的或不同的文件），并将它们合并成一个单一的输出流。因此，目前不可能完成例如把两个单独的单声道流成一个单一的立体声流的任务。然而，拆分立体声流分成两个单信道单声道流则是可能的。</p><p>如果你需要这个功能，一个可能的解决方法是使用<code>amerge</code>滤镜。例如，如果你需要用2个单声道音频融合媒体中的（在这里是input.mkv）流成一个单一的立体声声道音频流（并保持视频流），可以使用下面的命令：</p><pre><code>ffmpeg -i input.mkv -filter_complex &quot;[0:1] [0:2] amerge&quot; -c:a pcm_s16le -c:v copy output.mkv</code></pre><p><code>-map_metadata[:metadata_spec_out] infile[:metadata_spec_in] (output,per-metadata)</code></p><p>根据<code>infile</code>设置下一个输出文件的元数据信息。请注意，这些都是文件索引（从零开始），而不是文件名。可选参数<code>metadata_spec_in/out</code>可用于指定哪些元数据进行复制。元数据说明可以有以下几种形式：</p><pre><code>`g`全局元数据，即元数据应用于整个文件`s[:stream_spec]`每个流的元数据`stream_spec`是一个流标识符，参见流标识符一章。在输入的元数据时，将从第一个匹配的流复制。在输出元数据时，将复制到所有匹配流。`c:chapter_index`每章的元数据`chapter_index`是从零开始的章节索引。`p:program_index`每个项目的元数据`program_index`是从零开始的的项目索引。</code></pre><p>如果元数据说明被省略，则默认为全球性的。</p><p>缺省情况下，全局元数据是从第一输入文件复制的，每个流和每个章节的元数据与数据流/章节将被依次复制。创建相关类型的任何映射将禁用这些默认映射。否定的文件索引可以用来创建只禁用自动复制的虚拟映射。</p><pre><code>例如，从输入文件的第一数据流复制元数据到输出文件的全局元数据：ffmpeg -i in.ogg -map_metadata 0:s:0 out.mp3反过来，即全局的元数据复制到所有音频流：ffmpeg -i in.mkv -map_metadata:s:a 0:g out.mkv需要注意的是0在本实例中能起到相同的效果，由于全局元数据被假定默认。</code></pre><p><code>-map_chapters input_file_index (output)</code></p><p>从输入文件复制章节与索引<code>input_file_index</code>到下一个输出文件中。如果不指定章节映射，则章节从第一输入文件复制并至少有一个章节。使用负的文件索引来排除任何章节的复制。</p><p><code>-benchmark (global)</code></p><p>在编码结束显示基准信息。显示使用的CPU时间和最大内存消耗。不是所有系统都支持最大内存消耗，如果不支持，它通常会显示为0。</p><p><code>-benchmark_all (global)</code></p><p>显示编码过程中基准信息。显示各个步骤（音频/视频编码/解码）所使用的CPU时间。</p><p><code>-timelimit duration (global)</code></p><p>在已经运行<code>duration</code>秒后退出ffmpeg</p><p><code>-dump (global)</code></p><p>转储每个输入包到标准错误流。</p><p><code>-hex (global)</code></p><p>当dump包时，也dump有效载荷。</p><p><code>-re (input)</code></p><p>读取输入的原始帧速率。主要用于模拟抓取设备。或实时输入流（例如从文件读取时）。不应该在实际的抓取设备或实时输入流中使用（它可能会导致数据包丢失）。默认ffmpeg尝试尽可能快地读出输入端。此选项会减慢输入的本地帧速率的读取。它是用于实时输出（如直播）是有用的。</p><p><code>-loop_input</code></p><p>循环输入流。目前，它仅适用于图像流。此选项用于自动测试<code>ffserver</code>。此选项已被弃用，使用<code>-loop 1</code>。</p><p><code>-loop_output number_of_times</code></p><p>反复循环输出支持循环的格式如动画GIF（0意味着无限循环输出）。此选项已被弃用，使用<code>-loop</code>。</p><p><code>-vsync parameter</code></p><p>视频同步方法。出于兼容性考虑旧值可以被指定为数字。新添加的值将必须总是指定为字符串。</p><pre><code>0, passthrough每一帧传递从分流器到复用器的时间戳。1, cfr帧将被复制并下降至达到完全所需的恒定帧速率。2, vfr帧通过与其时间戳或下降，从而防止两帧具有相同的时间戳。drop与passthrough相同，但破坏所有时间戳，使得复用器生成基于帧速率新的时间戳。-1, auto根据复用器的功能选择1或者2。这是默认的方法。</code></pre><p>注意，该时间戳在此之后可以进一步由复用器修改。例如，在格式选项<code>avoid_negative_ts</code>被启用时。</p><p>与<code>-map</code>联用，您可以选择从哪个流提取时间戳。您可以留下视频或音频不变，同步剩余流不变的。</p><p><code>-async samples_per_second</code></p><p>音频同步的方法。“伸展/挤压”音频流相匹配的时间戳，所述参数是音频发生改变所容许的每秒最大点数。<code>-async 1</code>是一种特殊情况，音频数据流仅在开始校正，而其后将不再校正。</p><p>注意，该时间戳在此之后可以进一步由复用器修改。例如，在格式选项<code>avoid_negative_ts</code>被启用时。</p><p>此选项已被弃用。使用<code>aresample</code>音频过滤器代替。</p><p><code>-copyts</code></p><p>不要处理输入时间戳，但保持它们的值，不尝试对它们进行sanitize。尤其是，不要删除初始启动时间偏移值。</p><p>需要注意的是，即使使用了该选项，根据不同的<code>vsync</code>选项或对特定复用器处理（例如格式选项<code>avoid_negative_ts</code>被启用）输出时间戳与输入可能不匹配时间戳。</p><p><code>-start_at_zero</code></p><p>当与<code>copyts</code>使用，调整输入时间戳，使他们从零开始。</p><p>这意味着使用例如<code>-ss 50</code>将使输出时间戳开始50秒时，不管输入文件开始处的时间戳是多少。</p><p><code>-copytb mode</code></p><p>指定在拷贝流时如何设置编码器的时间基准，mode是一个整型，可以假定为下列值之一：</p><pre><code> 1使用分流器的时间基准。时间基准从相应的输入分流器复制到输出编码器。该情况下，对可变帧速率的视频流的复制，有时需要避免非单调的时间戳。0使用解码器时间基准。时间基准从相应的输入解码器复制到输出编码器。-1尝试自动做出选择，以便产生一个合理的输出。</code></pre><p>默认值为-1。</p><p><code>-shortest (output)</code></p><p>最短的输入流结束时完成编码。</p><p><code>-dts_delta_threshold</code></p><p>时间戳间断门槛。</p><p><code>-muxdelay seconds (input)</code></p><p>设置最大分流解码延时。</p><p><code>-muxpreload seconds (input)</code></p><p>设定初始解码分流延迟。</p><p><code>-streamid output-stream-index:new-value (output)</code></p><p>分配一个新流id值到输出流。该选项需指定到输出文件名之前。对于在多个输出文件存在的情况下，一个流id将被重新分配到不同的值。</p><pre><code>例如，要设置流0 到流33，同时流1至流36，到MPEGTS格式的输出文件：ffmpeg -i infile -streamid 0:33 -streamid 1:36 out.ts</code></pre><p><code>-bsf[:stream_specifier] bitstream_filters (output,per-stream)</code></p><p>指定匹配流的<code>bitstream_filters</code>。它是一个逗号分隔的滤镜列表。使用<code>-bsfs</code>选项得到的滤镜列表。</p><pre><code>ffmpeg -i h264.mp4 -c:v copy -bsf:v h264_mp4toannexb -an out.h264ffmpeg -i file.mov -an -vn -bsf:s mov2textsub -c:s copy -f rawvideo sub.txt</code></pre><p><code>-tag[:stream_specifier] codec_tag (input/output,per-stream)</code></p><p>指定匹配流的<code>tag/fourcc</code>。</p><p><code>-timecode hh:mm:ssSEPff</code></p><p>指定写入的时间码。对于non drop 时间码，分隔符SEP是<code>:</code> 对于drop时间码是<code>;</code> （或<code>.</code>）。</p><pre><code>ffmpeg -i input.mpg -timecode 01:02:03.04 -r 30000/1001 -s ntsc output.mpg</code></pre><p><code>-filter_complex filtergraph (global)</code></p><p>定义复杂FilterGraph，即具有输入和/或输出任意个数的滤镜组。对于简单滤镜——那些具有一个输入和相同类型的一个输出的滤镜——参见<code>-filter</code>选项。FilterGraph是FilterGraph的描述，参见FFMPEG滤镜手册的“FilterGraph语法”部分中的描述。</p><p>输入链路标签必须关联到使用<code>[file_index:stream_specifier]</code>语法标记的输入流（即-map选项中使用的格式）。如果<code>stream_specifier</code>匹配多个流，第一个将被使用。未标记的输入将被连接到匹配类型的第一未使用的输入流。</p><p>输出链接标签由<code>-map</code>指定。未标记的输出被加到第一输出文件。</p><p>注意，使用该选项，可以只有lavfi源而没有正常的输入文件。</p><pre><code>例如，叠加图像到视频    ffmpeg -i video.mkv -i image.png -filter_complex &#39;[0:v][1:v]overlay[out]&#39; -map &#39;[out]&#39; out.mkv这里[0:v]指的是在第一输入文件中的第一视频流，这是与覆盖滤波器的第一（主）输入。同样，在第二输入的第一视频流链接到覆盖层的第二（覆盖）输入。假定在每个输入文件中只有一个视频流，就可以省略输入标签，因此上述命令相当于ffmpeg -i video.mkv -i image.png -filter_complex &#39;overlay[out]&#39; -map&#39;[out]&#39; out.mkv此外，我们可以省略输出标签和单输出的滤镜，它将被自动添加到输出文件，所以我们可以简单地写ffmpeg -i video.mkv -i image.png -filter_complex &#39;overlay&#39; out.mkv要使用lavfi产生5秒钟的纯红色视频color来源：ffmpeg -filter_complex &#39;color=c=red&#39; -t 5 out.mkv</code></pre><p><code>-lavfi filtergraph (global)</code></p><p>定义复杂FilterGraph，即具有输入和/或输出任意个数的滤镜组。相当于<code>-filter_complex</code>。</p><p><code>-filter_complex_script filename (global)</code></p><p>这个选项类似于<code>-filter_complex</code>，唯一的区别是，它的参数是包含被读取FilterGraph的文件的名称。</p><p><code>-accurate_seek (input)</code></p><p>此选项启用或禁用准确寻求输入文件，配合<code>-ss</code>选项使用。它默认是启用的，所以当转码时是准确的。使用<code>-noaccurate_seek</code>禁用它，在拷贝一些数据流和转码时，这可能是有用的。</p><p><code>-override_ffserver (global)</code></p><p>从ffserver 覆盖输入规格。使用这个选项，你可以映射任何输入流到ffserver，并从ffmpeg 控制编码的许多方面。如果没有这个选项ffmpeg将发送ffserver要求的内容。</p><p>该选项用于那些不能被指定到ffserver的功能 ，即便他们可以用于ffmpeg 。</p><p><code>-discard (input)</code></p><p>允许在分路器丢弃特定流或流帧。不是所有的分路器都支持。</p><pre><code>none禁止丢弃帧。default默认情况下，不丢弃帧。noref丢弃所有非参考帧。bidir放弃所有双向帧。nokey丢弃所有帧除了关键帧。all丢弃所有帧。</code></pre><p>作为一个特例，可以使用一个位图字幕流作为输入：它将被转换到文件中最大的视频相同尺寸，或720×576，如果没有视频。需要注意的是，这是一个实验性和临时解决方案。它会在libavfilter有适当字幕支持后删除。</p><pre><code>例如，硬编码存储在MPEG-TS格式的DVB-T的记录的顶部字幕，1秒延迟字幕：ffmpeg -i input.ts -filter_complex \  &#39;[#0x2ef] setpts=PTS+1/TB [sub] ; [#0x2d0] [sub] overlay&#39; \  -sn -map &#39;#0x2dc&#39; output.mkv（0x2d0，0x2dc和0x2ef分别是MPEG-TS的PID的视频，音频和字幕流; 0：0,0：3和0：7可起到同样作用）</code></pre><h3 id="5-12-预置文件"><a href="#5-12-预置文件" class="headerlink" title="5.12 预置文件"></a>5.12 预置文件</h3><p>预置文件是包含<code>option = value</code>的文件 ，每行一个，指定的选项需按照命令行中的顺序。’＃’开头的字符行被忽略，并用来提供注释。参见FFmpeg的源代码树中的preset目录中的例子。</p><p>Preset files are specified with the vpre, apre, spre, and fpre options. The fpre option takes the filename of the preset instead of a preset name as input and can be used for any kind of codec. For the vpre, apre, and spre options, the options specified in a preset file are applied to the currently selected codec of the same type as the preset option.</p><p>The argument passed to the vpre, apre, and spre preset options identifies the preset file to use according to the following rules:</p><p>First ffmpeg searches for a file named arg.ffpreset in the directories FFMPEGDATADIR(ifset),andFFMPEGDATADIR(ifset),andHOME/.ffmpeg, and in the datadir defined at configuration time (usually PREFIX/share/ffmpeg) or in a ffpresets folder along the executable on win32, in that order. For example, if the argument is libvpx-1080p, it will search for the file libvpx-1080p.ffpreset.</p><p>If no such file is found, then ffmpeg will search for a file named codec_name-arg.ffpreset in the above-mentioned directories, where codec_name is the name of the codec to which the preset file options will be applied.</p><h2 id="6-提示"><a href="#6-提示" class="headerlink" title="6 提示"></a>6 提示</h2><ul><li>为在非常低的比特率流，使用低帧速率和小的GOP大小。这对于那些配置较低的Linux用户上播放RealVideo尤是如此，它可能会丢帧。例如： <code>ffmpeg -g 3 -r 3 -t 10 -b:v 50k -s qcif -f rv10 /tmp/b.rm</code></li><li>编码中显示的参数“q”是当前量化器。值1表示非常良好的质量。值31表示最差的质量。如果Q = 31出现过于频繁，这意味着该编码器无法压缩到满足您的比特率要求的码率。您必须增加比特率，降低帧率或减少帧的大小。</li><li>如果你的电脑不够快，可以牺牲压缩比换取速度。你可以用<code>-me 0</code>加快运动估计和<code>-g 0</code>完全禁用运动估计（你仅有I-frames，这意味着它几乎和JPEG压缩一样好）。</li><li>可通过降低采样频率获得非常低的音频比特率（对于MPEG音频，下降到22050Hz，对于AC-3， 使用22050或11025Hz）。</li><li>有一个恒定的质量（但可变比特率），使用选项<code>-qscale N</code>时，<code>N</code>是1（优秀品质）和31（质量最差）之间。</li></ul><h2 id="7-范例"><a href="#7-范例" class="headerlink" title="7 范例"></a>7 范例</h2><h3 id="7-1-预置文件"><a href="#7-1-预置文件" class="headerlink" title="7.1 预置文件"></a>7.1 预置文件</h3><p>预置文件包含option=value，一个用于每行，指定其也可以指定的命令行上的选项的序列的序列。’＃’开头的字符行被忽略，并用来提供注释。空行将也被忽略。检查的例子FFmpeg的源代码树中的preset目录。</p><p>预置文件中指定的pre选项，这个选项需要一个预设名称作为输入。FFmpeg在<code>$AVCONV_DATADIR</code>和<code>$HOME/.ffmpeg</code>以及编译时指定的目录（通常是<code>$PREFIX/share/ffmpeg</code>）的目录中搜索文件名为<code>preset_name.avpreset</code> 的文件。例如，如果该参数是<code>libx264-max</code>，它会搜索文件<code>libx264-max.avpreset</code>。</p><h3 id="7-2-视频和音频抓取"><a href="#7-2-视频和音频抓取" class="headerlink" title="7.2 视频和音频抓取"></a>7.2 视频和音频抓取</h3><p>如果指定了输入格式和设备，ffmpeg将可以直接抓取视频和音频。 ffmpeg -f oss -i /dev/dsp -f video4linux2 -i /dev/video0 /tmp/out.mpg</p><p>或从ALSA音源（单声道输入，卡ID 1），而不是OSS： ffmpeg -f alsa -ac 1 -i hw:1 -f video4linux2 -i /dev/video0 /tmp/out.mpg</p><p>请注意，您必须在启动ffmpeg前激活正确的视频源和信道，比如的xawtv。你也必须正确设置混音器中的音频记录电平。</p><h3 id="7-3-X11抓取"><a href="#7-3-X11抓取" class="headerlink" title="7.3 X11抓取"></a>7.3 X11抓取</h3><p>使用ffmpeg抓取X11显示器 ffmpeg -f x11grab -video_size cif -framerate 25 -i :0.0 /tmp/out.mpg 0.0是display.screen数量的X11服务器，与DISPLAY环境变量一致。</p><pre><code>ffmpeg -f x11grab -video_size cif -framerate 25 -i :0.0+10,20 /tmp/out.mpg0.0是display.screen数量的X11服务器，与DISPLAY环境变量一致。10是在x偏移和20的y偏移。</code></pre><h3 id="7-4-视频和音频文件格式转换"><a href="#7-4-视频和音频文件格式转换" class="headerlink" title="7.4 视频和音频文件格式转换"></a>7.4 视频和音频文件格式转换</h3><p>任何支持的文件格式和协议可以作为ffmpeg的输入：</p><p>范例：</p><p>您可以使用YUV文件作为输入： <code>ffmpeg -i /tmp/test%d.Y /tmp/out.mpg</code></p><p>它将使用文件：</p><pre><code>/tmp/test0.Y, /tmp/test0.U, /tmp/test0.V,/tmp/test1.Y, /tmp/test1.U, /tmp/test1.V, etc...Y，/ tmp目录/ TEST0。U, /tmp/test0.V,/tmp/test1.Y, /tmp/test1.U, /tmp/test1.V, etc...</code></pre><p>Y文件使用的U和V文件的分辨率的两倍。他们是原始文件，没有头信息。他们可以通过所有视频解码器产生。您必须指定图像的大小与-s选项，如果ffmpeg的不能猜测它。</p><p>您可以从原始YUV420P文件输入： <code>ffmpeg -i /tmp/test.yuv /tmp/out.avi</code></p><p>test.yuv是含有生YUV平面数据的文件。每个帧是由Y平面后跟U和V平面的一半的垂直和水平分辨率。</p><p>可以输出到原始文件YUV420P： <code>ffmpeg -i mydivx.avi hugefile.yuv</code></p><p>您可以设置多个输入文件和输出文件： <code>ffmpeg -i /tmp/a.wav -s 640x480 -i /tmp/a.yuv /tmp/a.mpg</code></p><p>转换音频文件a.wav和原始YUV视频文件a.yuv到MPEG文件a.mpg。</p><p>你也可以同时做音频和视频转换： <code>ffmpeg -i /tmp/a.wav -ar 22050 /tmp/a.mp2</code></p><p>使用22050赫兹的采样率转换a.wav到MPEG音频。</p><p>可以同时进行多种格式的编码，并定义从输入流到输出数据流的映射： <code>ffmpeg -i /tmp/a.wav -map 0:a -b:a 64k /tmp/a.mp2 -map 0:a -b:a 128k /tmp/b.mp2</code></p><p>转换a.wav到64千比特的a.mp2和128千比特的b.mp2。<code>-map file:index</code>输出数据流的定义的顺序指定了用于每一个输出流的输入流。</p><p>您可以转码解密的VOB： <code>ffmpeg -i snatch_1.vob -f avi -c:v mpeg4 -b:v 800k -g 300 -bf 2 -c:a libmp3lame -b:a 128k snatch.avi</code></p><p>这是一个典型的DVD翻录的例子;输入是VOB文件，输出与MPEG-4视频和MP3音频的AVI文件。注意，在这个命令，我们使用B帧，以便对MPEG-4流与DivX5兼容，并且GOP大小为300，这意味着每10秒插入一帧到29.97fps的输入视频。此外，音频流是MP3编码，所以你需要启用通过传递LAME支持可使用–enable-libmp3lame配置。该映射是特别有用的用于DVD的转码，以获得所需的音频语言。</p><p>注意：使用<code>ffmpeg -formats</code>查看支持的输入格式。</p><p>您可以从视频中提取图像，或从图像创建视频：</p><p>从视频中提取图片： <code>ffmpeg -i foo.avi -r 1 -s WxH -f image2 foo-%03d.jpeg</code></p><p>这将每秒提取一个视频帧，并输出到名为foo-001.jpeg，foo-002.jpeg等图片中。图片将被重新缩放到定义的尺寸。</p><p>如果你想提取有限数量的帧，你可以组合使用上面的命令与-vframes或-t选项，或与-ss开始从某一个时间点提取。</p><p>从图像创建视频： <code>ffmpeg -f image2 -i foo-%03d.jpeg -r 12 -s WxH foo.avi</code></p><p>语法<code>foo-%03d.jpeg</code>指定要使用的三个数字组成的十进制数用零填充到表达的序列号。它支持C语言<code>printf</code>函数相同的语法，但只有格式接受整数。</p><p>当导入图像序列，<code>-i</code>还支持Shell扩展通配符。这在内部通过image2-specific<code>-pattern_type glob</code>选项选择。</p><p>例如，从文件名匹配<code>foo-*.jpeg</code>的图片创建视频 <code>ffmpeg -f image2 -pattern_type glob -i &#39;foo-*.jpeg&#39; -r 12 -s WxH foo.avi</code></p><p>可以把相同类型的许多流到输出文件： <code>ffmpeg -i test1.avi -i test2.avi -map 1:1 -map 1:0 -map 0:1 -map 0:0 -c copy -y test12.nut</code></p><p>产生的输出文件test12.nut将包含从以相反的顺序存储的输入文件中的四个流。</p><p>要强制CBR视频输出： <code>ffmpeg -i myfile.avi -b 4000k -minrate 4000k -maxrate 4000k -bufsize 1835k out.m2v</code></p><p><code>lmin</code>，<code>lmax</code>，<code>mblmin</code>和<code>mblmax</code>四个选项的单位是<code>lambda</code>，但你可以使用QP2LAMBDA常熟轻松地从<code>q</code>单位转换： <code>ffmpeg -i src.ext -lmax 21*QP2LAMBDA dst.ext</code></p>]]></content>
      
      
      
        <tags>
            
            <tag> ffmpeg </tag>
            
            <tag> 文档翻译 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>ten-billion-data</title>
      <link href="/2018/04/19/ten-billion-data/"/>
      <url>/2018/04/19/ten-billion-data/</url>
      
        <content type="html"><![CDATA[<p><a href="https://blog.csdn.net/zyq522376829/article/details/47686867" target="_blank" rel="noopener">https://blog.csdn.net/zyq522376829/article/details/47686867</a></p><a id="more"></a>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>SteadyFlow 2D video stablization</title>
      <link href="/2018/04/19/consistent-hashing/"/>
      <url>/2018/04/19/consistent-hashing/</url>
      
        <content type="html"><![CDATA[<p><a href="http://www.cs.sfu.ca/~pingtan/Papers/cvpr14_stabilization.pdf" target="_blank" rel="noopener">http://www.cs.sfu.ca/~pingtan/Papers/cvpr14_stabilization.pdf</a></p><a id="more"></a>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>SteadyFlow 2D video stablization</title>
      <link href="/2018/04/19/video_stablization/"/>
      <url>/2018/04/19/video_stablization/</url>
      
        <content type="html"><![CDATA[<iframe width="560" height="315" src="https://www.youtube.com/embed/O050BC7wsv0" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe><p><a href="http://www.cs.sfu.ca/~pingtan/Papers/cvpr14_stabilization.pdf" target="_blank" rel="noopener">http://www.cs.sfu.ca/~pingtan/Papers/cvpr14_stabilization.pdf</a></p><a id="more"></a>]]></content>
      
      
      
        <tags>
            
            <tag> video processing </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>sql-optimization</title>
      <link href="/2018/04/19/sql-optimization/"/>
      <url>/2018/04/19/sql-optimization/</url>
      
        <content type="html"><![CDATA[<p><strong>一、问题的提出</strong></p><p>在应用系统开发初期，由于开发数据库数据比较少，对于查询SQL语句，复杂视图的的编写等体会不出SQL语句各种写法的性能优劣，但是如果将应用系统提交实际应用后，随着数据库中数据的增加，系统的响应速度就成为目前系统需要解决的最主要的问题之一。系统优化中一个很重要的方面就是SQL语句的优化。对于海量数据，劣质SQL语句和优质SQL语句之间的速度差别可以达到上百倍，可见对于一个系统不是简单地能实现其功能就可，而是要写出高质量的SQL语句，提高系统的可用性。<br><a id="more"></a></p><p><strong>在多数情况下，Oracle使用索引来更快地遍历表，优化器主要根据定义的索引来提高性能</strong>。但是，如果在SQL语句的where子句中写的SQL代码不合理，就会<strong>造成优化器删去索引而使用全表扫描</strong>，一般就这种SQL语句就是所谓的劣质SQL语句。在编写SQL语句时我们应清楚优化器根据何种原则来删除索引，这有助于写出高性能的SQL语句。</p><p><strong>二、SQL语句编写注意问题</strong></p><p>下面就某些SQL语句的where子句编写中需要注意的问题作详细介绍。在这些where子句中，即使某些列存在索引，但是由于编写了劣质的SQL，系统在运行该SQL语句时也不能使用该索引，而同样使用全表扫描，这就造成了响应速度的极大降低。</p><p><strong>1. 操作符优化</strong></p><p><strong>(a) IN 操作符</strong></p><p>用IN写出来的SQL的优点是比较容易写及清晰易懂，这比较适合现代软件开发的风格。但是用IN的SQL性能总是比较低的，从Oracle执行的步骤来分析用IN的SQL与不用IN的SQL有以下区别：</p><p>ORACLE试图将其转换成多个表的连接，如果转换不成功则先执行IN里面的子查询，再查询外层的表记录，如果转换成功则直接采用多个表的连接方式查询。由此可见用IN的SQL至少多了一个转换的过程。一般的SQL都可以转换成功，但对于含有分组统计等方面的SQL就不能转换了。</p><p><strong>推荐方案：</strong>在业务密集的SQL当中尽量不采用IN操作符，用EXISTS 方案代替。</p><p><strong>(b) NOT IN操作符</strong></p><p>此操作是强列不推荐使用的，因为它不能应用表的索引。</p><p><strong>推荐方案：</strong>用NOT EXISTS 方案代替</p><p><strong>(c) IS NULL 或IS NOT NULL操作</strong>（判断字段是否为空）</p><p>判断字段是否为空一般是不会应用索引的，因为索引是不索引空值的。不能用null作索引，任何包含null值的列都将不会被包含在索引中。即使索引有多列这样的情况下，只要这些列中有一列含有null，该列就会从索引中排除。也就是说如果某列存在空值，即使对该列建索引也不会提高性能。任何在where子句中使用is null或is not null的语句优化器是不允许使用索引的。</p><p><strong>推荐方案</strong>：用其它相同功能的操作运算代替，如：a is not null 改为 a&gt;0 或a&gt;’’等。不允许字段为空，而用一个缺省值代替空值，如申请中状态字段不允许为空，缺省为申请。</p><p><strong>(d) &gt; 及 &lt; 操作符（大于或小于操作符）</strong></p><p>大于或小于操作符一般情况下是不用调整的，因为它有索引就会采用索引查找，但有的情况下可以对它进行优化，如一个表有100万记录，一个数值型字段A，30万记录的A=0，30万记录的A=1，39万记录的A=2，1万记录的A=3。那么执行A&gt;2与A&gt;=3的效果就有很大的区别了，因为A&gt;2时ORACLE会先找出为2的记录索引再进行比较，而A&gt;=3时ORACLE则直接找到=3的记录索引。</p><p><strong>(e) LIKE操作符</strong></p><p>LIKE操作符可以应用通配符查询，里面的通配符组合可能达到几乎是任意的查询，但是如果用得不好则会产生性能上的问题，如LIKE ‘%5400%’ 这种查询不会引用索引，而LIKE ‘X5400%’则会引用范围索引。</p><p>一个实际例子：用YW_YHJBQK表中营业编号后面的户标识号可来查询营业编号 YY_BH LIKE ‘%5400%’ 这个条件会产生全表扫描，如果改成YY_BH LIKE ’X5400%’ OR YY_BH LIKE ’B5400%’ 则会利用YY_BH的索引进行两个范围的查询，性能肯定大大提高。</p><p>带通配符(%)的like语句：</p><p>同样以上面的例子来看这种情况。目前的需求是这样的，要求在职工表中查询名字中包含cliton的人。可以采用如下的查询SQL语句:</p><p>select * from employee where last_name like ‘%cliton%’;</p><p>这里由于通配符(%)在搜寻词首出现，所以Oracle系统不使用last_name的索引。在很多情况下可能无法避免这种情况，但是一定要心中有底，通配符如此使用会降低查询速度。然而当通配符出现在字符串其他位置时，优化器就能利用索引。在下面的查询中索引得到了使用:</p><p>select * from employee where last_name like ‘c%’;</p><p><strong>(f) UNION操作符</strong></p><p>UNION在进行表链接后会筛选掉重复的记录，所以在表链接后会对所产生的结果集进行排序运算，删除重复的记录再返回结果。实际大部分应用中是不会产生重复的记录，最常见的是过程表与历史表UNION。如：<br>select <em> from gc_dfys<br>union<br>select </em> from ls_jg_dfys<br>这个SQL在运行时先取出两个表的结果，再用排序空间进行排序删除重复的记录，最后返回结果集，如果表数据量大的话可能会导致用磁盘进行排序。</p><p><strong>推荐方案：</strong>采用UNION ALL操作符替代UNION，因为UNION ALL操作只是简单的将两个结果合并后就返回。</p><p>select <em> from gc_dfys<br>union all<br>select </em> from ls_jg_dfys</p><p><strong>(g) 联接列</strong></p><p>对于有联接的列，即使最后的联接值为一个静态值，优化器是不会使用索引的。我们一起来看一个例子，假定有一个职工表(employee)，对于一个职工的姓和名分成两列存放(FIRST_NAME和LAST_NAME)，现在要查询一个叫比尔.克林顿(Bill Cliton)的职工。</p><p>下面是一个采用联接查询的SQL语句：</p><p>select * from employss where first_name||’’||last_name =’Beill Cliton’;</p><p>上面这条语句完全可以查询出是否有Bill Cliton这个员工，但是这里需要注意，系统优化器对基于last_name创建的索引没有使用。当采用下面这种SQL语句的编写，Oracle系统就可以采用基于last_name创建的索引。</p><p><em>*</em> where first_name =’Beill’ and last_name =’Cliton’;</p><p><strong>(h) Order by语句</strong></p><p>ORDER BY语句决定了Oracle如何将返回的查询结果排序。Order by语句对要排序的列没有什么特别的限制，也可以将函数加入列中(象联接或者附加等)。任何在Order by语句的非索引项或者有计算表达式都将降低查询速度。</p><p>仔细检查order by语句以找出非索引项或者表达式，它们会降低性能。解决这个问题的办法就是重写order by语句以使用索引，也可以为所使用的列建立另外一个索引，同时应绝对避免在order by子句中使用表达式。</p><p><strong>(i) NOT</strong></p><p>我们在查询时经常在where子句使用一些逻辑表达式，如大于、小于、等于以及不等于等等，也可以使用and(与)、or(或)以及not(非)。NOT可用来对任何逻辑运算符号取反。下面是一个NOT子句的例子:</p><p>… where not (status =’VALID’)</p><p>如果要使用NOT，则应在取反的短语前面加上括号，并在短语前面加上NOT运算符。NOT运算符包含在另外一个逻辑运算符中，这就是不等于(&lt;&gt;)运算符。换句话说，即使不在查询where子句中显式地加入NOT词，NOT仍在运算符中，见下例:</p><p>… where status &lt;&gt;’INVALID’;</p><p>对这个查询，可以改写为不使用NOT：</p><p>select * from employee where salary<3000 or salary>3000;</3000></p><p>虽然这两种查询的结果一样，但是第二种查询方案会比第一种查询方案更快些。第二种查询允许Oracle对salary列使用索引，而第一种查询则不能使用索引。</p><p><strong>2. SQL书写的影响</strong></p><p><strong>(a) 同一功能同一性能不同写法SQL的影响。</strong></p><p>如一个SQL在A程序员写的为  Select * from zl_yhjbqk</p><p>B程序员写的为 Select * from dlyx.zl_yhjbqk（带表所有者的前缀）</p><p>C程序员写的为 Select * from DLYX.ZLYHJBQK（大写表名）</p><p>D程序员写的为 Select *  from DLYX.ZLYHJBQK（中间多了空格）</p><p>以上四个SQL在ORACLE分析整理之后产生的结果及执行的时间是一样的，但是从ORACLE共享内存SGA的原理，可以得出ORACLE对每个SQL 都会对其进行一次分析，并且占用共享内存，如果将SQL的字符串及格式写得完全相同，则ORACLE只会分析一次，共享内存也只会留下一次的分析结果，这不仅可以减少分析SQL的时间，而且可以减少共享内存重复的信息，ORACLE也可以准确统计SQL的执行频率。</p><p><strong>(b) WHERE后面的条件顺序影响</strong></p><p>WHERE子句后面的条件顺序对大数据量表的查询会产生直接的影响。如：<br>Select <em> from zl_yhjbqk where dy_dj = ‘1KV以下’ and xh_bz=1<br>Select </em> from zl_yhjbqk where xh_bz=1 and dy_dj = ‘1KV以下’<br>以上两个SQL中dy_dj（电压等级）及xh_bz（销户标志）两个字段都没进行索引，所以执行的时候都是全表扫描，第一条SQL的dy_dj = ‘1KV以下’条件在记录集内比率为99%，而xh_bz=1的比率只为0.5%，在进行第一条SQL的时候99%条记录都进行dy_dj及xh_bz的比较，而在进行第二条SQL的时候0.5%条记录都进行dy_dj及xh_bz的比较，以此可以得出第二条SQL的CPU占用率明显比第一条低。</p><p><strong>(c) 查询表顺序的影响</strong></p><p>在FROM后面的表中的列表顺序会对SQL执行性能影响，在没有索引及ORACLE没有对表进行统计分析的情况下，ORACLE会按表出现的顺序进行链接，由此可见表的顺序不对时会产生十分耗服物器资源的数据交叉。（注：如果对表进行了统计分析，ORACLE会自动先进小表的链接，再进行大表的链接）</p><p><strong>3. SQL语句索引的利用</strong></p><p><strong>(a) 对条件字段的一些优化</strong></p><p><strong>采用函数处理的字段不能利用索引，</strong>如：</p><p>substr(hbs_bh,1,4)=’5400’，优化处理：hbs_bh like ‘5400%’</p><p>trunc(sk_rq)=trunc(sysdate)， 优化处理：sk_rq&gt;=trunc(sysdate) and sk_rq&lt;trunc(sysdate+1)</p><p>进行了显式或隐式的运算的字段不能进行索引，如：ss_df+20&gt;50，优化处理：ss_df&gt;30</p><p>‘X’ || hbs_bh&gt;’X5400021452’，优化处理：hbs_bh&gt;’5400021542’</p><p>sk_rq+5=sysdate，优化处理：sk_rq=sysdate-5</p><p>hbs_bh=5401002554，优化处理：hbs_bh=’ 5401002554’，注：此条件对hbs_bh 进行隐式的to_number转换，因为hbs_bh字段是字符型。</p><p><strong>条件内包括了多个本表的字段运算时不能进行索引</strong>，如：</p><p>ys_df&gt;cx_df，无法进行优化<br>qc_bh || kh_bh=’5400250000’，优化处理：qc_bh=’5400’ and kh_bh=’250000’</p><p><strong>4. 更多方面SQL优化资料分享</strong></p><p><strong>（1） 选择最有效率的表名顺序(只在基于规则的优化器中有效)：</strong></p><p>ORACLE 的解析器按照从右到左的顺序处理FROM子句中的表名，FROM子句中写在最后的表(基础表 driving table)将被最先处理，在FROM子句中包含多个表的情况下,你必须选择记录条数最少的表作为基础表。如果有3个以上的表连接查询, 那就需要选择交叉表(intersection table)作为基础表, 交叉表是指那个被其他表所引用的表.</p><p><strong>（2） WHERE子句中的连接顺序：</strong></p><p>ORACLE采用自下而上的顺序解析WHERE子句,根据这个原理,表之间的连接必须写在其他WHERE条件之前, 那些可以过滤掉最大数量记录的条件必须写在WHERE子句的末尾.</p><p><strong>（3） SELECT子句中避免使用 ‘ * ‘：</strong></p><p>ORACLE在解析的过程中, 会将’*’ 依次转换成所有的列名, 这个工作是通过查询数据字典完成的, 这意味着将耗费更多的时间。</p><p><strong>（4） 减少访问数据库的次数：</strong></p><p>ORACLE在内部执行了许多工作: 解析SQL语句, 估算索引的利用率, 绑定变量 , 读数据块等。</p><p>（5） 在SQL<em>Plus , SQL</em>Forms和Pro*C中重新设置ARRAYSIZE参数, 可以增加每次数据库访问的检索数据量 ,建议值为200。</p><p><strong>（6） 使用DECODE函数来减少处理时间：</strong></p><p>使用DECODE函数可以避免重复扫描相同记录或重复连接相同的表.</p><p><strong>（7） 整合简单,无关联的数据库访问：</strong></p><p>如果你有几个简单的数据库查询语句,你可以把它们整合到一个查询中(即使它们之间没有关系) 。</p><p><strong>（8） 删除重复记录：</strong></p><p>最高效的删除重复记录方法 ( 因为使用了ROWID)例子：<br>DELETE  FROM  EMP E  WHERE  E.ROWID &gt; (SELECT MIN(X.ROWID) FROM  EMP X  WHERE  X.EMP_NO = E.EMP_NO)。</p><p><strong>（9） 用TRUNCATE替代DELETE：</strong></p><p>当删除表中的记录时,在通常情况下, 回滚段(rollback segments ) 用来存放可以被恢复的信息. 如果你没有COMMIT事务,ORACLE会将数据恢复到删除之前的状态(准确地说是恢复到执行删除命令之前的状况) 而当运用TRUNCATE时, 回滚段不再存放任何可被恢复的信息.当命令运行后,数据不能被恢复.因此很少的资源被调用,执行时间也会很短. (译者按: TRUNCATE只在删除全表适用,TRUNCATE是DDL不是DML) 。</p><p><strong>（10） 尽量多使用COMMIT：</strong></p><p>只要有可能,在程序中尽量多使用COMMIT, 这样程序的性能得到提高,需求也会因为COMMIT所释放的资源而减少，COMMIT所释放的资源:<br>a. 回滚段上用于恢复数据的信息.<br>b. 被程序语句获得的锁<br>c. redo log buffer 中的空间<br>d. ORACLE为管理上述3种资源中的内部花费</p><p><strong>（11） 用Where子句替换HAVING子句：</strong></p><p>避免使用HAVING子句, HAVING 只会在检索出所有记录之后才对结果集进行过滤. 这个处理需要排序,总计等操作. 如果能通过WHERE子句限制记录的数目,那就能减少这方面的开销. (非oracle中)on、where、having这三个都可以加条件的子句中，on是最先执行，where次之，having最后，因为on是先把不符合条件的记录过滤后才进行统计，它就可以减少中间运算要处理的数据，按理说应该速度是最快的，where也应该比having快点的，因为它过滤数据后才进行sum，在两个表联接时才用on的，所以在一个表的时候，就剩下where跟having比较了。在这单表查询统计的情况下，如果要过滤的条件没有涉及到要计算字段，那它们的结果是一样的，只是where可以使用rushmore技术，而having就不能，在速度上后者要慢如果要涉及到计算的字 段，就表示在没计算之前，这个字段的值是不确定的，根据上篇写的工作流程，where的作用时间是在计算之前就完成的，而having就是在计算后才起作 用的，所以在这种情况下，两者的结果会不同。在多表联接查询时，on比where更早起作用。系统首先根据各个表之间的联接条件，把多个表合成一个临时表 后，再由where进行过滤，然后再计算，计算完后再由having进行过滤。由此可见，要想过滤条件起到正确的作用，首先要明白这个条件应该在什么时候起作用，然后再决定放在那里。</p><p><strong>（12） 减少对表的查询：</strong></p><p>在含有子查询的SQL语句中,要特别注意减少对表的查询.例子：<br>SELECT  TAB_NAME FROM TABLES WHERE (TAB_NAME,DB_VER) = ( SELECT TAB_NAME,DB_VER FROM  TAB_COLUMNS  WHERE  VERSION = 604)</p><p><strong>（13） 通过内部函数提高SQL效率：</strong></p><p>复杂的SQL往往牺牲了执行效率. 能够掌握上面的运用函数解决问题的方法在实际工作中是非常有意义的。</p><p><strong>（14） 使用表的别名(Alias)：</strong></p><p>当在SQL语句中连接多个表时, 请使用表的别名并把别名前缀于每个Column上.这样一来,就可以减少解析的时间并减少那些由Column歧义引起的语法错误。</p><p><strong>（15） 用EXISTS替代IN、用NOT EXISTS替代NOT IN：</strong></p><p>在许多基于基础表的查询中,为了满足一个条件,往往需要对另一个表进行联接.在这种情况下, 使用EXISTS(或NOT EXISTS)通常将提高查询的效率. 在子查询中,NOT IN子句将执行一个内部的排序和合并. 无论在哪种情况下,NOT IN都是最低效的 (因为它对子查询中的表执行了一个全表遍历). 为了避免使用NOT IN ,我们可以把它改写成外连接(Outer Joins)或NOT EXISTS。<br>例子：<br>（高效）SELECT <em> FROM  EMP (基础表)  WHERE  EMPNO &gt; 0  AND  EXISTS (SELECT ‘X’  FROM DEPT  WHERE  DEPT.DEPTNO = EMP.DEPTNO  AND  LOC = ‘MELB’)<br>(低效)SELECT  </em> FROM  EMP (基础表)  WHERE  EMPNO &gt; 0  AND  DEPTNO IN(SELECT DEPTNO  FROM  DEPT  WHERE  LOC = ‘MELB’)</p><p><strong>（16） 识别’低效执行’的SQL语句：</strong></p><p>虽然目前各种关于SQL优化的图形化工具层出不穷,但是写出自己的SQL工具来解决问题始终是一个最好的方法：<br>SELECT  EXECUTIONS , DISK_READS, BUFFER_GETS,<br>ROUND((BUFFER_GETS-DISK_READS)/BUFFER_GETS,2) Hit_radio,<br>ROUND(DISK_READS/EXECUTIONS,2) Reads_per_run,<br>SQL_TEXT<br>FROM  V$SQLAREA<br>WHERE  EXECUTIONS&gt;0<br>AND  BUFFER_GETS &gt; 0<br>AND  (BUFFER_GETS-DISK_READS)/BUFFER_GETS &lt; 0.8<br>ORDER BY  4 DESC;</p><p><strong>（17） 用索引提高效率：</strong></p><p>索引是表的一个概念部分,用来提高检索数据的效率，ORACLE使用了一个复杂的自平衡B-tree结构. 通常,通过索引查询数据比全表扫描要快. 当ORACLE找出执行查询和Update语句的最佳路径时, ORACLE优化器将使用索引. 同样在联结多个表时使用索引也可以提高效率. 另一个使用索引的好处是,它提供了主键(primary key)的唯一性验证.。那些LONG或LONG RAW数据类型, 你可以索引几乎所有的列. 通常, 在大型表中使用索引特别有效. 当然,你也会发现, 在扫描小表时,使用索引同样能提高效率. 虽然使用索引能得到查询效率的提高,但是我们也必须注意到它的代价. 索引需要空间来存储,也需要定期维护, 每当有记录在表中增减或索引列被修改时, 索引本身也会被修改. 这意味着每条记录的INSERT , DELETE , UPDATE将为此多付出4 , 5 次的磁盘I/O . 因为索引需要额外的存储空间和处理,那些不必要的索引反而会使查询反应时间变慢.。定期的重构索引是有必要的：<br>ALTER  INDEX <indexname> REBUILD <tablespacename></tablespacename></indexname></p><p><strong>（18） 用EXISTS替换DISTINCT：</strong></p><p>当提交一个包含一对多表信息(比如部门表和雇员表)的查询时,避免在SELECT子句中使用DISTINCT. 一般可以考虑用EXIST替换, EXISTS 使查询更为迅速,因为RDBMS核心模块将在子查询的条件一旦满足后,立刻返回结果. 例子：<br>(低效):<br>SELECT  DISTINCT  DEPT_NO,DEPT_NAME  FROM  DEPT D , EMP E WHERE  D.DEPT_NO = E.DEPT_NO<br>(高效):<br>SELECT  DEPT_NO,DEPT_NAME  FROM  DEPT D  WHERE  EXISTS ( SELECT ‘X’  FROM  EMP E  WHERE E.DEPT_NO = D.DEPT_NO);</p><p>（19） sql语句用大写的；因为oracle总是先解析sql语句，把小写的字母转换成大写的再执行。</p><p>（20） 在java代码中尽量少用连接符“＋”连接字符串！</p><p>（21） 避免在索引列上使用NOT，通常我们要避免在索引列上使用NOT, NOT会产生在和在索引列上使用函数相同的影响. 当ORACLE”遇到”NOT,他就会停止使用索引转而执行全表扫描。</p><p><strong>（22） 避免在索引列上使用计算</strong> WHERE子句中，如果索引列是函数的一部分．优化器将不使用索引而使用全表扫描．举例:<br>低效：<br>SELECT … FROM  DEPT  WHERE SAL * 12 &gt; 25000;<br>高效:<br>SELECT … FROM DEPT WHERE SAL &gt; 25000/12;</p><p><strong>（23） 用&gt;=替代&gt;</strong><br>高效:<br>SELECT <em> FROM  EMP  WHERE  DEPTNO &gt;=4<br>低效:<br>SELECT </em> FROM EMP WHERE DEPTNO &gt;3<br>两者的区别在于, 前者DBMS将直接跳到第一个DEPT等于4的记录而后者将首先定位到DEPTNO=3的记录并且向前扫描到第一个DEPT大于3的记录。</p><p><strong>（24） 用UNION替换OR (适用于索引列)</strong></p><p>通常情况下, 用UNION替换WHERE子句中的OR将会起到较好的效果. 对索引列使用OR将造成全表扫描. 注意, 以上规则只针对多个索引列有效. 如果有column没有被索引, 查询效率可能会因为你没有选择OR而降低. 在下面的例子中, LOC_ID 和REGION上都建有索引.<br>高效:<br>SELECT LOC_ID , LOC_DESC , REGION<br>FROM LOCATION<br>WHERE LOC_ID = 10<br>UNION<br>SELECT LOC_ID , LOC_DESC , REGION<br>FROM LOCATION<br>WHERE REGION = “MELBOURNE”<br>低效:<br>SELECT LOC_ID , LOC_DESC , REGION<br>FROM LOCATION<br>WHERE LOC_ID = 10 OR REGION = “MELBOURNE”<br>如果你坚持要用OR, 那就需要返回记录最少的索引列写在最前面.</p><p><strong>（25） 用IN来替换OR</strong></p><p>这是一条简单易记的规则，但是实际的执行效果还须检验，在ORACLE8i下，两者的执行路径似乎是相同的．<br>低效:<br>SELECT…. FROM LOCATION WHERE LOC_ID = 10 OR LOC_ID = 20 OR LOC_ID = 30<br>高效<br>SELECT… FROM LOCATION WHERE LOC_IN  IN (10,20,30);</p><p><strong>（26） 避免在索引列上使用IS NULL和IS NOT NULL</strong></p><p>避免在索引中使用任何可以为空的列，ORACLE将无法使用该索引．对于单列索引，如果列包含空值，索引中将不存在此记录. 对于复合索引，如果每个列都为空，索引中同样不存在此记录. 如果至少有一个列不为空，则记录存在于索引中．举例: 如果唯一性索引建立在表的A列和B列上, 并且表中存在一条记录的A,B值为(123,null) , ORACLE将不接受下一条具有相同A,B值（123,null）的记录(插入). 然而如果所有的索引列都为空，ORACLE将认为整个键值为空而空不等于空. 因此你可以插入1000 条具有相同键值的记录,当然它们都是空! 因为空值不存在于索引列中,所以WHERE子句中对索引列进行空值比较将使ORACLE停用该索引.<br>低效: (索引失效)<br>SELECT … FROM  DEPARTMENT  WHERE  DEPT_CODE IS NOT NULL;<br>高效: (索引有效)<br>SELECT … FROM  DEPARTMENT  WHERE  DEPT_CODE &gt;=0;</p><p><strong>（27） 总是使用索引的第一个列：</strong></p><p>如果索引是建立在多个列上, 只有在它的第一个列(leading column)被where子句引用时,优化器才会选择使用该索引. 这也是一条简单而重要的规则，当仅引用索引的第二个列时,优化器使用了全表扫描而忽略了索引。</p><p><strong>（28） 用UNION-ALL 替换UNION ( 如果有可能的话)：</strong></p><p>当SQL 语句需要UNION两个查询结果集合时,这两个结果集合会以UNION-ALL的方式被合并, 然后在输出最终结果前进行排序. 如果用UNION ALL替代UNION, 这样排序就不是必要了. 效率就会因此得到提高. 需要注意的是，UNION ALL 将重复输出两个结果集合中相同记录. 因此各位还是要从业务需求分析使用UNION ALL的可行性. UNION 将对结果集合排序,这个操作会使用到SORT_AREA_SIZE这块内存. 对于这块内存的优化也是相当重要的. 下面的SQL可以用来查询排序的消耗量<br>低效：<br>SELECT  ACCT_NUM, BALANCE_AMT<br>FROM  DEBIT_TRANSACTIONS<br>WHERE TRAN_DATE = ‘31-DEC-95’<br>UNION<br>SELECT ACCT_NUM, BALANCE_AMT<br>FROM DEBIT_TRANSACTIONS<br>WHERE TRAN_DATE = ‘31-DEC-95’<br>高效:<br>SELECT ACCT_NUM, BALANCE_AMT<br>FROM DEBIT_TRANSACTIONS<br>WHERE TRAN_DATE = ‘31-DEC-95’<br>UNION ALL<br>SELECT ACCT_NUM, BALANCE_AMT<br>FROM DEBIT_TRANSACTIONS<br>WHERE TRAN_DATE = ‘31-DEC-95’</p><p><strong>（29） 用WHERE替代ORDER BY：</strong></p><p>ORDER BY 子句只在两种严格的条件下使用索引.<br>ORDER BY中所有的列必须包含在相同的索引中并保持在索引中的排列顺序.<br>ORDER BY中所有的列必须定义为非空.<br>WHERE子句使用的索引和ORDER BY子句中所使用的索引不能并列.<br>例如:<br>表DEPT包含以下列:<br>DEPT_CODE PK NOT NULL<br>DEPT_DESC NOT NULL<br>DEPT_TYPE NULL<br>低效: (索引不被使用)<br>SELECT DEPT_CODE FROM  DEPT  ORDER BY  DEPT_TYPE<br>高效: (使用索引)<br>SELECT DEPT_CODE  FROM  DEPT  WHERE  DEPT_TYPE &gt; 0</p><p><strong>（30） 避免改变索引列的类型:</strong></p><p>当比较不同数据类型的数据时, ORACLE自动对列进行简单的类型转换.<br>假设 EMPNO是一个数值类型的索引列.<br>SELECT …  FROM EMP  WHERE  EMPNO = ‘123’<br>实际上,经过ORACLE类型转换, 语句转化为:<br>SELECT …  FROM EMP  WHERE  EMPNO = TO_NUMBER(‘123’)<br>幸运的是,类型转换没有发生在索引列上,索引的用途没有被改变.<br>现在,假设EMP_TYPE是一个字符类型的索引列.<br>SELECT …  FROM EMP  WHERE EMP_TYPE = 123<br>这个语句被ORACLE转换为:<br>SELECT …  FROM EMP  WHERE TO_NUMBER(EMP_TYPE)=123<br>因为内部发生的类型转换, 这个索引将不会被用到! 为了避免ORACLE对你的SQL进行隐式的类型转换, 最好把类型转换用显式表现出来. 注意当字符和数值比较时, ORACLE会优先转换数值类型到字符类型。</p><p>分析select   emp_name   form   employee   where   salary   &gt;   3000   在此语句中若salary是Float类型的，则优化器对其进行优化为Convert(float,3000)，因为3000是个整数，我们应在编程时使用3000.0而不要等运行时让DBMS进行转化。同样字符和整型数据的转换。</p><p><strong>（31） 需要当心的WHERE子句:</strong></p><p>某些SELECT 语句中的WHERE子句不使用索引. 这里有一些例子.<br>在下面的例子里, (1)‘!=’ 将不使用索引. 记住, 索引只能告诉你什么存在于表中, 而不能告诉你什么不存在于表中. (2) ‘ ¦ ¦’是字符连接函数. 就象其他函数那样, 停用了索引. (3) ‘+’是数学函数. 就象其他数学函数那样, 停用了索引. (4)相同的索引列不能互相比较,这将会启用全表扫描.</p><p>（32） a. 如果检索数据量超过30%的表中记录数.使用索引将没有显著的效率提高. b. 在特定情况下, 使用索引也许会比全表扫描慢, 但这是同一个数量级上的区别. 而通常情况下,使用索引比全表扫描要块几倍乃至几千倍!</p><p><strong>（33） 避免使用耗费资源的操作：</strong></p><p>带有DISTINCT,UNION,MINUS,INTERSECT,ORDER BY的SQL语句会启动SQL引擎执行耗费资源的排序(SORT)功能. DISTINCT需要一次排序操作, 而其他的至少需要执行两次排序. 通常, 带有UNION, MINUS , INTERSECT的SQL语句都可以用其他方式重写. 如果你的数据库的SORT_AREA_SIZE调配得好, 使用UNION , MINUS, INTERSECT也是可以考虑的, 毕竟它们的可读性很强。</p><p><strong>（34） 优化GROUP BY：</strong></p><p>提高GROUP BY 语句的效率, 可以通过将不需要的记录在GROUP BY 之前过滤掉.下面两个查询返回相同结果但第二个明显就快了许多.<br>低效:<br>SELECT JOB , AVG(SAL)<br>FROM EMP<br>GROUP by JOB<br>HAVING JOB = ‘PRESIDENT’<br>OR JOB = ‘MANAGER’<br>高效:<br>SELECT JOB , AVG(SAL)<br>FROM EMP<br>WHERE JOB = ‘PRESIDENT’<br>OR JOB = ‘MANAGER’<br>GROUP by JOB</p>]]></content>
      
      
      
        <tags>
            
            <tag> database </tag>
            
            <tag> sql </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>java后端，我面了五轮阿里巴巴，然后凉了</title>
      <link href="/2018/04/19/gulugulu/"/>
      <url>/2018/04/19/gulugulu/</url>
      
        <content type="html"><![CDATA[<h2 id="如题"><a href="#如题" class="headerlink" title="如题"></a>如题</h2><a id="more"></a><h4 id="一面"><a href="#一面" class="headerlink" title="一面"></a>一面</h4><ol><li><p>自我介绍</p></li><li><p>Java基础：</p><p>深浅拷贝</p><p>String StringBuilder StringBuffer区分，线程安全</p><p>等号和equal to的区别，重写equalto需不需要重写hashcode</p><p>Hashcode存在的意义</p><p>java集合框架：</p><p>HashMap底层实现</p><p>多线程：</p><p>如何创建一个线程</p></li><li><p>Java虚拟机：</p><p>底层数据结构，</p><p>full gc</p><blockquote><p>Minor GC ，Full GC 触发条件</p><p>Minor GC触发条件：当Eden区满时，触发Minor GC。</p><p>Full GC触发条件：</p><p>（1）调用System.gc时，系统建议执行Full GC，但是不必然执行</p><p>（2）老年代空间不足</p><p>（3）方法去空间不足</p><p>（4）通过Minor GC后进入老年代的平均大小大于老年代的可用内存</p><p>（5）由Eden区、From Space区向To Space区复制时，对象大小大于To Space可用内存，则把该对象转存到老年代，且老年代的可用内存小于该对象大小</p></blockquote><p>内存溢出，堆溢出栈溢出</p><p>堆区放object</p></li><li><p>数据结构：</p><p>数组和链表区别</p><p>中缀转后缀</p></li><li><p>算法：</p><p>随便讲个算法，快排，时间复杂度，最坏情况的时间复杂度</p><p>简要介绍动态规划法</p></li><li><p>计算机网络：</p><p>TCP和UDP区别</p><p>Tcp三次握手</p></li><li><p>有个人博客吗？</p></li><li><p>Github有没有过一些有趣的项目</p></li><li><p>框架有没有看过源码，哪些</p></li></ol><h4 id="二面"><a href="#二面" class="headerlink" title="二面"></a>二面</h4><ol><li>自我介绍</li><li>聊项目</li><li>java虚拟机原理，内存分配和回收机制</li><li>java虚拟机垃圾清理</li><li>学的课程，课都干嘛了，做的项目，人脸识别用的什么算法</li><li>未来规划</li></ol><h4 id="三面"><a href="#三面" class="headerlink" title="三面"></a>三面</h4><ol><li>自我介绍</li><li>手写代码：//评测题目: 1.给定100w行文本，找出重复数最多的那一行的内容，请编码实现</li><li>java基础：<ol><li>HashMap底层实现描述，</li><li>HashMap的    put原理，</li><li>高低位异或、取余算法计算HashMap</li><li>线程不安全的原因infinite Loop</li><li>Hashcode存在的意义</li><li>==和equals</li><li>多线程实现Runnable接口和继承Thread类的区别</li><li>Dubbo听说过吗。。这是自卖自夸啊我说我是梁飞脑残粉</li></ol></li><li>操作系统：<ol><li>Java 线程的状态及切换</li><li>CPU调度算法：先来先服务、最长时间未使用、短作业优先</li></ol></li><li>数据库：<ol><li>锁的实现，悲观锁，行级锁锁表。乐观锁的原理，讲一个用乐观锁的例子我说抢票的时候余票的统计。</li></ol></li><li>设计模式：实战中用过哪些设计模式？我说我没用过，我懂原理。。<ol><li>单例模式，懒汉饿汉双重锁单例。</li></ol></li><li>有没有什么问题</li></ol><h4 id="四面"><a href="#四面" class="headerlink" title="四面"></a>四面</h4><p>项目经历，主要考虑scalability。</p><p>未来规划</p><p>GitHub和博客</p><h4 id="五面"><a href="#五面" class="headerlink" title="五面"></a>五面</h4><ol><li>项目经历</li><li>websocket的底层实现？协议。</li><li>websocket通信中出现阻塞，断掉，怎么处理。</li><li>websocket的全双工通信是如何实现的。</li><li>websocket和socket的区别</li><li>websocket如何保持长连接HTTP？我说ajax轮询。</li><li>websocket和http的区别</li><li>websocket群发是如何实现的</li><li>全双工单工半双工的区别</li><li>数据库表的大量数据查询如何优化（和项目经历有关）</li><li>消息推送，消息表里有大量数据的时候怎么办，不仅仅是分表，表的设计上的优化（和项目经历有关）</li><li>Redis作为数据库前件的时候数据一致性是如何保证的，需要做哪些工作。</li><li>Hibernate sessionFactory如何控制事务，流程是怎么样的。</li><li>Hibernate二级缓存如何配置，实现的原理是什么。</li><li>Hibernate连接池的管理</li><li>mysql是两列联合索引的时候构造的B+树是什么样子的，为什么单独查第二列命中不了索引？</li><li>spring源码：IOC什么时候初始化beanFactory，初始完做哪些工作，实例化bean的步骤是什么。</li><li>bean的类型，单例/prototype。</li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> alibaba </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>12306</title>
      <link href="/2018/04/16/12306/"/>
      <url>/2018/04/16/12306/</url>
      
        <content type="html"><![CDATA[<h2 id="PostgreSQL-与-12306-抢火车票的思考-转载"><a href="#PostgreSQL-与-12306-抢火车票的思考-转载" class="headerlink" title="PostgreSQL 与 12306 抢火车票的思考 (转载)"></a>PostgreSQL 与 12306 抢火车票的思考 (转载)</h2><p>转自：<a href="https://github.com/digoal/blog/blob/master/201611/20161124_02.md" target="_blank" rel="noopener">https://github.com/digoal/blog/blob/master/201611/20161124_02.md</a><br><a id="more"></a></p><h2 id="背景"><a href="#背景" class="headerlink" title="背景"></a>背景</h2><p>马上春节了，又到了火车票的销售旺季，一票难求的问题依旧存在吗？    </p><p>还记得10年前春节前买火车票得在放票前1天搬个小板凳去排队，对于热门路线，排一个晚上都有可能买不到票。    </p><p>随着互联网的发展，几年前建设了12306网上购票系统，可以从电脑上买票，但是不要以为在电脑上就能买到票。    </p><p>我记得12306刚推出时，经常发生12306网站打不开，或者无法付款的问题。   </p><p>为什么呢？   </p><p>原因很简单，春节期间网上购票的人可能达到几亿的级别，而且放票日期是同一天同一个时间点，也就是说同一时刻12306要接受几亿用户的访问。   </p><p>处理能力和实际的访问需求更不上，带来的结果就是网站打不开，系统不稳定的现象。   </p><p><img data-src="20161124_02_pic_004.png" alt="pic">    </p><p>后来12306想了分线路分时段开启的办法，想办法把不同线路的用户错开时间来访问12306的网站，但是这个方法起初的效果不明显，并不是所有用户都知道的（就好像你临时通知今天不上班，但还是有用户会来单位的），所以大多数用户还是集中在一个点去访问12306的网站。    </p><p>随着硬件的发展，技术的演进，12306的系统越来越趋于成熟，稳定性和响应速度也越来越好。   </p><p>据说现在很多商家还开通了云抢票业务，本质上是让你不要冲击12306系统了，把需求提前收集，在放票时，这些系统会进行排队与合并购买，这种手段可以减少12306的访问并发。        </p><p>抢火车票是很有意思的一个课题，对IT人的智商以及IT系统的健壮性，尤其是数据库的功能和性能都是一种挑战。      </p><p>接下来我们一起来缕一缕有哪些难点，又有怎样的解决手段。     </p><h2 id="一、扒一扒熟悉的铁路售票系统"><a href="#一、扒一扒熟悉的铁路售票系统" class="headerlink" title="一、扒一扒熟悉的铁路售票系统"></a>一、扒一扒熟悉的铁路售票系统</h2><p>铁路售票系统最基本的功能包括    </p><pre><code>查询余票、余票统计、购票、车次变化、退票、改签、中转乘车规划 等。</code></pre><p>每个需求都有各自的特点，例如    </p><p>1. 查询余票，用户在购票前通常会查一下到达目的地有哪些余票，它属于一个高并发的操作，同时需要统计余票张数，需要很强的CPU来支撑实时的查询。    </p><p>2. 购票，购票和查询不一样，购票是会改变库存的，所以对数据库来说是更新的操作。   </p><p>而且购票很可能发生冲突，例如很多人要买同一趟车的票，那就出现冲突了，到底卖给谁呢？    </p><p>需要考虑锁冲突，尽量的让不同的人购买时可并行，或者可以合并多人的购票请求，来减少数据库的更新操作。     </p><p>3. 中转乘车，当用户需要购买的起点和到达站无票时，需要计算中转的搭乘方案。   </p><p>比如从北京到上海，如果没有直达车，是不是该转车呢？转哪趟，在哪里转就成了问题，简单一点就是买票的人自己想。   </p><p>高级一点的话，可以让12306给你推荐路线，这个涉及的是数据库的路径规划功能。    </p><p>我们来逐一分析一下这些需求的特点。       </p><h3 id="1-查询余票"><a href="#1-查询余票" class="headerlink" title="1 查询余票"></a>1 查询余票</h3><p>1. 普通的余票查询需求  </p><p>你如果要买从北京到上海的火车票，通常会查一下哪些车次还有余票。    </p><p>查询的过滤条件可能很多，比如    </p><p>1.1. 上车站、下车站、中转站  </p><p>1.2. 车次类型（高铁、动车、直达、快速、普客、…）  </p><p>1.3. 出发日期、时段  </p><p>1.4. 到达日期、时段  </p><p>1.5. 席别（硬座、硬卧、…站票）  </p><p>1.6. 过滤掉没有余票的车次  </p><p>展示给用时还要考虑到怎么排序（是按始发时间排呢，还是按票价，或者按余票数量排？），怎么分页。    </p><p><img data-src="20161124_02_pic_001.png" alt="pic">      </p><p><strong>眼见不一定为实</strong>    </p><p>查询余票通常不是实时的、或者说不一定是准确的，有可能是后台异步统计的结果。    </p><p>即使是实时统计的结果，在高并发的抢票期间，你看到的信息对你来说也许很快就会失效。    </p><p>比如你看到某趟车还有100张票，很可能等你付款的时候，已经卖光了。    </p><p>所以在高峰期，余票信息的参考价值并不大，不要被迷惑了。   </p><p>2. 查询余票的另一个更高级的需求是路径规划, 自动适配(根据用户输入的中转站点s)   </p><p>这个功能以前可能没有，但是总有一天会暴露出来，特别是车票很紧张的情况下。    </p><p>就比如从北京到上海，直达的没有了，系统可以帮你看看转一趟车的，转2趟车的，转N趟车的。（当然，转的越多越复杂）。    </p><p>从中转这个角度来讲，实际上已经扯上路径规划的技术了。    </p><p>怎么中转是时间最短的、价格最低的、中转次数最少的等等。（里面还涉及转车的输入要求（比如用户要求在一线城市转车，或者必须要转高铁））。       </p><p>关于路径规划，可以参考一下PostgreSQL pgrouting，已支持多种路径规划算法，支持算法的自定义扩展。  </p><p>简直是居家旅行，杀人灭口的必备良药。  </p><p><a href="../201607/20160710_01.md">《聊一聊双十一背后的技术 - 物流, 动态路径规划》</a>      </p><h4 id="设计痛点"><a href="#设计痛点" class="headerlink" title="设计痛点"></a>设计痛点</h4><p>1. 大多数用户是有选择综合症的，通常来说，用户可能会查询很多次，才选到合适日期的合适车次的票。   </p><p>查询量比较大，春节期间更甚。    </p><p>2. 为了展示余票数量，需要统计，会耗费较多的CPU, IO资源。    </p><p>3. 路径规划，帮用户选择最佳的转车路线，很考验数据库的功能，大多数数据库没有这个功能。  </p><h3 id="2-余票统计"><a href="#2-余票统计" class="headerlink" title="2 余票统计"></a>2 余票统计</h3><p>对于售票系统来说，查询余票实际上是一个统计操作。    </p><p>统计操作相比简单查询，不但消耗更多的IO还消耗更多的CPU资源。    </p><p>想像一下几亿人（其实不用这么多，可能几十万就够了）来查询余票，即使机器没挂掉，也会把所有机器的资源跑满，CPU产生的热量，可能几分钟就能把鸡蛋煮熟咯。     </p><p>为了减少实时查询余票的开销，通常会分时进行统计，更新最新的统计信息。    </p><p>用户查询余票信息时，查到的是统计后的结果，前面我已经分析过了，余票是不可信的，所以存在一定的延迟其实也是允许的。    </p><p>这下不能煮鸡蛋了，因为把几亿个统计请求，变成了1个统计请求，是不是一下子世界就冷静了呢？     </p><p>我们可以看到12306主页的余票大盘数据    </p><p><img data-src="20161124_02_pic_002.png" alt="pic">      </p><h4 id="设计痛点-1"><a href="#设计痛点-1" class="headerlink" title="设计痛点"></a>设计痛点</h4><p>1. 余票信息需要统计，查询会耗费较多的CPU,IO。  </p><p>由于余票是不可信的，所以存在一定的延迟其实也是允许的，优化手段是异步统计，用户查询统计后的结果。   </p><h3 id="3-购票"><a href="#3-购票" class="headerlink" title="3 购票"></a>3 购票</h3><p>购票相对于查询余票来说，从请求量来分析，比查询请求更少，因为通常来说，用户可能会查询很多次，才选到合适日期的合适车次的票。     </p><p>但是由于购票是一次交易，每次交易都会产生写操作，而且这种交易并不是无限库存的交易，因为库存是有限的，所以设计的关键是降低粒度，减少锁冲突，减少数据扫描量。    </p><p>另外还需要考虑的因素包括      </p><p>1. 同一趟车次的同一个座位，在不同的维度可能会被多次售卖    </p><p>1.1 时间维度，如发车日期    </p><p>1.2 空间维度，不同的起始站点  </p><p>2. 票价  </p><p>票价一般和席别绑定，按区间计费。    </p><p>另一个需求是尽量的将票卖出去，减少空洞座位。    </p><p>打个比方，从北京到上海的车，中间经过（天津、徐州、南京、无锡、苏州），如果天津到南京段有人买了，剩下的没有被购买的段应该还可以继续被购买。    </p><p>如果一趟从北京到上海的车，所有的票都被苏州到上海的用户买了，其他的位置没有卖出，铁大哥是不是要哭晕在厕所。   </p><p>又或者某趟车大量的座位被中途上车的用户买了，是不是可以买到全程的票数就少了。   </p><p>以前就存在这种情况，对铁大哥的成本是个不小的考验。    </p><h4 id="设计痛点-2"><a href="#设计痛点-2" class="headerlink" title="设计痛点"></a>设计痛点</h4><p>1. 为了减少购票系统的写锁冲突，例如同一个座位，尽量不出现因为一个会话在更新它，其他会话需要等待的情况。    </p><p>（比如A用户买了北京到天津的，B用户买了天津到上海的同一趟车的同一个座位，那么应该设计合理的合并操作（如数据库内核改进）或者从设计上避免锁等待）  </p><p>其实就是把座位的空间维度（从哪里到哪里）、本身的属性（座位号）、时间维度（发车日期）进行解耦，放到多条记录中，从而在购买时，可以同时进行。   </p><p>因为数据库中最小的锁目前是行锁（单行记录同一时刻只允许一个会话进行更新，其他的被堵塞，等待释放锁），也许随着技术的发展，会演变成列锁，或者列里面的元素锁（比如数组，JSON）。    </p><h3 id="4-车次新增、删除、变更"><a href="#4-车次新增、删除、变更" class="headerlink" title="4 车次新增、删除、变更"></a>4 车次新增、删除、变更</h3><p>春节来临时、通常需要对某些热门线路增加车次。    </p><p>及车次的新增、删除和变更需求。    </p><p>在设计数据库时，应该考虑到这一点。  </p><h4 id="设计痛点-3"><a href="#设计痛点-3" class="headerlink" title="设计痛点"></a>设计痛点</h4><p>车次的变更简直是牵一发而动全身，比如余票统计会跟着变化，查询系统也要跟着变化。  </p><p>还有初始化信息的准备，例如为了加快购票的速度，可能会将车次的数据提前准备好（也许是每个座位一条记录），参考第3个需求的解说。    </p><h3 id="5-对账需求"><a href="#5-对账需求" class="headerlink" title="5 对账需求"></a>5 对账需求</h3><p>票可能是经过很多渠道卖出去的，例如支付宝、去哪儿、携程、铁老大的售票窗口、银行的代理窗口、客运机构 等等。   </p><p>涉及到实际的销售信息与资金往来的对账需求。    </p><p>通常这个操作是隔天延迟对账的。    </p><h3 id="6-退票、改签需求"><a href="#6-退票、改签需求" class="headerlink" title="6 退票、改签需求"></a>6 退票、改签需求</h3><p>退票和改签也是比较常见的需求，特别是现在APP流行起来，退改签都很方便。  </p><p>这就导致了用户可能会先买好一些，特别是春节期间，用户无法预先知道什么时候请假回家，所以先买几张不同日期的，到时候提前退票或者改签。    </p><p>改签和退票就涉及到位置回收（对数据库来说也许是更新数据），改签还涉及购票同样的流程。    </p><h4 id="设计痛点-4"><a href="#设计痛点-4" class="headerlink" title="设计痛点"></a>设计痛点</h4><p>与购票类似  </p><h3 id="7-取票"><a href="#7-取票" class="headerlink" title="7 取票"></a>7 取票</h3><p>这个就很简单了，就是按照用户ID，查询已购买，未打印的车票。    </p><h3 id="8-其他需求"><a href="#8-其他需求" class="headerlink" title="8 其他需求"></a>8 其他需求</h3><h4 id="票的种类"><a href="#票的种类" class="headerlink" title="票的种类"></a>票的种类</h4><p>学生票、团体票、卧铺、站票    </p><p>这里特别是站票，站票是有上限的，需要控制一趟车的站票人数    </p><p>站票同样有起点和终点，但是有些用户可能买不到终点的票，会先买一段的，然后补票或者就一直在车上不下车，下车后再补票。  </p><h4 id="先上车后补票"><a href="#先上车后补票" class="headerlink" title="先上车后补票"></a>先上车后补票</h4><p>这个手段极其恶劣，不过很多人都是这么干的，未婚先孕，现在的年轻人啊。。。。  </p><p>通常会考虑容积率，避免站票太多。    </p><p>如果无节制的销售站票，可能坐不下的。   </p><h2 id="痛点小结"><a href="#痛点小结" class="headerlink" title="痛点小结"></a>痛点小结</h2><p>1. 大多数用户是有选择综合症的，通常来说，用户可能会查询很多次，才选到合适日期的合适车次的票。   </p><p>查询量比较大，春节期间更甚。    </p><p>2. 为了展示余票数量，需要统计，会耗费较多的CPU, IO资源。    </p><p>3. 路径规划的需求，帮用户找出（时间最短、行程最短、指定中转站、最廉价、或者站票最少）等条件的中转搭乘路线。    </p><p>妈妈再也不用担心买不到票啦。  </p><p>4. 余票信息需要统计，查询会耗费较多的CPU,IO。  </p><p>由于余票是不可信的，所以存在一定的延迟其实也是允许的，优化手段是异步统计，用户查询统计后的结果。   </p><p>5. 为了减少购票系统的写锁冲突，例如同一个座位，尽量不出现因为一个会话在更新它，其他会话需要等待的情况。    </p><p>（比如A用户买了北京到天津的，B用户买了天津到上海的同一趟车的同一个座位，那么应该设计合理的合并操作（如数据库内核改进）或者从设计上避免锁等待）  </p><p>其实就是把座位的空间维度（从哪里到哪里）、本身的属性（座位号）、时间维度（发车日期）进行解耦，放到多条记录中，从而在购买时，可以同时进行。   </p><p>因为数据库中最小的锁目前是行锁（单行记录同一时刻只允许一个会话进行更新，其他的被堵塞，等待释放锁），也许随着技术的发展，会演变成列锁，或者列里面的元素锁（比如数组，JSON）。    </p><p>6. 车次的变更简直是牵一发而动全身，比如余票统计会跟着变化，查询系统也要跟着变化。  </p><p>还有初始化信息的准备，例如为了加快购票的速度，可能会将车次的数据提前准备好（也许是每个座位一条记录），参考第3个需求的解说。    </p><p>综合以上痛点和需求分析，我们在设计时应尽量避免锁等待，避免实时余票查询，同时还要避免席位空洞。    </p><h2 id="二、猴子请来的救兵来啦"><a href="#二、猴子请来的救兵来啦" class="headerlink" title="二、猴子请来的救兵来啦"></a>二、猴子请来的救兵来啦</h2><p>经过前面的分析，已经把铁路售票系统最关键的几个业务场景进行了描述，并且阐述了其中的设计痛点，那么我们如何设计合理的系统来满足几亿人民抢票的需求呢？   </p><p>西游记里每一集孙悟空师父被妖怪抓走，总能找到救兵来解救。   </p><p>我们也需要救兵，救兵快来啊。。。。     </p><p>PostgreSQL是全世界最高级的开源数据库，几乎适用于任何场景。    </p><p>有很多特性是可以用来加快开发效率，满足架构需求的。     </p><p>针对铁路售票系统，可以用到哪些救命法宝呢？      </p><p>1. 法宝1，varbit类型   </p><p>使用varbit存储每趟车的每个座位途径站点是否已销售。    </p><p>例如 G1921车次，从北京到上海，途径天津、徐州、南京、苏州。包括起始站，总共6个站点。   那么使用6个比特位来表示。    </p><pre><code>&#39;000000&#39;</code></pre><p>如果我要买从天津到徐州的，这个值变更为(下车站的BIT不需要设置)   </p><pre><code>&#39;010000&#39;</code></pre><p>这个位置还可以卖从北京到天津，从徐州到终点的任意站点。    </p><p>余票统计也很方便，对整个车次根据BIT做聚合计算即可。    </p><p>统计任意组合站点的余票（ 北京-天津, 北京-徐州, 北京-南京, 北京-苏州, 北京-上海, 天津-徐州, 天津-南京, ……, 苏州-上海 ）    </p><pre><code>udf_count(varbit) returns record</code></pre><p>统计指定起始站点的余票（start: 北京, end: 南京； 则返回的是 北京-南京 的余票）    </p><pre><code>udf_count(varbit, start, end) returns record</code></pre><p>以上两个需求，开发对应的聚合函数即可，其实就是一些指定范围的bitand的count操作。     </p><p>通过法宝1，解决了统计余票的需求、售票无空洞的需求。  </p><p>2. 法宝2，数组类型   </p><p>使用数组存储每趟车的起始站点，途经站点。     </p><p>使用数组来存储，好处是可以使用到数组的GIN索引，快速的检索哪些车次是可以搭乘的。    </p><p>例如查询从北京到南京的车次。    </p><pre><code>select 车次 from 全国列车时刻表 where column_arr @&gt; array[&#39;北京&#39;,&#39;南京&#39;];</code></pre><p>这条SQL是可以走索引的，效率非常高，每秒请求几十万不是问题。   </p><p>法宝2解决了高并发请求查询符合条件的列车信息的需求。       </p><p>3. 法宝3，skip locked    </p><p>这个特性是跳过已被锁定的行，比如用户在购买某一趟从北京到南京的车票时，其实是一次UPDATE … SET BIT的操作。    </p><p>但是很可能其他用户也在购买，可能就会出现锁冲突，为了避免这个情况发生，可以skip locked，跳过锁冲突，直接找另一个座位。    </p><pre><code>select * from table     where column1=&#39;车次号&#39;   -- 指定车次    and column2=&#39;车次日期&#39;   -- 指定发车日期    -- and mod(pg_backend_pid(),100) = mod(pk,100)   -- 提高并发，如果有多个连接并发的在更新，可以直接分开落到不同的行，但是可能某些pID卖完了，可能会找不到票，建议不要开启这个条件    and column4=&#39;席别&#39;  -- 指定席别    and getbit(column3, 开始站点位置, 结束站点位置-1) = &#39;0...0&#39;  -- 获取起始位置的BIT位，要求全部为0    order by column3 desc   -- 这个目的是先把已经卖了散票的的座位拿来卖，也符合铁大哥的思想，尽量把起点和重点的票卖出去，减少空洞    for update    skip locked  -- 跳过被锁的行，老牛逼了，不需要锁等待    limit ?;     -- 要买几张票</code></pre><p>法宝3解决了一伙人来抢票时，在同一趟车的座位发生冲突的问题。   </p><p>4. 法宝4，cursor  </p><p>如果要查询大量记录，可以使用cursor，减少重复扫描。  </p><p>5. 法宝5，路径规划  </p><p>如果用户选择直达车已经无票了，可以自动计算如何转乘，根据用户的乘车站点和目的地选择最佳搭乘路线。    </p><p>参考一下pgrouting，与物流的动态路径规划需求一致。    </p><p><a href="../201607/20160710_01.md">《聊一聊双十一背后的技术 - 物流, 动态路径规划》</a>      </p><p>6. 法宝6，多核并行计算  </p><p>开源也支持多核并行计算的，在生成余票统计时，为了提高生成速度，可以将更多的CPU加入进来并行计算，快速得到余票统计。    </p><p>就比如你策划了一本书，已经列好了大纲，同时你找了100个作者，这100个作者可以根据你分配的工作，同时开始写作，很快就能把一本书写完。   </p><p>而传统的情况，一本书，只能一个作者帮你写，即使你找了100个作者，另外的99位也只能空闲，或者他们只能写其他的99本书。    </p><p>7. 法宝7，资源隔离  </p><p>PostgreSQL为进程模型，所以可以控制每个进程的资源开销，包括(CPU,IOPS,MEMORY,network)，在铁路售票系统中，查询和售票是最关键的需求，使用这种方法，可以在关键时刻保证关键业务有足够的资源，流畅运行。  </p><p>这个思想和双十一护航也是一样的，在双十一期间，会关掉一些不必要的业务，保证主要业务的资源，以及它们的流畅运行。    </p><p>8. 法宝8，分库分表  </p><p>铁路数据达到了海量数据的级别，很显然一台机器无法存下所有的铁路数据。    </p><p>那么怎么办呢？ 可以将铁路的数据进行分区存储，存到不同的主机。      </p><p>PostgreSQL的分库分表方案很多，例如plproxy, pgpool-II, pg-xl, pg-xc, citus等等.    </p><p>9. 法宝9，递归查询   </p><p>铁路有非常典型的上下文相关特性，例如一趟车途径N个站点，全国铁路组成了一个很大的铁路网。  </p><p>递归查询可以根据某一个节点，向上或者向下递归搜索相关的站点。  </p><p>比如在有哪些车可以直达北京，有哪些车可以转车到达北京，又或者查询从北京到拉萨，有哪些线路以及途经线路可以走。   </p><p><img data-src="20161124_02_pic_003.gif" alt="pic">    </p><p>10. 法宝10，MPP    </p><p>为了持续的提高12306的体验，铁大哥还有数据挖掘的需求，比如今年春节应该对哪些线路增加车次，每天的车次增加的规划，哪些线路可以减少车次也能在春节前将用户送回家。   </p><p>这些问题可以基于以往的运输数据进行挖掘计算，进行回答。    </p><p>基于PostgreSQL的MPP产品很多，例如Postgres-XL, Greenplum, Hawq, REDSHIFT, paraccl, 等等。    </p><p>使用PG可以和这些产品很好的融合，保持语法一致。   </p><p>降低数据分析的开发成本。    </p><p>猴子请来的救兵厉害吧，还有更厉害的，阿里云在PostgreSQL基础上做了很多的改进，比对对于12306的系统，就有特别的定制特性。   </p><h2 id="三、阿里云PostgreSQL-varbit-array增强介绍"><a href="#三、阿里云PostgreSQL-varbit-array增强介绍" class="headerlink" title="三、阿里云PostgreSQL varbit, array增强介绍"></a>三、阿里云PostgreSQL varbit, array增强介绍</h2><p>在铁路购票系统中，有几个需求需要用到bit和array的特殊功能，这些特殊的功能目前社区版本没有，阿里云RDS PostgreSQL对此做了增强，如下。    </p><p>1. 余票统计  </p><p>统计指定bit范围=全0的计数  </p><p>不指定范围，查询任意组合的bit范围全=0的计数  </p><p>2. 购票  </p><p>指定bit位置过滤、取出、设置对应的bit值    </p><p>根据数组值取其位置下标。    </p><p>回顾一下我之前写的两篇文章，也是使用varbit的应用场景，有异曲同工之妙        </p><p><a href="../201610/20161021_01.md">《基于 阿里云 RDS PostgreSQL 打造实时用户画像推荐系统》</a>         </p><p><a href="20161124_01.md">《门禁广告销售系统需求剖析 与 PostgreSQL数据库实现》</a>      </p><p>PostgreSQL的bit, array功能已经很强大，阿里云RDS PostgreSQL的bitpack也是用户实际应用中的需求提炼的新功能，大伙一起来给阿里云提需求。      </p><p>打造属于国人的PostgreSQL吧。           </p><h2 id="四、数据库设计-伪代码"><a href="#四、数据库设计-伪代码" class="headerlink" title="四、数据库设计(伪代码)"></a>四、数据库设计(伪代码)</h2><p>讲了这么多，最后提供一些伪代码，帮助大家来理解一下。  </p><p>1. 列车信息表 :     </p><pre><code>create table train     (id int primary key, --主键    go_date date, -- 发车日期    train_num name, -- 车次    station text[] -- 途径站点数组    );</code></pre><p>2. 位置信息表 :     </p><pre><code>create table train_sit     (id serial8 primary key, -- 主键    tid int references train (id), --关联列车ID    bno int, -- 车厢或bucket号    sit_level text, -- 席别  sit_no int,  -- 座位号  station_bit varbit  -- 途径站点组成的BIT位信息, 已售站点用1表示, 未售站点用0表示. 购票时设置起点和终点-1, 终点不设置   );</code></pre><p>3. 测试数据模型, 1趟火车, 途径14个站点.    </p><pre><code>insert into train values (1, &#39;2013-01-20&#39;, &#39;D645&#39;, array[&#39;上海南&#39;,&#39;嘉兴&#39;,&#39;杭州南&#39;,&#39;诸暨&#39;,&#39;义乌&#39;,&#39;金华&#39;,&#39;衢州&#39;,&#39;上饶&#39;,&#39;鹰潭&#39;,&#39;新余&#39;,&#39;宜春&#39;,&#39;萍乡&#39;,&#39;株洲&#39;,&#39;长沙&#39;]);</code></pre><p>4. 插入测试数据, 共计200W个车厢或bucket, 每个车厢98个位置.    </p><pre><code>insert into train_sit values (id, 1, id, &#39;一等座&#39;, generate_series(1,98), repeat(&#39;0&#39;,14)::varbit) from generate_series(1,1000000) t(id);    insert into train_sit values (id, 1, id, &#39;二等座&#39;, generate_series(1,98), repeat(&#39;0&#39;,98)::varbit) from generate_series(1000001,2000000) t(id);</code></pre><p>5. 创建取数组中元素位置的函数 (实际生产时可以使用C实现) :     </p><pre><code>create or replace function array_pos (a anyarray, b anyelement) returns int as $$    declare      i int;    begin      for i in 1..array_length(a,1) loop        if b=a[i] then          return i;        end if;        i := i+1;      end loop;      return null;    end;    $$ language plpgsql;</code></pre><p>6. 创建购票函数 (伪代码) :      </p><p>下单，更新    </p><pre><code>create or replace function buy     (    inout i_train_num name,     inout i_fstation text,     inout i_tstation text,    inout i_go_date date,    inout i_sits int, -- 购买多少张  out o_slevel text,    out o_bucket_no int,    out o_sit_no int,    out o_order_status boolean    )     declare    vid int[];  begin  -- 锁定席位  open cursor for  select array_agg(id) into vid[] from table     where column1=&#39;车次号&#39;   -- 指定车次    and column2=&#39;车次日期&#39;   -- 指定发车日期    -- and mod(pg_backend_pid(),100) = mod(pk,100)   -- 提高并发，如果有多个连接并发的在更新，可以直接分开落到不同的行，但是可能某些pID卖完了，可能会找不到票，建议不要开启这个条件    and column4=&#39;席别&#39;  -- 指定席别    and getbit(column3, 开始站点位置, 结束站点位置-1) = &#39;0...0&#39;  -- 获取起始位置的BIT位，要求全部为0    order by column3 desc   -- 这个目的是先把已经卖了散票的的座位拿来卖，也符合铁大哥的思想，尽量把起点和重点的票卖出去，减少空洞    for update    skip locked  -- 跳过被锁的行，老牛逼了，不需要锁等待    limit ?;     -- 要买几张票    if array_lengty(vid,1)=? then  -- 确保锁定行数与实际需要购票的数量一致       -- 购票，更新席别，设置对应BIT=1      update ... set column3=set_bit(column3, 1, 开始位置, 结束位置) where id = any(vid);    end if;  end;  $$ language plpgsql;</code></pre><p>测试(old 输出) :     </p><pre><code>digoal=# select * from buy(&#39;D645&#39;,&#39;杭州南&#39;,&#39;宜春&#39;,&#39;2013-01-20&#39;, 10);     i_train_num | i_fstation | i_tstation | i_go_date  | o_slevel | o_bucket_no | o_sit_no | o_order_status     -------------+------------+------------+------------+----------+-------------+----------+----------------     D645        | 杭州南     | 宜春       | 2013-01-20 | 一等座   |       35356 |        9 | t    (1 row)</code></pre><p>7. 余票统计(伪代码)  </p><p>表结构  </p><pre><code>create table ? (   车次   发车日期   起点   到站   余票  );</code></pre><p>统计SQL  </p><pre><code>select 车次,发车日期,count(varbit, 起点, 到站) from table group by 车次 发车日期;</code></pre><h2 id="五、小结"><a href="#五、小结" class="headerlink" title="五、小结"></a>五、小结</h2><p>本文从铁路购票系统的需求出发，分析了购票系统的痛点，以及数据库设计时需要注意的事项。      </p><p>PostgreSQL的10个特性，加上阿里云对varbit和array的改进，可以很好的满足铁路购票系统的需求。    </p><p>1. 照顾到余票查询的实时性、购票的锁竞争、以及超大规模的分库分表的需求。       </p><p>2. 购票时，如果是中途票，会尽量选择已售的中途票，减少位置空洞的产生，保证更多的人可以购买到全程票。  </p><p>3. 使用bit描述了每一个站点是否被售出，不会出现有票不能卖的情况。  </p><h2 id="六、以前写的同类文章"><a href="#六、以前写的同类文章" class="headerlink" title="六、以前写的同类文章"></a>六、以前写的同类文章</h2><h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><p>在PostgreSQL 中可以使用varbit存储比特位, 下面模拟一个简单的应用场景.    </p><p>马上春节了, 火车票又到了销售旺季, 一票难求依旧.  </p><p>下面就以火车票销售为例来介绍一下PostgreSQL varbit类型的用法.  </p><p>测试环境 :   </p><p>PostgreSQL 9.2.1  </p><p>测试表 :   </p><p>列车信息表 :   </p><pre><code>create table train   (id int primary key, --主键  go_date date, -- 发车日期  train_num name, -- 车次  station text[] -- 途径站点  );</code></pre><p>车厢或bucket信息表 :   </p><pre><code>create table train_bucket   (id int primary key, --主键  tid int references train (id), -- 关联列车ID  bno int, -- 车厢或bucket号  sit_level text, -- 席别  sit_cnt int, -- 该车厢或bucket的座位总数  sit_remain int, -- 剩余座位  sit_bit varbit -- 座位BIT位, 已售座位用1表示, 未售座位用0表示  );</code></pre><p>位置信息表 :   </p><pre><code>create table train_sit   (id serial8 primary key, -- 主键  tid int references train (id), --关联列车ID  tbid int references train_bucket(id), --关联bucket表ID  sit_no int,  -- 座位号, 来自train_bucket.sit_bit的位置信息.  station_bit varbit  -- 途径站点组成的BIT位信息, 已售站点用1表示, 未售站点用0表示.  );</code></pre><p>创建索引 :   </p><pre><code>create index idx_train_bucket_sit_remain on train_bucket(sit_remain) where sit_remain&gt;0;  create index idx_train_sit_station_bit on train_sit (station_bit) where station_bit&lt;&gt;repeat(&#39;1&#39;, 13)::varbit;</code></pre><p>插入测试数据, 1趟火车, 途径14个站点.  </p><pre><code>insert into train values (1, &#39;2013-01-20&#39;, &#39;D645&#39;, array[&#39;上海南&#39;,&#39;嘉兴&#39;,&#39;杭州南&#39;,&#39;诸暨&#39;,&#39;义乌&#39;,&#39;金华&#39;,&#39;衢州&#39;,&#39;上饶&#39;,&#39;鹰潭&#39;,&#39;新余&#39;,&#39;宜春&#39;,&#39;萍乡&#39;,&#39;株洲&#39;,&#39;长沙&#39;]);</code></pre><p>插入测试数据, 共计200W个车厢或bucket, 每个车厢98个位置.  </p><pre><code>insert into train_bucket values (generate_series(1,1000000), 1, generate_series(1,1000000), &#39;一等座&#39;, 98, 98, repeat(&#39;0&#39;,98)::varbit);  insert into train_bucket values (generate_series(1000001,2000000), 1, generate_series(1000001,2000000), &#39;二等座&#39;, 98, 98, repeat(&#39;0&#39;,98)::varbit);</code></pre><p>创建取数组中元素位置的函数 :   </p><pre><code>create or replace function array_pos (a anyarray, b anyelement) returns int as $$  declare    i int;  begin    for i in 1..array_length(a,1) loop      if b=a[i] then        return i;      end if;      i := i+1;    end loop;    return null;  end;  $$ language plpgsql;</code></pre><p>创建购票函数 :    </p><pre><code>create or replace function buy   (  inout i_train_num name,   inout i_fstation text,   inout i_tstation text,  inout i_go_date date,  out o_slevel text,  out o_bucket_no int,  out o_sit_no int,  out o_order_status boolean  )   returns record as $$  declare    curs1 refcursor;    curs2 refcursor;    v_row int;    v_station text[];    v_train_id int;    v_train_bucket_id int;    v_train_sit_id int;    v_from_station_idx int;    v_to_station_idx int;    v_station_len int;  begin    set enable_seqscan=off;    v_row := 0;    o_order_status := false;    select array_length(station,1), station, id, array_pos(station, i_fstation), array_pos(station, i_tstation)       into v_station_len, v_station, v_train_id, v_from_station_idx, v_to_station_idx       from train where train_num=i_train_num and go_date = i_go_date;    if ( found and array_pos(v_station, i_fstation) is not null          and array_pos(v_station, i_tstation) is not null          and array_pos(v_station, i_fstation) &lt; array_pos(v_station, i_tstation)        ) then    else      o_order_status := false;      return;    end if;    open curs2 for select tid,tbid,sit_no from train_sit      where (station_bit &amp; bitsetvarbit(repeat(&#39;0&#39;, v_station_len-1)::varbit, v_from_station_idx-1, v_to_station_idx-v_from_station_idx, 1)) = repeat(&#39;0&#39;, v_station_len-1)::varbit       and station_bit &lt;&gt; repeat(&#39;1&#39;, v_station_len-1)::varbit      -- and ctid not in (select locked_row from pgrowlocks(&#39;train_sit&#39;)) -- 耗时约300毫秒, 用它来解决热点锁等待不划算.      limit 1      for update nowait; -- 也可不加nowait, 加了的话如果获取锁失败将返回55P03异常, 需要程序重新提交    loop      fetch curs2 into v_train_id,v_train_bucket_id,o_sit_no;      if found then        update train_sit set station_bit=bitsetvarbit(station_bit, v_from_station_idx-1, v_to_station_idx-v_from_station_idx, 1)           where current of curs2;        GET DIAGNOSTICS v_row = ROW_COUNT;        if (v_row = 1) then          select sit_level, bno into o_slevel, o_bucket_no from train_bucket where id=v_train_bucket_id;   close curs2;   o_order_status := true;   return;        end if;      else         close curs2;        exit;      end if;    end loop;    v_row := 0;    open curs1 for select id, tid, strpos(sit_bit::text,&#39;0&#39;), sit_level, bno from train_bucket       where sit_remain&gt;0      -- and ctid not in (select locked_row from pgrowlocks(&#39;train_bucket&#39;)) -- 耗时约300毫秒, 用它来解决热点锁等待不划算.      limit 1       for update nowait; -- 也可不加nowait, 加了的话如果获取锁失败将返回55P03异常, 需要程序重新提交.    loop      fetch curs1 into v_train_bucket_id, v_train_id, o_sit_no, o_slevel, o_bucket_no;      if found then        update train_bucket set sit_bit = set_bit(sit_bit, strpos(sit_bit::text,&#39;0&#39;)-1, 1), sit_remain = sit_remain-1          where current of curs1;        GET DIAGNOSTICS v_row = ROW_COUNT;        if (v_row = 1) then          close curs1;   exit;        end if;      else         close curs1;        exit;      end if;    end loop;    if v_row = 1 then      insert into train_sit(tid,tbid,sit_no,station_bit)      values (        v_train_id,         v_train_bucket_id,         o_sit_no,        bitsetvarbit(repeat(&#39;0&#39;, v_station_len-1)::varbit, v_from_station_idx-1, v_to_station_idx-v_from_station_idx, 1)        );      o_order_status := true;      return;    else      o_order_status := false;      return;    end if;    exception     when others then      o_order_status := false;      return;  end;  $$ language plpgsql;</code></pre><p>测试 :   </p><pre><code>digoal=# select * from buy(&#39;D645&#39;,&#39;杭州南&#39;,&#39;宜春&#39;,&#39;2013-01-20&#39;);   i_train_num | i_fstation | i_tstation | i_go_date  | o_slevel | o_bucket_no | o_sit_no | o_order_status   -------------+------------+------------+------------+----------+-------------+----------+----------------   D645        | 杭州南     | 宜春       | 2013-01-20 | 一等座   |       35356 |        9 | t  (1 row)</code></pre><h2 id="压力测试"><a href="#压力测试" class="headerlink" title="压力测试"></a>压力测试</h2><pre><code>vi test.sql  select * from buy(&#39;D645&#39;,&#39;上海南&#39;,&#39;长沙&#39;,&#39;2013-01-20&#39;);</code></pre><p>不加nowait测试结果 :   </p><pre><code>ocz@db-172-16-3-150-&gt; pgbench -M prepared -f ./test.sql -n -r -c 16 -j 8 -T 1200 -U postgres digoal  transaction type: Custom query  scaling factor: 1  query mode: prepared  number of clients: 16  number of threads: 8  duration: 1200 s  number of transactions actually processed: 2197407  tps = 1831.143708 (including connections establishing)  tps = 1831.169308 (excluding connections establishing)  statement latencies in milliseconds:          8.734424        select * from buy(&#39;D645&#39;,&#39;上海南&#39;,&#39;长沙&#39;,&#39;2013-01-20&#39;);</code></pre><p>加nowait测试结果 :   </p><pre><code>ocz@db-172-16-3-150-&gt; pgbench -M prepared -f ./test.sql -n -r -c 16 -j 16 -T 12 -U postgres digoal  transaction type: Custom query  scaling factor: 1  query mode: prepared  number of clients: 16  number of threads: 16  duration: 12 s  number of transactions actually processed: 93632  tps = 7800.056248 (including connections establishing)  tps = 7818.803904 (excluding connections establishing)  statement latencies in milliseconds:          2.042862        select * from buy(&#39;D645&#39;,&#39;上海南&#39;,&#39;长沙&#39;,&#39;2013-01-20&#39;);</code></pre><h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>1. 需要解决更新热点, 降低等待, 提高并行处理几率.   </p><pre><code>本例的热点在 :   </code></pre><pre><code>update train_bucket set sit_bit = set_bit(sit_bit, strpos(sit_bit::text,&#39;0&#39;)-1, 1), sit_remain = sit_remain-1     where current of curs1;</code></pre><p>以及  </p><pre><code> update train_sit set station_bit=bitsetvarbit(station_bit, v_from_station_idx-1, v_to_station_idx-v_from_station_idx, 1)       where current of curs2;</code></pre><p>对应的游标  :  </p><pre><code>  open curs2 for select tid,tbid,sit_no from train_sit      where (station_bit &amp; bitsetvarbit(repeat(&#39;0&#39;, v_station_len-1)::varbit, v_from_station_idx-1, v_to_station_idx-v_from_station_idx, 1)) = repeat(&#39;0&#39;, v_station_len-1)::varbit       and station_bit &lt;&gt; repeat(&#39;1&#39;, v_station_len-1)::varbit      -- and ctid not in (select locked_row from pgrowlocks(&#39;train_sit&#39;)) -- 耗时约300毫秒, 用它来解决热点锁等待不划算.      limit 1      for update;</code></pre><p>以及  </p><pre><code>  open curs1 for select id, tid, strpos(sit_bit::text,&#39;0&#39;), sit_level, bno from train_bucket       where sit_remain&gt;0      -- and ctid not in (select locked_row from pgrowlocks(&#39;train_bucket&#39;)) -- 耗时约300毫秒, 用它来解决热点锁等待不划算.      limit 1       for update;</code></pre><p>解决的关键在这里.  </p><p>如果不能解决热点的问题, 那就提高处理速度, 精简字段数量和长度, 精简索引. 提高更新速度.  </p><p>2. 减少数据扫描的量.  </p><pre><code>partial index, 避免满座车厢的扫描, 以及全程占位位子的扫描.  </code></pre><p>3. 先查bucket 是否空闲, 再查sit是否空闲.   </p><p>4. 还需要考虑优先级的问题 :   </p><p>例如有111000和111100两个位子, 如果请求要最后两个站的票, 应该优先匹配111100, 这样更不容易浪费。如下 :   </p><pre><code>111000 | 000011 = 111011  111100 | 000011 = 111111</code></pre><h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p>1. setbitvarbit  </p><p><a href="http://blog.163.com/digoal@126/blog/static/163877040201302192427651/" target="_blank" rel="noopener">http://blog.163.com/digoal@126/blog/static/163877040201302192427651/</a>  </p><p>​<br>​              </p><p><a rel="noopener" href="http://info.flagcounter.com/h9V1" target="_blank"><img data-src="http://s03.flagcounter.com/count/h9V1/bg_FFFFFF/txt_000000/border_CCCCCC/columns_2/maxflags_12/viewers_0/labels_0/pageviews_0/flags_0/" alt="Flag Counter" border="0"></a>  </p>]]></content>
      
      
      
        <tags>
            
            <tag> 12306 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>杂</title>
      <link href="/2018/04/16/everything/"/>
      <url>/2018/04/16/everything/</url>
      
        <content type="html"><![CDATA[<h2 id="Spring的传播特性"><a href="#Spring的传播特性" class="headerlink" title="Spring的传播特性"></a>Spring的传播特性</h2><a id="more"></a><ol><li><strong>PROPAGATION_REQUIRED（加入已有事务）</strong><br>尝试加入已经存在的事务中，如果没有则开启一个新的事务。</li><li><strong>RROPAGATION_REQUIRES_NEW（独立事务）</strong><br>挂起当前存在的事务，并开启一个全新的事务，新事务与已存在的事务之间彼此没有关系。</li><li><strong>PROPAGATION_NESTED（嵌套事务）</strong><br>在当前事务上开启一个子事务（Savepoint），如果递交主事务。那么连同子事务一同递交。如果递交子事务则保存点之前的所有事务都会被递交。</li><li><strong>PROPAGATION_SUPPORTS（跟随环境）</strong><br>是指 Spring 容器中如果当前没有事务存在，就以非事务方式执行；如果有，就使用当前事务。</li><li><strong>PROPAGATION_NOT_SUPPORTED（非事务方式）</strong><br>是指如果存在事务则将这个事务挂起，并使用新的数据库连接。新的数据库连接不使用事务。</li><li><strong>PROPAGATION_NEVER（排除事务）</strong><br>当存在事务时抛出异常，否则就已非事务方式运行。</li><li><strong>PROPAGATION_MANDATORY（需要事务）</strong><br>如果不存在事务就抛出异常，否则就已事务方式运行。</li></ol><h2 id="如何处理高并发情况下具有竞争关系的数据库资源"><a href="#如何处理高并发情况下具有竞争关系的数据库资源" class="headerlink" title="如何处理高并发情况下具有竞争关系的数据库资源"></a>如何处理高并发情况下具有竞争关系的数据库资源</h2><p>秒杀系统数据库减压方法；</p><p>节流：仅让能成功抢购到商品的流量（可以有一定余量）进入我们的系统。</p><p>削峰：将进入系统的瞬时高流量拉平，使得系统可以在自己处理能力范围内，将所有抢购的请求处理完毕。</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>java gc复制法为什么要用survivor</title>
      <link href="/2018/04/16/java-gc/"/>
      <url>/2018/04/16/java-gc/</url>
      
        <content type="html"><![CDATA[<h3 id="java-gc复制法为什么要用survivor区？它在分配担保的时候发挥了什么作用？"><a href="#java-gc复制法为什么要用survivor区？它在分配担保的时候发挥了什么作用？" class="headerlink" title="java gc复制法为什么要用survivor区？它在分配担保的时候发挥了什么作用？"></a>java gc复制法为什么要用survivor区？它在分配担保的时候发挥了什么作用？</h3><a id="more"></a><p>复制法的出发点是为了提高效率、避免出现内存碎片，其实现方式为：将内存一分为二，对象申请内存时只使用其中一半，当GC时将活着的对象拷贝到另外一半内存，然后将现在使用的这段内存回收掉。 但复制法的代价太高了：内存只能用一半。实际上虚拟机在新生代的回收使用了复制法，但做了改进：由于新生代的内存在GC时可能只有极少数的对象还处于存活状态，所以复制目的地不需要“一半”这么大。HotSpot虚拟机以8：1的比例来分割内存，内存分为1块Eden区（8/10），2块Survivor区（各1/10），内存回收的时候将Eden+Survivor的存活对象拷贝到另一块Survivor区。如果不幸存活对象太多导致Survivor空间存储不下，虚拟机会从老年代进行分配担保。 这里有一个问题，为什么要设计2块Survivor区呢？ 答案是很“显然”的，如果只有1块Survivor区，那么下次回收时，这块Survivor上还是有对象是活跃的，既然是复制法，那也只能复制到另外一块Survivor上了。考虑到Survivor空间不大，而更可能的是其对象在下次GC的时候需要被回收的可能性很高，所以也没有必要对Survivor区去做Mark-Sweep/Compact了。</p><p><strong>survivor区每存活一次，年龄+1 加到一定程度的时候会统一复制到老年代</strong></p><p>作用：</p><p><strong>我们知道如果对象在复制到Survivor区时若Survivor空间不足，则会出发担保机制，将对象转入老年代；</strong></p><p>但老年代的能力也不是无限的，因此需要在minor GC时做一个是否需要Major GC 的判断：</p><ul><li>如果老年代的剩余空间 &lt; 之前转入老年代的对象的平均大小，则触发Major GC</li><li>如果老年代的剩余空间 &gt; 之前转入老年代的对象的平均大小，并且允许担保失败，则直接Minor GC，不需要做Full GC</li><li>如果老年代的剩余空间 &gt; 之前转入老年代的对象的平均大小，并且不允许担保失败，则触发Major GC</li></ul><p>出发点还是尽量为对象分配内存。但是一般会配置允许担保失败，避免频繁的去做Full GC。</p>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> 虚拟机 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>如何理解HTTP请求的RESTful的幂等性</title>
      <link href="/2018/04/04/http-idempotence/"/>
      <url>/2018/04/04/http-idempotence/</url>
      
        <content type="html"><![CDATA[<p>理解RESTful的幂等性，并且设计符合幂等规范的高质量RESTful API。<br><a id="more"></a></p><h2 id="怎么理解HTTP请求GET-POST-PUT-DELETE的幂等性"><a href="#怎么理解HTTP请求GET-POST-PUT-DELETE的幂等性" class="headerlink" title="怎么理解HTTP请求GET/POST/PUT/DELETE的幂等性"></a>怎么理解HTTP请求GET/POST/PUT/DELETE的幂等性</h2><p>HTTP幂等方法，是指无论调用多少次都不会有不同结果的 HTTP 方法。不管你调用一次，还是调用一百次，一千次，结果都是相同的。</p><p>还是以之前的博文的例子为例。</p><pre><code class="lang-javascript">GET     /tickets       # 获取ticket列表GET     /tickets/12    # 查看某个具体的ticketPOST    /tickets       # 新建一个ticketPUT     /tickets/12    # 更新ticket 12PATCH   /tickets/12    # 更新ticket 12DELETE  /tickets/12    # 删除ticekt 12</code></pre><h3 id="HTTP-GET方法"><a href="#HTTP-GET方法" class="headerlink" title="HTTP GET方法"></a>HTTP GET方法</h3><p>HTTP GET方法，用于获取资源，不管调用多少次接口，结果都不会改变，所以是幂等的。</p><pre><code>GET     /tickets       # 获取ticket列表GET     /tickets/12    # 查看某个具体的ticket</code></pre><p>只是查询数据，不会影响到资源的变化，因此我们认为它幂等。</p><p>值得注意，幂等性指的是作用于结果而非资源本身。怎么理解呢？例如，这个HTTP GET方法可能会每次得到不同的返回内容，但并不影响资源。</p><p>可能你会问有这种情况么？当然有咯。例如，我们有一个接口获取当前时间，我们就应该设计成</p><pre><code>GET     /service_time # 获取服务器当前时间</code></pre><p>它本身不会对资源本身产生影响，因此满足幂等性。</p><h3 id="HTTP-POST方法"><a href="#HTTP-POST方法" class="headerlink" title="HTTP POST方法"></a>HTTP POST方法</h3><p>HTTP POST方法是一个非幂等方法，因为调用多次，都将产生新的资源。</p><pre><code>POST    /tickets       # 新建一个ticket</code></pre><p>因为它会对资源本身产生影响，每次调用都会有新的资源产生，因此不满足幂等性。</p><h3 id="HTTP-PUT方法"><a href="#HTTP-PUT方法" class="headerlink" title="HTTP PUT方法"></a>HTTP PUT方法</h3><p>HTTP PUT方法是不是幂等的呢？我们来看下</p><pre><code>PUT     /tickets/12    # 更新ticket 12</code></pre><p>因为它直接把实体部分的数据替换到服务器的资源，我们多次调用它，只会产生一次影响，但是有相同结果的 HTTP 方法，所以满足幂等性。</p><h3 id="HTTP-PATCH方法"><a href="#HTTP-PATCH方法" class="headerlink" title="HTTP PATCH方法"></a>HTTP PATCH方法</h3><p>HTTP PATCH方法是非幂等的。HTTP POST方法和HTTP PUT方法可能比较好理解，但是HTTP PATCH方法只是更新部分资源，怎么是非幂等的呢?</p><p>因为，PATCH提供的实体则需要根据程序或其它协议的定义，解析后在服务器上执行，以此来修改服务器上的资源。换句话说，PATCH请求是会执行某个程序的，如果重复提交，程序可能执行多次，对服务器上的资源就可能造成额外的影响，这就可以解释它为什么是非幂等的了。</p><p>可能你还不能理解这点。我们举个例子</p><pre><code>PATCH   /tickets/12    # 更新ticket 12</code></pre><p>此时，我们服务端对方法的处理是，当调用一次方法，更新部分字段，将这条ticket记录的操作记录加一，这次，每次调用的资源是不是变了呢，所以它是有可能是非幂等的操作。</p><h3 id="HTTP-DELETE方法"><a href="#HTTP-DELETE方法" class="headerlink" title="HTTP DELETE方法"></a>HTTP DELETE方法</h3><p>HTTP DELETE方法用于删除资源，会将资源删除。</p><pre><code>DELETE  /tickets/12    # 删除ticekt 12</code></pre><p>调用一次和多次对资源产生影响是相同的，所以也满足幂等性。</p><h2 id="如何设计符合幂等性的高质量RESTful-API"><a href="#如何设计符合幂等性的高质量RESTful-API" class="headerlink" title="如何设计符合幂等性的高质量RESTful API"></a>如何设计符合幂等性的高质量RESTful API</h2><h3 id="HTTP-GET方法-vs-HTTP-POST方法"><a href="#HTTP-GET方法-vs-HTTP-POST方法" class="headerlink" title="HTTP GET方法 vs HTTP POST方法"></a>HTTP GET方法 vs HTTP POST方法</h3><p>也许，你会想起一个面试题。HTTP请求的GET与POST方式有什么区别？你可能会回答到：GET方式通过URL提交数据，数据在URL中可以看到；POST方式，数据放置在HTML HEADER内提交。但是，我们现在从RESTful的资源角度来看待问题，HTTP GET方法是幂等的，所以它适合作为查询操作，HTTP POST方法是非幂等的，所以用来表示新增操作。</p><p>但是，也有例外，我们有的时候可能需要把查询方法改造成HTTP POST方法。比如，超长（1k）的GET URL使用POST方法来替代，因为GET受到URL长度的限制。虽然，它不符合幂等性，但是它是一种折中的方案。</p><h3 id="HTTP-POST方法-vs-HTTP-PUT方法"><a href="#HTTP-POST方法-vs-HTTP-PUT方法" class="headerlink" title="HTTP POST方法 vs HTTP PUT方法"></a>HTTP POST方法 vs HTTP PUT方法</h3><p>对于HTTP POST方法和TTP PUT方法，我们一般的理解是POST表示创建资源，PUT表示更新资源。当然，这个是正确的理解。</p><p>但是，实际上，两个方法都用于创建资源，更为本质的差别是在幂等性。HTTP POST方法是非幂等，所以用来表示创建资源，HTTP PUT方法是幂等的，因此表示更新资源更加贴切。</p><h3 id="HTTP-PUT方法-vs-HTTP-PATCH方法"><a href="#HTTP-PUT方法-vs-HTTP-PATCH方法" class="headerlink" title="HTTP PUT方法 vs HTTP PATCH方法"></a>HTTP PUT方法 vs HTTP PATCH方法</h3><p>此时，你看会有另外一个问题。HTTP PUT方法和HTTP PATCH方法，都是用来表述更新资源，它们之间有什么区别呢？我们一般的理解是PUT表示更新全部资源，PATCH表示更新部分资源。首先，这个是我们遵守的第一准则。根据上面的描述，PATCH方法是非幂等的，因此我们在设计我们服务端的RESTful API的时候，也需要考虑。如果，我们想要明确的告诉调用者我们的资源是幂等的，我的设计更倾向于使用 HTTP PUT 方法。</p>]]></content>
      
      
      
        <tags>
            
            <tag> 计算机网络 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>数据库隔离级别</title>
      <link href="/2018/03/21/database-isolation/"/>
      <url>/2018/03/21/database-isolation/</url>
      
        <content type="html"><![CDATA[<h1 id="数据库隔离级别"><a href="#数据库隔离级别" class="headerlink" title="数据库隔离级别"></a>数据库隔离级别</h1><a id="more"></a><div class="table-container"><table><thead><tr><th></th><th>脏读</th><th>不可重复读</th><th>幻读</th></tr></thead><tbody><tr><td>Read uncommitted</td><td>√</td><td>√</td><td>√</td></tr><tr><td>Read committed</td><td>×</td><td>√</td><td>√</td></tr><tr><td>Repeatable read</td><td>×</td><td>×</td><td>√</td></tr><tr><td>Serializable</td><td>×</td><td>×</td><td>×</td></tr></tbody></table></div><h3 id="Read-Uncommitted"><a href="#Read-Uncommitted" class="headerlink" title="Read Uncommitted"></a>Read Uncommitted</h3><p>读未提交，脏读</p><blockquote><p>事物B开始——B修改数据（1000-&gt;2000）——B发现不对，回滚——B修改数据1000-&gt;1500——B提交</p><p>​    事物A开始——A读到数据2000——结束                                    A再读发现不是2000</p></blockquote><h3 id="Read-committed-Oracle-Sql-Server"><a href="#Read-committed-Oracle-Sql-Server" class="headerlink" title="Read committed    Oracle Sql Server"></a>Read committed    Oracle Sql Server</h3><p>避免脏读但是不能避免<strong>不可重复读</strong>，</p><p>指在一个事务内，多次读同一数据。在这个事务还没有结束时，另外一个事务也访问该同一数据。那么，在第一个事务中的两 次读数据之间，由于第二个事务的修改，那么第一个事务两次读到的的数据可能是不一样的。这样就发生了在一个事务内两次读到的数据是不一样的，因此称为是不 可重复读。例如，一个编辑人员两次读取同一文档，但在两次读取之间，作者重写了该文档。当编辑人员第二次读取文档时，文档已更改。原始读取不可重复。如果 只有在作者全部完成编写后编辑人员才可以读取文档，则可以避免该问题。</p><blockquote><p>事物B开始——B访问数据得到2000————B休息了一会————B又访问发现不是2000——事物B提交错误</p><p>​                       事物A开始——事物A转走了2000——事物A提交</p></blockquote><h3 id="Repeatable-read-MySQL"><a href="#Repeatable-read-MySQL" class="headerlink" title="Repeatable read   MySQL"></a>Repeatable read   MySQL</h3><p>避免不可重复读，但是会出现<strong>幻读</strong>。</p><p>在可重复读中，该sql第一次读取到数据后，就将这些数据加锁，其它事务无法修改这些数据，就可以实现可重复 读了。<strong>但这种方法却无法锁住insert的数据</strong>，所以当事务A先前读取了数据，或者修改了全部数据，事务B还是可以insert数据提交，这时事务A就会 发现莫名其妙多了一条之前没有的数据，这就是幻读，不能通过行锁来避免</p><blockquote><p>事物A读 ——事物查询全部——事物A等了一会儿——事物A又查询，发现多了一条</p><p>​                        事物B插入</p></blockquote><p>​        </p><h3 id="Serializable"><a href="#Serializable" class="headerlink" title="Serializable"></a>Serializable</h3><p>最高级别</p>]]></content>
      
      
      
        <tags>
            
            <tag> database </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>深入理解java虚拟机阅读笔记</title>
      <link href="/2018/03/19/java-reading-note/"/>
      <url>/2018/03/19/java-reading-note/</url>
      
        <content type="html"><![CDATA[<h3 id="第二章java内存区域与内存溢出异常"><a href="#第二章java内存区域与内存溢出异常" class="headerlink" title="第二章java内存区域与内存溢出异常"></a>第二章java内存区域与内存溢出异常</h3><a id="more"></a><ol><li><p>java虚拟机规范中对于java虚拟机栈规定了两种异常状况，如果线程请求的栈的深度大于虚拟机所允许的深度，将抛出<strong>Stack Overflow</strong>的异常，虚拟机动态扩展时都无法申请到足够的内存时，或者方法区无法满足内存分配的需求时，抛出<strong>OutofMemory</strong>的异常。常量池是方法区的一部分，当它无法申请到内存的时候，也会抛出<strong>OutofMemory</strong>的异常。</p></li><li><p>java虚拟机的栈内存（Stack）存放的是局部变量表的部分，包括各种基本数据类型（Boolean，byte，cjhar short float long int double），对象引用和returnAddress类型（指向了一条字节码指令的地址）。</p><p>堆内存是虚拟机管理的内存中最大的一块。<strong>被所有线程共享</strong>，存放对象实例。垃圾收集器的主要管理区域。</p></li><li><p>​</p></li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> java源码 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>redis 应用场景 application scenarios</title>
      <link href="/2018/03/09/redis-scenarios/"/>
      <url>/2018/03/09/redis-scenarios/</url>
      
        <content type="html"><![CDATA[<h1 id="Redis实际应用场景"><a href="#Redis实际应用场景" class="headerlink" title="Redis实际应用场景"></a>Redis实际应用场景</h1><p>​        Redis在很多方面与其他数据库解决方案不同：它使用内存提供主存储支持，而仅使用硬盘做持久性的存储；它的数据模型非常独特，用的是单线程。另一个大区别在于，你可以在开发环境中使用Redis的功能，但却不需要转到Redis。<br><a id="more"></a></p><p>转向Redis当然也是可取的，许多开发者从一开始就把Redis作为首选数据库；但设想如果你的开发环境已经搭建好，应用已经在上面运行了，那么更换数据库框架显然不那么容易。另外在一些需要大容量数据集的应用，Redis也并不适合，因为它的数据集不会超过系统可用的内存。所以如果你有大数据应用，而且主要是读取访问模式，那么Redis并不是正确的选择。</p><p>​        然而我喜欢Redis的一点就是你可以把它融入到你的系统中来，这就能够解决很多问题，比如那些你现有的数据库处理起来感到缓慢的任务。这些你就可以通过Redis来进行优化，或者为应用创建些新的功能。在本文中，我就想探讨一些怎样将Redis加入到现有的环境中，并利用它的原语命令等功能来解决 传统环境中碰到的一些常见问题。在这些例子中，Redis都不是作为首选数据库。</p><h3 id="1、显示最新的项目列表"><a href="#1、显示最新的项目列表" class="headerlink" title="1、显示最新的项目列表"></a><strong>1、显示最新的项目列表</strong></h3><p>下面这个语句常用来显示最新项目，随着数据多了，查询毫无疑问会越来越慢。</p><pre><code class="lang-sql">SELECT * FROM foo WHERE ... ORDER BY time DESC LIMIT 10</code></pre><p>​        在Web应用中，“列出最新的回复”之类的查询非常普遍，这通常会带来可扩展性问题。这令人沮丧，因为项目本来就是按这个顺序被创建的，但要输出这个顺序却不得不进行排序操作。</p><p>​        类似的问题就可以用Redis来解决。比如说，我们的一个Web应用想要列出用户贴出的最新20条评论。在最新的评论边上我们有一个“显示全部”的链接，点击后就可以获得更多的评论。</p><p>​        我们假设数据库中的每条评论都有一个唯一的递增的ID字段。</p><p>​        我们可以使用分页来制作主页和评论页，使用Redis的模板，每次新评论发表时，我们会将它的ID添加到一个Redis列表：</p><pre><code class="lang-sql"> LPUSH latest.comments &lt;ID&gt;</code></pre><p>​       我们将列表裁剪为指定长度，因此Redis只需要保存最新的5000条评论：</p><blockquote><p> LTRIM latest.comments 0 5000 </p></blockquote><p>​      每次我们需要获取最新评论的项目范围时，我们调用一个函数来完成（使用伪代码）：</p><pre><code class="lang-javascript">FUNCTION get_latest_comments(start, num_items):      id_list = redis.lrange(&quot;latest.comments&quot;,start,start+num_items - 1)      IF id_list.length &lt; num_items          id_list = SQL_DB(&quot;SELECT ... ORDER BY time LIMIT ...&quot;)      END      RETURN id_list  END</code></pre><p>​      这里我们做的很简单。在Redis中我们的最新ID使用了常驻缓存，这是一直更新的。但是我们做了限制不能超过5000个ID，因此我们的获取ID函数会一直询问Redis。只有在start/count参数超出了这个范围的时候，才需要去访问数据库。</p><p>​        我们的系统不会像传统方式那样“刷新”缓存，Redis实例中的信息永远是一致的。SQL数据库（或是硬盘上的其他类型数据库）只是在用户需要获取“很远”的数据时才会被触发，而主页或第一个评论页是不会麻烦到硬盘上的数据库了。</p><h3 id="2、删除与过滤"><a href="#2、删除与过滤" class="headerlink" title="2、删除与过滤"></a><strong>2、删除与过滤</strong></h3><p>​      我们可以使用LREM来删除评论。如果删除操作非常少，另一个选择是直接跳过评论条目的入口，报告说该评论已经不存在。</p><p>​       有些时候你想要给不同的列表附加上不同的过滤器。如果过滤器的数量受到限制，你可以简单的为每个不同的过滤器使用不同的Redis列表。毕竟每个列表只有5000条项目，但Redis却能够使用非常少的内存来处理几百万条项目。</p><h3 id="3、排行榜相关"><a href="#3、排行榜相关" class="headerlink" title="3、排行榜相关"></a><strong>3、排行榜相关</strong></h3><p>​      另一个很普遍的需求是各种数据库的数据并非存储在内存中，因此在按得分排序以及实时更新这些几乎每秒钟都需要更新的功能上数据库的性能不够理想。</p><p>​      典型的比如那些在线游戏的排行榜，比如一个Facebook的游戏，根据得分你通常想要：</p><p>​         - 列出前100名高分选手</p><p>​         - 列出某用户当前的全球排名</p><p>​      这些操作对于Redis来说小菜一碟，即使你有几百万个用户，每分钟都会有几百万个新的得分。</p><p>​      模式是这样的，每次获得新得分时，我们用这样的代码：</p><p>​      ZADD leaderboard  <score>  <username> </username></score></p><p>​     你可能用userID来取代username，这取决于你是怎么设计的。</p><p>​      得到前100名高分用户很简单：ZREVRANGE leaderboard 0 99。</p><p>​      用户的全球排名也相似，只需要：ZRANK leaderboard <username>。</username></p><h3 id="4、按照用户投票和时间排序"><a href="#4、按照用户投票和时间排序" class="headerlink" title="4、按照用户投票和时间排序"></a><strong>4、按照用户投票和时间排序</strong></h3><p>​      排行榜的一种常见变体模式就像Reddit或Hacker News用的那样，新闻按照类似下面的公式根据得分来排序：</p><p>​       score = points / time^alpha </p><p>​      因此用户的投票会相应的把新闻挖出来，但时间会按照一定的指数将新闻埋下去。下面是我们的模式，当然算法由你决定。</p><p>​      模式是这样的，开始时先观察那些可能是最新的项目，例如首页上的1000条新闻都是候选者，因此我们先忽视掉其他的，这实现起来很简单。</p><p>​      每次新的新闻贴上来后，我们将ID添加到列表中，使用LPUSH + LTRIM，确保只取出最新的1000条项目。</p><p>​      有一项后台任务获取这个列表，并且持续的计算这1000条新闻中每条新闻的最终得分。计算结果由ZADD命令按照新的顺序填充生成列表，老新闻则被清除。这里的关键思路是排序工作是由后台任务来完成的。</p><h3 id="5、处理过期项目"><a href="#5、处理过期项目" class="headerlink" title="5、处理过期项目"></a><strong>5、处理过期项目</strong></h3><p>​      另一种常用的项目排序是按照时间排序。我们使用unix时间作为得分即可。</p><p>​      模式如下：</p><p>​       - 每次有新项目添加到我们的非Redis数据库时，我们把它加入到排序集合中。这时我们用的是时间属性，current_time和time_to_live。</p><p>​       - 另一项后台任务使用ZRANGE…SCORES查询排序集合，取出最新的10个项目。如果发现unix时间已经过期，则在数据库中删除条目。</p><h3 id="6、计数"><a href="#6、计数" class="headerlink" title="6、计数"></a><strong>6、计数</strong></h3><p>​       Redis是一个很好的计数器，这要感谢INCRBY和其他相似命令。</p><p>​       我相信你曾许多次想要给数据库加上新的计数器，用来获取统计或显示新信息，但是最后却由于写入敏感而不得不放弃它们。</p><p>​       好了，现在使用Redis就不需要再担心了。有了原子递增（atomic increment），你可以放心的加上各种计数，用GETSET重置，或者是让它们过期。</p><p>​       例如这样操作：</p><p>​         INCR user:<id> EXPIRE </id></p><p>​         user:<id> 60 </id></p><p>​       你可以计算出最近用户在页面间停顿不超过60秒的页面浏览量，当计数达到比如20时，就可以显示出某些条幅提示，或是其它你想显示的东西。</p><h3 id="7、特定时间内的特定项目"><a href="#7、特定时间内的特定项目" class="headerlink" title="7、特定时间内的特定项目"></a><strong>7、特定时间内的特定项目</strong></h3><p>​        另一项对于其他数据库很难，但Redis做起来却轻而易举的事就是统计在某段特点时间里有多少特定用户访问了某个特定资源。比如我想要知道某些特定的注册用户或IP地址，他们到底有多少访问了某篇文章。</p><p>​      每次我获得一次新的页面浏览时我只需要这样做：</p><p>​       SADD page:day1:<page_id> <user_id> </user_id></page_id></p><p>​      当然你可能想用unix时间替换day1，比如time()-(time()%3600*24)等等。</p><p>​      想知道特定用户的数量吗？只需要使用SCARD page:day1:<page_id>。</page_id></p><p>​       需要测试某个特定用户是否访问了这个页面？SISMEMBER page:day1:<page_id>。</page_id></p><hr><h3 id="8、实时分析正在发生的情况，用于数据统计与防止垃圾邮件等"><a href="#8、实时分析正在发生的情况，用于数据统计与防止垃圾邮件等" class="headerlink" title="8、实时分析正在发生的情况，用于数据统计与防止垃圾邮件等"></a><strong>8、实时分析正在发生的情况，用于数据统计与防止垃圾邮件等</strong></h3><p>​        我们只做了几个例子，但如果你研究Redis的命令集，并且组合一下，就能获得大量的实时分析方法，有效而且非常省力。使用Redis原语命令，更容易实施垃圾邮件过滤系统或其他实时跟踪系统。</p><h3 id="9、Pub-Sub"><a href="#9、Pub-Sub" class="headerlink" title="9、Pub/Sub"></a><strong>9、Pub/Sub</strong></h3><p>​       Redis的Pub/Sub非常非常简单，运行稳定并且快速。支持模式匹配，能够实时订阅与取消频道。</p><h3 id="10、队列"><a href="#10、队列" class="headerlink" title="10、队列"></a><strong>10、队列</strong></h3><p>​        你应该已经注意到像list push和list pop这样的Redis命令能够很方便的执行队列操作了，但能做的可不止这些：比如Redis还有list pop的变体命令，能够在列表为空时阻塞队列。</p><p>​       现代的互联网应用大量地使用了消息队列（Messaging）。消息队列不仅被用于系统内部组件之间的通信，同时也被用于系统跟其它服务之间的交互。消息队列的使用可以增加系统的可扩展性、灵活性和用户体验。非基于消息队列的系统，其运行速度取决于系统中最慢的组件的速度（注：短板效应）。而基于消息队列可以将系统中各组件解除耦合，这样系统就不再受最慢组件的束缚，各组件可以异步运行从而得以更快的速度完成各自的工作。</p><p>​    此外，当服务器处在高并发操作的时候，比如频繁地写入日志文件。可以利用消息队列实现异步处理。从而实现高性能的并发操作。</p><h3 id="11、缓存"><a href="#11、缓存" class="headerlink" title="11、缓存"></a><strong>11、缓存</strong></h3><p>​        Redis的缓存部分值得写一篇新文章，我这里只是简单的说一下。Redis能够替代memcached，让你的缓存从只能存储数据变得能够更新数据，因此你不再需要每次都重新生成数据了。</p><h3 id="补充"><a href="#补充" class="headerlink" title="补充"></a>补充</h3><ol><li><p>redis作为MySQL外部缓存的时候如何保证数据的一致性，数据库更新和缓存的同步问题</p><ol><li>全部写入Redis。</li></ol></li></ol><ol><li>MySQL binlog增量订阅消费+消息队列+处理并把数据更新到redis，也就是说增量更新</li><li>如果MySQL的增删改操作太多太频繁又需要保持搞高一致性，不要加缓存了。</li></ol><ol><li><p>Redis作为消息队列时如果消息队列满了，怎么办？</p><p>大型的持久类消息队列，kafka。一般满了肯定是前面阻塞了。赶紧报警。</p><p>​</p></li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> redis </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>一个测试工程师走进一家酒吧</title>
      <link href="/2018/03/09/test-engineer/"/>
      <url>/2018/03/09/test-engineer/</url>
      
        <content type="html"><![CDATA[<blockquote><p>一个测试工程师走进一家酒吧，要了一杯啤酒<br>两个测试工程师走进一家酒吧</p></blockquote><p>正常需求。<br><a id="more"></a></p><blockquote><p>一个测试工程师走进一家酒吧，要了一杯咖啡<br>一个测试工程师走进一家酒吧，要了一杯洗脚水<br>一个测试工程师走进一家酒吧，要了一杯蜥蜴<br>一个测试工程师走进一家酒吧，要了一份 asdfQwer@24dg!&amp;*(@<br>一个测试工程师走进一家酒吧，要了一杯烫烫烫的锟斤拷<br>一个测试工程师走进一家酒吧，要了一杯美国啤酒，一杯德国啤酒，一杯比利时啤酒，一杯青岛啤酒（五厂）<br>一个测试工程师走进一家酒吧，要了一杯 Nil，一杯 Null 和一杯 None</p></blockquote><p>数据类型错误。</p><blockquote><p>一个测试工程师走进一家酒吧，要了 0.7 杯啤酒<br>一个测试工程师走进一家酒吧，要了 -1 杯啤酒<br>一个测试工程师走进一家酒吧，要了 232232 杯啤酒<br>一个测试工程师走进一家酒吧，要了 NaN 杯 Null<br>一个测试工程师走进一家酒吧，什么也没要</p></blockquote><p>数字溢出/非法。</p><blockquote><p>一个测试工程师冲进一家酒吧，要了 500 个啤酒咖啡洗脚水野猫狼牙棒奶茶</p></blockquote><p>结合上面两个。</p><blockquote><p>一个测试工程师走进一家酒吧，又走出去又从窗户进来又从后门出去从下水道钻进来</p></blockquote><p>内网漫游。</p><blockquote><p>一个测试工程师走进一</p></blockquote><p>参数错误。</p><blockquote><p>一个测试工程师把酒吧拆了</p></blockquote><p>非法删除。</p><blockquote><p>一个测试工程师化装成老板走进一家酒吧，要了 500 杯啤酒并且不付钱</p></blockquote><p>身份盗用。</p><blockquote><p>一万个测试工程师在酒吧门外呼啸而过</p></blockquote><p>DDoS 攻击。</p><blockquote><p>一个测试工程师走进一家酒吧，要了一杯啤酒’;DROP TABLE 酒吧 #</p></blockquote><p>SQL 注入。</p><blockquote><p>一个测试工程师跳进一家酒吧<br>一个测试工程师蒙着眼睛，倒退着走进一家酒吧</p></blockquote><p>请求方式不正确。</p><blockquote><p>一个体重五百吨的测试工程师走进一家酒吧<br>一个酒量五百吨的测试工程师走进一家酒吧<br>一个酒量为零的测试工程师走进一家酒吧<br>一个测试工程师走进酒吧，另个一师程工走也进吧酒</p></blockquote><p>请求体有误。</p><blockquote><p>一个测试工程师来到一家酒吧门口，拿出电脑，敲了几个命令，232−1232−1 个测试工程师走进一家酒吧</p></blockquote><p>病毒。</p><blockquote><p>一个测试工程师戴着墨镜，手持两把 Uzi 冲进一家酒吧，对着室内一顿扫射，然后要了一杯啤酒</p></blockquote><p>测试……健壮性？</p><blockquote><p>一个名叫 exception 的测试工程师走进一家酒吧，被丢了出来</p></blockquote><p>叫你不用 try-catch。</p><blockquote><p>一个测试工程师盗用老板身份走进了酒吧进了后台放了一瓶自己的酒</p></blockquote><p>文件上传漏洞。</p><blockquote><p>一个测试工程师走进一家酒吧，<script>alert("要了一杯酒");</script></p></blockquote><p>XSS 攻击。</p><blockquote><p>一个测试工测试工程师程师走进一家酒吧</p></blockquote><p>绕过过滤。</p><hr><p><strong>测试工程师：我再进去一趟好不好？</strong></p><p><strong>酒吧老板：滚出去……</strong></p><p>全剧终。</p>]]></content>
      
      
      
        <tags>
            
            <tag> 测试工程师 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>webrtc通信过程</title>
      <link href="/2018/03/06/webrtc-procedure/"/>
      <url>/2018/03/06/webrtc-procedure/</url>
      
        <content type="html"><![CDATA[<h3 id="WebRTC-Api"><a href="#WebRTC-Api" class="headerlink" title="WebRTC Api"></a>WebRTC Api</h3><h5 id="PeerConnection"><a href="#PeerConnection" class="headerlink" title="PeerConnection"></a>PeerConnection</h5><p>WebRTC中最主要的就是一个叫做<code>PeerConnection</code>的对象，这个是WebRTC中已经封装好的对象。每一路的音视频会话都会有唯一的一个<code>PeerConnection</code>对象，WebRTC通过这个<code>PeerConnection</code>对象进行视频的发起、传输、接收和挂断等操作。</p><a id="more"></a><p>PeerConnection中包含的属性如下：</p><ul><li>localDescription：本地描述信息，类型：<code>RTCSessionDescription</code></li><li>remoteDescription：远端描述信息，类型：<code>RTCSessionDescription</code></li><li>onicecandidate：传入一个回调方法，该回调方法有一个返回参数，返回参数类型为：<code>RTCIceCandidateEvent</code>，</li><li>onaddstream：传入一个回调方法，该回调方法有一个返回参数，返回参数类型为：``，如果检测到有远程媒体流传输到本地之后便会调用该方法。</li><li>ondatachannel：（暂未用到）</li><li>oniceconnectionstatechange：（暂未用到）</li><li>onnegotiationneeded：（暂未用到）</li><li>onremovestream：（暂未用到）</li><li>onsignalingstatechange：（暂未用到）</li></ul><p><code>PeerConnection</code>中还包含了一些方法：</p><ul><li>setLocalDescription：设置本地offer，将自己的描述信息加入到<code>PeerConnection</code>中，参数类型：<code>RTCSessionDescription</code></li><li>setRemoteDescription：设置远端的<code>answer</code>，将对方的描述信息加入到<code>PeerConnection</code>中，参数类型：<code>RTCSessionDescription</code></li><li>createOffer：创建一个offer，需要传入两个参数，第一个参数是创建offer成功的回调方法，会返回创建好的offer，可以在这里将这个offer发送出去。第二个参数是创建失败的回调方法，会返回错误信息。</li><li>createAnswer：创建一个<code>answer</code>，需要传入两个参数，第一个参数是创建<code>answer</code>成功的回调方法，会返回创建好的<code>answer</code>，可以在这里将这个<code>answer</code>发送出去。第二个参数是创建失败的回调方法，会返回错误信息。</li><li>addIceCandidate：将打洞服务器加入到配置信息中，参数类型：<code>RTCIceCandidate</code></li><li>addStream：向<code>PeerConnection</code>中加入需要发送的数据流，参数类型：<code>MediaStream</code></li><li>close：</li><li>createDTMFSender：</li><li>createDataChannel：</li><li>getLocalStreams：</li><li>getRemoteStreams：</li><li>getStats：</li><li>getStreamById：</li><li>removeStream：</li><li>updateIce：</li></ul><h5 id="RTCSessionDescription"><a href="#RTCSessionDescription" class="headerlink" title="RTCSessionDescription"></a>RTCSessionDescription</h5><p><code>RTCSessionDescription</code>类型中包含了两个属性：</p><ul><li><p>sdp：a read-only <a href="https://developer.mozilla.org/en-US/docs/Web/API/DOMString" target="_blank" rel="noopener"><code>DOMString</code></a> containing the <a href="https://developer.mozilla.org/en-US/docs/Glossary/SDP" target="_blank" rel="noopener">SDP</a> which describes the session.</p><p>​    </p></li><li><p>type：这个指明了是视频的接收方还是发起方</p><p>​    This enum defines strings that describe the current state of the session description, as used in the <a href="https://developer.mozilla.org/en-US/docs/Web/API/RTCSessionDescription/type" target="_blank" rel="noopener"><code>type</code></a> property. The session description’s type will be specified using one of these values.</p><p>| Value      | Description                              |<br>| ————— | ———————————————————— |<br>| <code>answer</code>   | The SDP contained in the <a href="https://developer.mozilla.org/en-US/docs/Web/API/RTCSessionDescription/sdp" target="_blank" rel="noopener"><code>sdp</code></a> property is the definitive choice in the exchange. In other words, this session description describes the agreed-upon configuration, and is being sent to finalize negotiation. |<br>| <code>offer</code>    | The session description object describes the initial proposal in an offer/answer exchange. The session negotiation process begins with an offer being sent from the caller to the callee. |<br>| <code>pranswer</code> | The session description object describe a provisional answer; that is, it’s a response to a previous offer or provisional answer. |<br>| <code>rollback</code> | This special type with an empty session description is used to roll back to the previous stable state. |</p></li></ul><h3 id="Peer-to-Peer-Connection-通信过程"><a href="#Peer-to-Peer-Connection-通信过程" class="headerlink" title="Peer to Peer Connection 通信过程"></a>Peer to Peer Connection 通信过程</h3><p>用户A合用户B建立连接的过程（websocket通信）</p><ol><li><p>新建websocket的连接</p><pre><code class="lang-javascript">var conn = new WebSocket(&#39;ws://localhost:9090&#39;);</code></pre></li><li><p>创建iceserver</p><pre><code class="lang-javascript">var configuration = {                &quot;iceServers&quot;: [{ &quot;urls&quot;: &quot;stun:stun2.1.google.com:19302&quot; }]            };</code></pre></li><li><p>A获取UserMedia</p><p>A创建PeerConnection</p><p>向本地视频中加入当前视频流stream</p><p>向PeerConnection中加入该stream</p><pre><code class="lang-javascript">      navigator.getUserMedia = (            navigator.getUserMedia ||            navigator.webkitGetUserMedia ||            navigator.mozGetUserMedia ||            navigator.msGetUserMedia        );    navigator.getUserMedia({ audio: false, video: true }, function(stream) {            //displaying local video stream on the page             localVideo.srcObject = stream;            // setup a new PeerConnection            yourConn = new RTCPeerConnection(configuration);            // setup stream listening             yourConn.addStream(stream);        }, function(error) {            console.log(error);        });</code></pre></li><li><p>A创建一个offer，把offer加入LocalDescription中</p><pre><code class="lang-javascript">// create an offer         yourConn.createOffer(function(offer) {            send({                type: &quot;offer&quot;,                offer: offer            });            yourConn.setLocalDescription(offer);        }</code></pre></li><li><p>A监听iceCandicate服务器信息，收到event后把其中的candicate发送出去</p><pre><code class="lang-javascript">// 发送ICE候选到其他客户端            yourConn.onicecandidate = function(event) {                if (event.candidate) {                    send({                        type: &quot;candidate&quot;,                        candidate: event.candidate                    });                }            };</code></pre></li><li><p>websocket的connection监听offer，</p><p>如果B收到offer，</p><p>B将offer存入peerConnection中setRemoteDescription, 并创建一个answer并返回给A：</p><pre><code class="lang-javascript">//when we got a message from a signaling server conn.onmessage = function(msg) {    console.log(&quot;Got message&quot;, msg.data);    var data = JSON.parse(msg.data);    switch (data.type) {        case &quot;login&quot;:            handleLogin(data.success);            break;            //when somebody wants to call us         case &quot;offer&quot;:            handleOffer(data.offer, data.name);            break;        case &quot;answer&quot;:            handleAnswer(data.answer);            break;            //when a remote peer sends an ice candidate to us         case &quot;candidate&quot;:            handleCandidate(data.candidate);            break;        case &quot;leave&quot;:            handleLeave();            break;        default:            break;    }};//when somebody sends us an offer function handleOffer(offer, name) {    connectedUser = name;      //offer存入peerConnection中,    yourConn.setRemoteDescription(new RTCSessionDescription(offer));    //create an answer to an offer     yourConn.createAnswer(function(answer) {        yourConn.setLocalDescription(answer);        send({            type: &quot;answer&quot;,            answer: answer        });    }, function(error) {        alert(&quot;Error when creating an answer&quot;);    });};</code></pre></li><li><p>B收到了A发来的iceCandicate，将IceCandidate加入连接中</p><pre><code class="lang-javascript">//when we got an ice candidate from a remote user function handleCandidate(candidate) {    yourConn.addIceCandidate(new RTCIceCandidate(candidate));};</code></pre></li><li><p>B开始获取视音频数据，将视音频数据存入<code>PeerConnection</code>中，WebRTC便会自动将视音频数据发送给A。</p><pre><code class="lang-javascript">            //这里在连接建立的时候就提前设置好远程连接的监听，一旦B把A添加            //when a remote user adds stream to the peer connection, we display it             yourConn.onaddstream = function(e) {                remoteVideo.srcObject = e.stream;            };</code></pre></li><li><p>A接收到B返回的<code>answer</code>，将B返回的<code>answer</code>设置为<code>PeerConnection</code>的<code>remoteDescription</code>。</p><pre><code class="lang-javascript">//when we got an answer from a remote userfunction handleAnswer(answer) {    yourConn.setRemoteDescription(new RTCSessionDescription(answer));};</code></pre></li><li><p>这个时候WebRTC会将视音频数据自动发送给B，A和B就建立起了实时视音频通信。</p></li></ol><h3 id="全部代码"><a href="#全部代码" class="headerlink" title="全部代码"></a>全部代码</h3><pre><code class="lang-js">//our username var name;var connectedUser;//connecting to our signaling servervar conn = new WebSocket(&#39;ws://localhost:9090&#39;);conn.onopen = function() {    console.log(&quot;Connected to the signaling server&quot;);};//when we got a message from a signaling server conn.onmessage = function(msg) {    console.log(&quot;Got message&quot;, msg.data);    var data = JSON.parse(msg.data);    switch (data.type) {        case &quot;login&quot;:            handleLogin(data.success);            break;            //when somebody wants to call us         case &quot;offer&quot;:            handleOffer(data.offer, data.name);            break;        case &quot;answer&quot;:            handleAnswer(data.answer);            break;            //when a remote peer sends an ice candidate to us         case &quot;candidate&quot;:            handleCandidate(data.candidate);            break;        case &quot;leave&quot;:            handleLeave();            break;        default:            break;    }};conn.onerror = function(err) {    console.log(&quot;Got error&quot;, err);};//alias for sending JSON encoded messages function send(message) {    //attach the other peer username to our messages     if (connectedUser) {        message.name = connectedUser;    }    conn.send(JSON.stringify(message));};//****** //UI selectors block //******var loginPage = document.querySelector(&#39;#loginPage&#39;);var usernameInput = document.querySelector(&#39;#usernameInput&#39;);var loginBtn = document.querySelector(&#39;#loginBtn&#39;);var callPage = document.querySelector(&#39;#callPage&#39;);var callToUsernameInput = document.querySelector(&#39;#callToUsernameInput&#39;);var callBtn = document.querySelector(&#39;#callBtn&#39;);var hangUpBtn = document.querySelector(&#39;#hangUpBtn&#39;);var localVideo = document.querySelector(&#39;#localVideo&#39;);var remoteVideo = document.querySelector(&#39;#remoteVideo&#39;);var yourConn;var stream;callPage.style.display = &quot;none&quot;;// Login when the user clicks the button loginBtn.addEventListener(&quot;click&quot;, function(event) {    name = usernameInput.value;    if (name.length &gt; 0) {        send({            type: &quot;login&quot;,            name: name        });    }});function handleLogin(success) {    if (success === false) {        alert(&quot;Ooops...try a different username&quot;);    } else {        loginPage.style.display = &quot;none&quot;;        callPage.style.display = &quot;block&quot;;        //**********************         //Starting a peer connection         //**********************         //getting local video stream         navigator.getUserMedia = (            navigator.getUserMedia ||            navigator.webkitGetUserMedia ||            navigator.mozGetUserMedia ||            navigator.msGetUserMedia        );        navigator.getUserMedia({ audio: false, video: true }, function(myStream) {            stream = myStream;            //displaying local video stream on the page             localVideo.srcObject = stream;            //using Google public stun server             var configuration = {                &quot;iceServers&quot;: [{ &quot;urls&quot;: &quot;stun:stun2.1.google.com:19302&quot; }]            };            yourConn = new RTCPeerConnection(configuration);            // setup stream listening             yourConn.addStream(stream);            //when a remote user adds stream to the peer connection, we display it             yourConn.onaddstream = function(e) {                remoteVideo.srcObject = e.stream;            };            // Setup ice handling ?????????            yourConn.onicecandidate = function(event) {                if (event.candidate) {                    send({                        type: &quot;candidate&quot;,                        candidate: event.candidate                    });                }            };        }, function(error) {            console.log(error);        });    }};//initiating a call callBtn.addEventListener(&quot;click&quot;, function() {    var callToUsername = callToUsernameInput.value;    if (callToUsername.length &gt; 0) {        connectedUser = callToUsername;        // create an offer         yourConn.createOffer(function(offer) {            send({                type: &quot;offer&quot;,                offer: offer            });            yourConn.setLocalDescription(offer);        }, function(error) {            alert(&quot;Error when creating an offer&quot;);        });    }});//when somebody sends us an offer function handleOffer(offer, name) {    connectedUser = name;    yourConn.setRemoteDescription(new RTCSessionDescription(offer));    //create an answer to an offer     yourConn.createAnswer(function(answer) {        yourConn.setLocalDescription(answer);        send({            type: &quot;answer&quot;,            answer: answer        });    }, function(error) {        alert(&quot;Error when creating an answer&quot;);    });};//when we got an answer from a remote userfunction handleAnswer(answer) {    yourConn.setRemoteDescription(new RTCSessionDescription(answer));};//when we got an ice candidate from a remote user function handleCandidate(candidate) {    yourConn.addIceCandidate(new RTCIceCandidate(candidate));};//hang up hangUpBtn.addEventListener(&quot;click&quot;, function() {    send({        type: &quot;leave&quot;    });    handleLeave();});function handleLeave() {    connectedUser = null;    remoteVideo.src = null;    yourConn.close();    yourConn.onicecandidate = null;    yourConn.onaddstream = null;};</code></pre><h3 id="websocket代码："><a href="#websocket代码：" class="headerlink" title="websocket代码："></a>websocket代码：</h3><pre><code class="lang-js">//require our websocket library var WebSocketServer = require(&#39;ws&#39;).Server; //creating a websocket server at port 9090 var wss = new WebSocketServer({port: 9090}); //all connected to the server users var users = {};//when a user connects to our sever wss.on(&#39;connection&#39;, function(connection) {   console.log(&quot;User connected&quot;);   //when server gets a message from a connected user    connection.on(&#39;message&#39;, function(message) {       var data;       //accepting only JSON messages       try {          data = JSON.parse(message);       } catch (e) {          console.log(&quot;Invalid JSON&quot;);          data = {};       }      //switching type of the user message       switch (data.type) {          //when a user tries to login         case &quot;login&quot;:             console.log(&quot;User logged&quot;, data.name);             //if anyone is logged in with this username then refuse             if(users[data.name]) {                sendTo(connection, {                   type: &quot;login&quot;,                   success: false                });             } else {                //save user connection on the server                users[data.name] = connection;                connection.name = data.name;                sendTo(connection, {                   type: &quot;login&quot;,                   success: true                });             }             break;         case &quot;offer&quot;:             //for ex. UserA wants to call UserB             console.log(&quot;Sending offer to: &quot;, data.name);            //if UserB exists then send him offer details             var conn = users[data.name];             if(conn != null) {                //setting that UserA connected with UserB                connection.otherName = data.name;                 //发给那个users[data.name]就是用户B，的conn，消息是用户A要给你发offer，里面带的name是用户A的名字                   sendTo(conn, {                   type: &quot;offer&quot;,                   offer: data.offer,                   name: connection.name                });             }            break;         case &quot;answer&quot;:             console.log(&quot;Sending answer to: &quot;, data.name);             //for ex. UserB answers UserA             var conn = users[data.name];             //这个conn的B消息里发的data.name也就是A的名字，发给a一个answer            if(conn != null) {                connection.otherName = data.name;                sendTo(conn, {                   type: &quot;answer&quot;,                   answer: data.answer                });             }             break;          case &quot;candidate&quot;:             console.log(&quot;Sending candidate to:&quot;,data.name);            //同样，这是A发给B的candidate             var conn = users[data.name];            if(conn != null) {                sendTo(conn, {                   type: &quot;candidate&quot;,                   candidate: data.candidate                });             }             break;         case &quot;leave&quot;:             console.log(&quot;Disconnecting from&quot;, data.name);             var conn = users[data.name];             conn.otherName = null;             //notify the other user so he can disconnect his peer connection             if(conn != null) {               sendTo(conn, {                   type: &quot;leave&quot;               });             }            break;         default:             sendTo(connection, {                type: &quot;error&quot;,                message: &quot;Command not found: &quot; + data.type             });             break;       }   });    //when user exits, for example closes a browser window    //this may help if we are still in &quot;offer&quot;,&quot;answer&quot; or &quot;candidate&quot; state    connection.on(&quot;close&quot;, function() {       if(connection.name) {          delete users[connection.name];          if(connection.otherName) {             console.log(&quot;Disconnecting from &quot;, connection.otherName);             var conn = users[connection.otherName];             conn.otherName = null;            if(conn != null) {                sendTo(conn, {                   type: &quot;leave&quot;                });             }         }       }   });     connection.send(&quot;Hello world&quot;);  });function sendTo(connection, message) {    connection.send(JSON.stringify(message)); }</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> webrtc </tag>
            
            <tag> 前端 </tag>
            
            <tag> node.js </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>二叉查找树的删除操作</title>
      <link href="/2018/02/15/bst-delete/"/>
      <url>/2018/02/15/bst-delete/</url>
      
        <content type="html"><![CDATA[<p>文章介绍了二叉查找树如何删除。</p><a id="more"></a><h2 id="二叉查找树的删除操作"><a href="#二叉查找树的删除操作" class="headerlink" title="二叉查找树的删除操作"></a>二叉查找树的删除操作</h2><p>对于二叉查找树的删除操作（这里根据值删除，而非结点）分三种情况：<br>不过在此之前，我们应该确保根据给定的值找到了要删除的结点，如若没找到该结点<br>不会执行删除操作！<br>下面三种情况假设已经找到了要删除的结点。</p><ol><li>如果结点为叶子结点（没有左、右子树），此时删除该结点不会玻化树的结构<br>直接删除即可，并修改其父结点指向它的引用为null.如下图:<br> <img data-src="http://img.blog.csdn.net/20130506115644277" alt="img"></li><li>如果其结点只包含左子树，或者右子树的话，此时直接删除该结点，并将其左子树<br>或者右子树设置为其父结点的左子树或者右子树即可，此操作不会破坏树结构。<br>  <img data-src="http://img.blog.csdn.net/20130506115712159" alt="img"></li><li>当结点的左右子树都不空的时候，一般的删除策略是用其右子树的最小数据<br>（容易找到）代替要删除的结点数据并递归删除该结点（此时为null），因为<br>右子树的最小结点不可能有左孩子，所以第二次删除较为容易。<br>z的左子树和右子树均不空。找到z的后继y，因为y一定没有左子树，所以可以删除y，<br>并让y的父亲节点成为y的右子树的父亲节点，并用y的值代替z的值.如图：<br>  <img data-src="http://img.blog.csdn.net/20130506120027521" alt="img"></li></ol><pre><code class="lang-java"> /**     * 二叉查找树的删除     */    public BinaryNode&lt;T&gt; remove(BinaryNode&lt;T&gt; node, T data) {        if (node == null) {            return node;        }        int res = data.compareTo(node.data);        if (res == 0) {            if (node.leftNode != null &amp;&amp; node.rightNode != null) {                node.data = findMin(node.rightNode);                node.rightNode = remove(node.rightNode, node.data);            }            // 如果左子树不为空，则返回左子树；否则返回右子树，右子树为 null，也没问题            node = node.leftNode != null ? node.leftNode : node.rightNode;        }        if (res &gt; 0) {            node.rightNode = remove(node.rightNode, data);        } else {            node.leftNode = remove(node.leftNode, data);        }        return node;    }</code></pre>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> 数据结构和算法 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>内部类用法</title>
      <link href="/2018/02/08/inner-outer-class/"/>
      <url>/2018/02/08/inner-outer-class/</url>
      
        <content type="html"><![CDATA[<p>内部类又称为嵌套类，可以把内部类理解为外部类的一个普通成员。<br><a id="more"></a></p><h3 id="1-内部类的分类"><a href="#1-内部类的分类" class="headerlink" title="1. 内部类的分类"></a>1. 内部类的分类</h3><h4 id="1-1-成员内部类"><a href="#1-1-成员内部类" class="headerlink" title="1.1 成员内部类"></a>1.1 成员内部类</h4><p>​    成员内部类可以无条件访问外部类的所有成员属性和成员方法（包括private成员和静态成员）。、</p><p>　　不过要注意的是，当成员内部类拥有和外部类同名的成员变量或者方法时，会发生隐藏现象，即默认情况下访问的是成员内部类的成员。如果要访问外部类的同名成员，需要以下面的形式进行访问：</p><p><code>外部类.this.成员变量</code></p><p><code>外部类.this.成员方法</code></p><h4 id="1-2-局部内部类"><a href="#1-2-局部内部类" class="headerlink" title="1.2 局部内部类"></a>1.2 局部内部类</h4><p>　　局部内部类是定义在一个方法或者一个作用域里面的类，它和成员内部类的区别在于局部内部类的访问仅限于方法内或者该作用域内。</p><pre><code class="lang-java">class People{    public People() {    }}class Man{    public Man(){    }    public People getWoman(){        class Woman extends People{   //局部内部类            int age =0;        }        return new Woman();    }}</code></pre><p>　　注意，局部内部类就像是方法里面的一个局部变量一样，是不能有public、protected、private以及static修饰符的。</p><h4 id="1-3-匿名内部类"><a href="#1-3-匿名内部类" class="headerlink" title="1.3 匿名内部类"></a>1.3 匿名内部类</h4><p>　　匿名内部类应该是平时我们编写代码时用得最多的，在编写事件监听的代码时使用匿名内部类不但方便，而且使代码更加容易维护。下面这段代码是一段Android事件监听代码：</p><h4 id="1-4-静态内部类"><a href="#1-4-静态内部类" class="headerlink" title="1.4 静态内部类"></a>1.4 静态内部类</h4><p>　　静态内部类也是定义在另一个类里面的类，只不过在类的前面多了一个关键字static。静态内部类是不需要依赖于外部类的，这点和类的静态成员属性有点类似，并且它不能使用外部类的非static成员变量或者方法，这点很好理解，因为在没有外部类的对象的情况下，可以创建静态内部类的对象，如果允许访问外部类的非static成员就会产生矛盾，因为外部类的非static成员必须依附于具体的对象。</p><hr><h3 id="2-内部类的使用场景和好处"><a href="#2-内部类的使用场景和好处" class="headerlink" title="2.内部类的使用场景和好处"></a>2.内部类的使用场景和好处</h3><p>为什么在Java中需要内部类？总结一下主要有以下四点：</p><ol><li>每个内部类都能独立的继承一个接口的实现，所以无论外部类是否已经继承了某个(接口的)实现，对于内部类都没有影响。内部类使得多继承的解决方案变得完整，</li><li>方便将存在一定逻辑关系的类组织在一起，又可以对外界隐藏。</li></ol><ol><li>方便编写事件驱动程序</li></ol><ol><li>方便编写线程代码</li></ol><p>　个人觉得第一点是最重要的原因之一，内部类的存在使得Java的多继承机制变得更加完善。</p><hr><h3 id="3-内部类访问外部类"><a href="#3-内部类访问外部类" class="headerlink" title="3.内部类访问外部类"></a>3.内部类访问外部类</h3><p>里面的可以自由访问外面的，规则和static一样。（访问非静态时必须先创建对象）</p><p>具体如下：</p><h4 id="3-1-非静态内部类的非静态方法"><a href="#3-1-非静态内部类的非静态方法" class="headerlink" title="3.1 非静态内部类的非静态方法"></a>3.1 非静态内部类的非静态方法</h4><p>直接访问</p><p>　　不过要注意的是，当成员内部类拥有和外部类同名的成员变量或者方法时，会发生隐藏现象，即默认情况下访问的是成员内部类的成员。如果要访问外部类的同名成员，需要以下面的形式进行访问：</p><p><code>外部类.this.成员变量</code></p><p><code>外部类.this.成员方法</code></p><pre><code class="lang-java">public class Outter {      int i = 5;      static String string = &quot;Hello&quot;;      class Inner1{          void Test1 (){              System.out.println(i);              System.out.println(string);          }      }  }</code></pre><h4 id="3-2-静态内部类的非静态方法"><a href="#3-2-静态内部类的非静态方法" class="headerlink" title="3.2. 静态内部类的非静态方法"></a>3.2. 静态内部类的非静态方法</h4><p>因为静态方法访问非静态外部成员需先创建实例，所以访问i时必须先new外部类。</p><blockquote><p> 因为静态的内部类可以脱离外部类创建，不用先创建外部类实例，这样要访问外部类的静态数据还好，毕竟外部类的静态数据也不需要先创建外部类实例，但是访问外部类的非静态变量或者方法的时候就需要先创建外部类实例。</p></blockquote><pre><code class="lang-java">public class Outter {      int i = 5;      static String string = &quot;Hello&quot;;      static class Inner2{          void Test1 (){              System.out.println(new Outter().i);              System.out.println(string);          }      }  }</code></pre><h4 id="3-3-静态内部类的静态方法"><a href="#3-3-静态内部类的静态方法" class="headerlink" title="3.3 静态内部类的静态方法"></a>3.3 静态内部类的静态方法</h4><p>规则如上</p><pre><code class="lang-java">public class Outter {      int i = 5;      static String string = &quot;Hello&quot;;      static class Inner2{          static void Test2 (){              System.out.println(new Outter().i);              System.out.println(string);          }      }  }</code></pre><p>但是static方法只能访问static变量，假如在Inner2里有一个非static属性int i = 16，直接在test2里访问会产生编译报错。</p><pre><code class="lang-java">public class Main {    int i = 5;    static String string = &quot;Hello&quot;;    static class Inner2{        int i = 16;        static void Test2 (){            System.out.println(new Main().i);            System.out.println(string);            // System.out.println(i); 这里报错！！！        }    }}</code></pre><h3 id="4-外部类访问内部类"><a href="#4-外部类访问内部类" class="headerlink" title="4.外部类访问内部类"></a>4.外部类访问内部类</h3><p>大方向：因为将内部类理解为外部类的一个普通成员，所以外面的访问里面的需先new一个对象。</p><h4 id="4-1非静态方法访问非静态内部类的成员："><a href="#4-1非静态方法访问非静态内部类的成员：" class="headerlink" title="4.1非静态方法访问非静态内部类的成员："></a>4.1非静态方法访问非静态内部类的成员：</h4><p><strong>new 内部类对象，然后直接访问</strong></p><pre><code class="lang-java">public class Outter {      void Test1(){          System.out.println(new Inner1().i);      }      class Inner1{          int i = 5;  //      static String string = &quot;Hello&quot;;  定义错误！  因为非静态的内部类成员不能创建静态变量，不然静态变量可以独立访问，而内部类又依赖外部类存在这样就矛盾了    }  }</code></pre><h4 id="4-2-静态方法访问非静态内部类的成员"><a href="#4-2-静态方法访问非静态内部类的成员" class="headerlink" title="4.2 静态方法访问非静态内部类的成员"></a>4.2 静态方法访问非静态内部类的成员</h4><p><strong>静态方法Test2访问非静态Inner1需先new外部类；</strong></p><p><strong>继续访问非静态成员i需先new 内部类</strong></p><p>所以访问规则为：new Outter().new Inner1().i。</p><p>毕竟你都静态方法了，可以独立存在的，那访问内部类的时候万一外部类不存在不就掉不到了嘛，所以先创建个外部类</p><pre><code class="lang-java">public class Outter {      static void Test2(){          System.out.println(new Outter().new Inner1().i);      }      class Inner1{          int i = 5;  //      static String string = &quot;Hello&quot;;  定义错误！      }  }</code></pre><p>反正要记得，访问static修饰的内部类就像调用static修饰的方法一样，就是内部类里还有属性或者对象，访问静态就可以通过名字一路到底，非静态就需要创建相应的实例。</p><h3 id="5-匿名内部类"><a href="#5-匿名内部类" class="headerlink" title="5.匿名内部类"></a>5.匿名内部类</h3><p>匿名内部类访问外部成员变量时，成员变量前应加final关键字。</p><pre><code class="lang-java">final int k = 6;  ((Button)findViewById(R.id.button2)).setOnClickListener(new OnClickListener() {      @Override      public void onClick(View v) {          // TODO Auto-generated method stub          System.out.println(k);      }  });</code></pre><p>原因是，内部类编译的时候会把外部类拷贝一份作为内部类的变量独立使用，为了保证数据一致性，防止内部类线程修改了外部变量，所以直接设置成final，一旦初始化不可修改。</p>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> 内部类 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Java HashMap 源码学习笔记</title>
      <link href="/2018/02/07/java-hashmap/"/>
      <url>/2018/02/07/java-hashmap/</url>
      
        <content type="html"><![CDATA[<h2 id="Java-HashMap-源码学习笔记"><a href="#Java-HashMap-源码学习笔记" class="headerlink" title="Java HashMap 源码学习笔记"></a>Java HashMap 源码学习笔记</h2><a id="more"></a><p>map子类中的hashmap，源码学习记录。部分是翻译还有一些是自己的记录。</p><p>写起来才发现好坑啊，网上很多是1.7的，我看的是1.8的，没想到1.7根1.8差的还挺多的。1.7源码都没看过的我…🙁</p><h3 id="1-常量初始化"><a href="#1-常量初始化" class="headerlink" title="1.常量初始化"></a>1.常量初始化</h3><p>The default initial capacity - MUST be a power of two.\</p><p>最小初始容量，必须是2的指数次幂</p><pre><code class="lang-java">     static final int DEFAULT_INITIAL_CAPACITY = 1 &lt;&lt; 4; // aka 16</code></pre><p>The maximum capacity, used if a higher value is implicitly specified by either of the constructors with arguments. MUST be a power of two &lt;= 1&lt;&lt;30.</p><p>最大容量，必须是2的指数次幂，<strong>为什么呢？</strong>见hash函数</p><pre><code class="lang-java">     static final int MAXIMUM_CAPACITY = 1 &lt;&lt; 30;</code></pre><p>The load factor used when none specified in constructor.</p><p>负载平衡，是时间复杂度和空间复杂度的平衡，oracle官方给出的是0.75是最佳。过高的因子会降低存储空间但是查找（lookup，包括HashMap中的put与get方法）的时间就会增加</p><pre><code class="lang-java">     static final float DEFAULT_LOAD_FACTOR = 0.75f;</code></pre><p>The bin count threshold for using a tree rather than list for a bin. Bins are converted to trees when adding an element to a bin with at least this many nodes. The value must be greater than 2 and should be at least 8 to mesh with assumptions in tree removal about conversion back to plain bins upon shrinkage.</p><p>当节点个数&gt;= TREEIFY_THRESHOLD - 1时，HashMap将采用红黑树存储。为什么这么做呢？当发生Hash冲突时，HashMap首先是采用链表将重复的值串起来，并将最后放入的值置于链首，java8对HashMap进行了优化。当节点个数多了之后使用红黑树存储。这样做的好处是，最坏的情况下即所有的key都Hash冲突，采用链表的话查找时间为O(n),而采用红黑树为O(logn)，这也是Java8中HashMap性能提升的奥秘</p><pre><code class="lang-java">     static final int TREEIFY_THRESHOLD = 8;</code></pre><p>The bin count threshold for untreeifying a (split) bin during a resize operation. Should be less than TREEIFY_THRESHOLD, and at most 6 to mesh with shrinkage detection under removal.</p><pre><code class="lang-java">     static final int UNTREEIFY_THRESHOLD = 6;</code></pre><p>The smallest table capacity for which bins may be treeified. (Otherwise the table is resized if too many nodes in a bin.) Should be at least 4 * TREEIFY_THRESHOLD to avoid conflicts between resizing and treeification thresholds.</p><p>重新分配大小</p><pre><code class="lang-java">     static final int MIN_TREEIFY_CAPACITY = 64;</code></pre><p>HashMap性能的有两个参数：初始容量(initialCapacity) 和加载因子(loadFactor)。容量 是哈希表中桶的数量，初始容量只是哈希表在创建时的容量。加载因子 是哈希表在其容量自动增加之前可以达到多满的一种尺度。当哈希表中的条目数超出了加载因子与当前容量的乘积时，则要对该哈希表进行 rehash 操作（即重建内部数据结构），从而哈希表将具有大约两倍的桶数。</p><h3 id="2-Hash函数"><a href="#2-Hash函数" class="headerlink" title="2.Hash函数"></a>2.Hash函数</h3><p>Computes key.hashCode() and spreads (XORs) higher bits of hash to lower. Because the table uses power-of-two masking, sets of hashes that vary only in bits above the current mask will always collide. (Among known examples are sets of Float keys holding consecutive whole numbers in small tables.) So we apply a transform that spreads the impact of higher bits downward. There is a tradeoff between speed, utility, and quality of bit-spreading. Because many common sets of hashes are already reasonably distributed (so don’t benefit from spreading), and because we use trees to handle large sets of collisions in bins, we just XOR some shifted bits in the cheapest possible way to reduce systematic lossage, as well as to incorporate impact of the highest bits that would otherwise never be used in index calculations because of table bounds.</p><pre><code class="lang-java">static final int hash(Object key) {   //jdk1.8 &amp; jdk1.7     int h;     // h = key.hashCode() 为第一步 取hashCode值     // h ^ (h &gt;&gt;&gt; 16)  为第二步 高位参与运算     return (key == null) ? 0 : (h = key.hashCode()) ^ (h &gt;&gt;&gt; 16);}static int indexFor(int h, int length) {  //jdk1.7的源码，jdk1.8没有这个方法，但是实现原理一样的     return h &amp; (length-1);  //第三步 取模运算}</code></pre><p>在哈希表容量（也就是buckets或slots大小）为length的情况下，为了使每个key都能在冲突最小的情况下映射到<code>[0,length)</code>（注意是左闭右开区间）的索引（index）内，一般有两种做法：</p><ol><li>让length为素数，然后用<code>hashCode(key) mod length</code>的方法得到索引</li><li>让length为2的指数倍，然后用<code>hashCode(key) &amp; (length-1)</code>的方法得到索引</li></ol><p><a href="http://docs.oracle.com/javase/7/docs/api/index.html?java/util/Hashtable.html" target="_blank" rel="noopener">HashTable</a>用的是方法1，<code>HashMap</code>用的是方法2。</p><p>下图，n为哈希列表长度</p><p><img data-src="https://ws2.sinaimg.cn/large/006tKfTcgy1fo6yr3pfc8j30xa0j0q5u.jpg" alt></p><p>也就是高16位与低16位XOR（其实是原值和高16位右移16位后的值XOR）后再和 length-1 &amp;运算，length是2 的指数次幂，最小是16，也就是说length-1的末4为全1，32就是末8位全1，以此类推。</p><p>为什么要这么做呢？</p><blockquote><p>如果<code>hashCode(key)</code>的大于<code>length</code>的值，而且<code>hashCode(key)</code>的二进制位的低位变化不大，那么冲突就会很多，举个例子：</p><p> Java中对象的哈希值都32位整数，而HashMap默认大小为16，那么有两个对象那么的哈希值分别为：<code>0xABAB0000</code>与<code>0xBABA0000</code>，它们的后几位都是一样，那么与16异或后得到结果应该也是一样的，也就是产生了冲突。</p><p>造成冲突的原因关键在于16限制了只能用低位来计算，高位直接舍弃了，所以我们需要额外的哈希函数而不只是简单的对象的<code>hashCode</code>方法了。<br> 具体来说，就是HashMap中<code>hash</code>函数干的事了.</p></blockquote><h3 id="3-List式的bucket-Node"><a href="#3-List式的bucket-Node" class="headerlink" title="3. List式的bucket Node"></a>3. List式的bucket Node</h3><pre><code class="lang-java">/**     * Basic hash bin node, used for most entries.  (See below for     * TreeNode subclass, and in LinkedHashMap for its Entry subclass.)     */    static class Node&lt;K,V&gt; implements Map.Entry&lt;K,V&gt; {        final int hash;        final K key;        V value;        Node&lt;K,V&gt; next;        Node(int hash, K key, V value, Node&lt;K,V&gt; next) {            this.hash = hash;            this.key = key;            this.value = value;            this.next = next;        }        public final K getKey()        { return key; }        public final V getValue()      { return value; }        public final String toString() { return key + &quot;=&quot; + value; }        public final int hashCode() {            return Objects.hashCode(key) ^ Objects.hashCode(value);        }        public final V setValue(V newValue) {            V oldValue = value;            value = newValue;            return oldValue;        }        public final boolean equals(Object o) {            if (o == this)                return true;            if (o instanceof Map.Entry) {                Map.Entry&lt;?,?&gt; e = (Map.Entry&lt;?,?&gt;)o;                if (Objects.equals(key, e.getKey()) &amp;&amp;                    Objects.equals(value, e.getValue()))                    return true;            }            return false;        }    }</code></pre><p>bucket是一个list，每个元素是一个节点，用于开散列法解决冲突（如果冲突就在这个节点后面链式存储）</p><p>所以每个元素有一个next节点，也是一个Node类。</p><h3 id="4-get"><a href="#4-get" class="headerlink" title="4.get"></a>4.get</h3><pre><code class="lang-java">    /**     * Returns the value to which the specified key is mapped,     * or {@code null} if this map contains no mapping for the key.     *     * &lt;p&gt;More formally, if this map contains a mapping from a key     * k to a value v such that  (key==null ? k==null :     * key.equals(k)), then this method returns v; otherwise     * it returns null.  (There can be at most one such mapping.)     *     * &lt;p&gt;A return value of null does not &lt;i&gt;necessarily&lt;/i&gt;     * indicate that the map contains no mapping for the key; it&#39;s also     * possible that the map explicitly maps the key to null.     * The {@link #containsKey containsKey} operation may be used to     * distinguish these two cases.     * 这段话意思就是说有的key是空的，但是不代表这个map没有建立关联的关系     * @see #put(Object, Object)     */    public V get(Object key) {        Node&lt;K,V&gt; e;        return (e = getNode(hash(key), key)) == null ? null : e.value;    }    /**    * The table, initialized on first use, and resized as necessary. When allocated,         length is always a power of two. (We also tolerate length zero in some operations to      allow bootstrapping mechanics that are currently not needed.)    */    transient Node&lt;K,V&gt;[] table;    /**     * Implements Map.get and related methods     *     * @param hash hash for key 哈希值     * @param key the key 原值     * @return the node, or null if none     */    final Node&lt;K,V&gt; getNode(int hash, Object key) {        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; int n; K k;        if ((tab = table) != null &amp;&amp; (n = tab.length) &gt; 0 &amp;&amp;            (first = tab[(n - 1) &amp; hash]) != null) {    //数组非空，把数组赋给tab，长度大于0，而且头结点(用哈希值和数组长度异或)非空            if (first.hash == hash &amp;&amp; // always check first node                ((k = first.key) == key || (key != null &amp;&amp; key.equals(k))))                return first;        //看first的key是否等于传入的key            if ((e = first.next) != null) {                if (first instanceof TreeNode)        //如果是红黑树方式存储的                    return ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);    //调用红黑树寻找treenode                do {                //链式存储的话链式寻找                    if (e.hash == hash &amp;&amp;                        ((k = e.key) == key || (key != null &amp;&amp; key.equals(k))))                        return e;                } while ((e = e.next) != null);            }        }        //找不到或者长度为空什么的返回空        return null;    }</code></pre><h3 id="5-put"><a href="#5-put" class="headerlink" title="5.put"></a>5.put</h3><p><img data-src="https://ws4.sinaimg.cn/large/006tKfTcgy1fo7ooddqq6j31bo11s7cj.jpg" alt="hashMap put方法执行流程图"></p><p>①.判断键值对数组table[i]是否为空或为null，否则执行resize()进行扩容；</p><p>②.根据键值key计算hash值得到插入的数组索引i，如果table[i]==null，直接新建节点添加，转向⑥，如果table[i]不为空，转向③；</p><p>③.判断table[i]的首个元素是否和key一样，如果相同直接覆盖value，否则转向④，这里的相同指的是hashCode以及equals；</p><p>④.判断table[i] 是否为treeNode，即table[i] 是否是红黑树，如果是红黑树，则直接在树中插入键值对，否则转向⑤；</p><p>⑤.遍历table[i]，判断链表长度是否大于8，大于8的话把链表转换为红黑树，在红黑树中执行插入操作，否则进行链表的插入操作；遍历过程中若发现key已经存在直接覆盖value即可；</p><p>⑥.插入成功后，判断实际存在的键值对数量size是否超多了最大容量threshold，如果超过，进行扩容。</p><pre><code class="lang-java">/**     * Associates the specified value with the specified key in this map.     * If the map previously contained a mapping for the key, the old     * value is replaced.     *     * @param key key with which the specified value is to be associated     * @param value value to be associated with the specified key     * @return the previous value associated with &lt;tt&gt;key&lt;/tt&gt;, or     *         &lt;tt&gt;null&lt;/tt&gt; if there was no mapping for &lt;tt&gt;key&lt;/tt&gt;.     *         (A &lt;tt&gt;null&lt;/tt&gt; return can also indicate that the map     *         previously associated &lt;tt&gt;null&lt;/tt&gt; with &lt;tt&gt;key&lt;/tt&gt;.)     */    public V put(K key, V value) {        return putVal(hash(key), key, value, false, true);    }    /**     * Implements Map.put and related methods     *     * @param hash hash for key     * @param key the key     * @param value the value to put     * @param onlyIfAbsent if true, don&#39;t change existing value     * @param evict if false, the table is in creation mode.     * @return previous value, or null if none     */    final V putVal(int hash, K key, V value, boolean onlyIfAbsent,                   boolean evict) {        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; int n, i;        if ((tab = table) == null || (n = tab.length) == 0)            n = (tab = resize()).length;    //如果tab为空，先创建tab        if ((p = tab[i = (n - 1) &amp; hash]) == null)        //如果tab中当前hash值不存在先创建该节点            tab[i] = newNode(hash, key, value, null);        else {            Node&lt;K,V&gt; e; K k;            if (p.hash == hash &amp;&amp;                ((k = p.key) == key || (key != null &amp;&amp; key.equals(k))))        //对于链表的头节点或者树根，如果该key存在，直接覆盖value                e = p;            else if (p instanceof TreeNode)        //如果p是红黑树节点，用红黑树的添加方式添加                e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(this, tab, hash, key, value);            else {        //链表，顺序添加                for (int binCount = 0; ; ++binCount) {                    if ((e = p.next) == null) {                        p.next = newNode(hash, key, value, null);                        if (binCount &gt;= TREEIFY_THRESHOLD - 1) // -1 for 1st //链表长度大于8转换为红黑树进行处理                            treeifyBin(tab, hash);                        break;                    }                    if (e.hash == hash &amp;&amp;                        ((k = e.key) == key || (key != null &amp;&amp; key.equals(k))))        //链表中存在该key                        break;                    p = e;                }            }            if (e != null) { // existing mapping for key                V oldValue = e.value;                if (!onlyIfAbsent || oldValue == null)                    e.value = value;                afterNodeAccess(e);                return oldValue;            }        }        ++modCount;        if (++size &gt; threshold)        //超过最大容量，扩容            resize();        afterNodeInsertion(evict);        return null;    }</code></pre><h3 id="5-resize-扩容"><a href="#5-resize-扩容" class="headerlink" title="5.resize 扩容"></a>5.resize 扩容</h3><p>使用的是2次幂的扩展(指长度扩为原来2倍)，所以，元素的位置要么是在原位置，要么是在原位置再移动2次幂的位置。看下图可以明白这句话的意思，n为table的长度，图（a）表示扩容前的key1和key2两种key确定索引位置的示例，图（b）表示扩容后key1和key2两种key确定索引位置的示例，其中<strong>hash1是key1对应的哈希与高位运算结果</strong>。</p><p><img data-src="https://ws3.sinaimg.cn/large/006tKfTcgy1fo7q9z8yzsj319c0ce761.jpg" alt></p><p>元素在重新计算hash之后，因为n变为2倍，那么n-1的mask范围在高位多1bit(红色)，因此新的index就会发生这样的变化：</p><p><img data-src="https://ws3.sinaimg.cn/large/006tKfTcly1g0l59m14g8j30k003taao.jpg" alt="b2cb057773e3d67976c535d6ef547d51_hd"></p><p>因此，我们在扩充HashMap的时候，不需要像JDK1.7的实现那样重新计算hash，只需要看看原来的hash值新增的那个bit是1还是0就好了，是0的话索引没变，是1的话索引变成“原索引+oldCap”，可以看看下图为16扩充为32的resize示意图：</p><p><img data-src="https://ws4.sinaimg.cn/large/006tKfTcly1g0l58u0adjj30k00bjjtk.jpg" alt="544caeb82a329fa49cc99842818ed1ba_hd">&lt;/u&gt;</p><pre><code class="lang-java">    /**     * Initializes or doubles table size.  If null, allocates in     * accord with initial capacity target held in field threshold.     * Otherwise, because we are using power-of-two expansion, the     * elements from each bin must either stay at same index, or move     * with a power of two offset in the new table.     *     * @return the table     */    final Node&lt;K,V&gt;[] resize() {        Node&lt;K,V&gt;[] oldTab = table;        int oldCap = (oldTab == null) ? 0 : oldTab.length; //旧容量，旧Thr(Resize上限)，超过这个threshold就要开始扩容了        int oldThr = threshold;        int newCap, newThr = 0;        if (oldCap &gt; 0) {    //超过最大值直接把threshold置为integer最大值，不再扩容了            if (oldCap &gt;= MAXIMUM_CAPACITY) {                threshold = Integer.MAX_VALUE;                return oldTab;            }        //向左移一位，扩成原来的2倍            else if ((newCap = oldCap &lt;&lt; 1) &lt; MAXIMUM_CAPACITY &amp;&amp;                     oldCap &gt;= DEFAULT_INITIAL_CAPACITY)                newThr = oldThr &lt;&lt; 1; // double threshold        }        else if (oldThr &gt; 0) // initial capacity was placed in threshold                                  //如果oldThreshold初始化了，那新的容量初始化为threshold            newCap = oldThr;        else {               // zero initial threshold signifies using defaults否则用默认值初始化            newCap = DEFAULT_INITIAL_CAPACITY;            newThr = (int)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);        }      // 计算新的resize上限newThr        if (newThr == 0) {            float ft = (float)newCap * loadFactor;            newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (float)MAXIMUM_CAPACITY ?                      (int)ft : Integer.MAX_VALUE);        }        threshold = newThr;        @SuppressWarnings({&quot;rawtypes&quot;,&quot;unchecked&quot;})            Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])new Node[newCap];        table = newTab;    //初始化新的table        if (oldTab != null) {            for (int j = 0; j &lt; oldCap; ++j) {                Node&lt;K,V&gt; e;                if ((e = oldTab[j]) != null) {                    oldTab[j] = null;                    if (e.next == null)                        newTab[e.hash &amp; (newCap - 1)] = e;                    else if (e instanceof TreeNode)                        ((TreeNode&lt;K,V&gt;)e).split(this, newTab, j, oldCap);                    else { // preserve order                        Node&lt;K,V&gt; loHead = null, loTail = null;                        Node&lt;K,V&gt; hiHead = null, hiTail = null;                        Node&lt;K,V&gt; next;                        do {                            next = e.next;                            if ((e.hash &amp; oldCap) == 0) {                               //这里为什么是 &amp; oldCap，图上画的是表长-1，表长16的时候扩容32应该和31 &amp;，是因为要判断是否为0，比如说5和21，&amp;31的时候是 一个是5，一个是21，不好判断，但是如果 &amp;16，5&amp;16是0，21&amp;16是 16，这样就可以判断了。                                if (loTail == null)                                    loHead = e;                                else                                    loTail.next = e;                                loTail = e;                            }            //如果不需要移位，找个链表lo链起来                            else {                                if (hiTail == null)                                    hiHead = e;                                else                                    hiTail.next = e;                                hiTail = e;                            }            //如果需要移位，找个链表hi链起来                        } while ((e = next) != null);                        if (loTail != null) {                            loTail.next = null;                            newTab[j] = loHead;                        }            //lo表的不动                        if (hiTail != null) {                            hiTail.next = null;                            newTab[j + oldCap] = hiHead;                        }            //hi表的放在加上旧表容量的位置                    }                }            }        }        return newTab;    }</code></pre><h3 id="6-Remove"><a href="#6-Remove" class="headerlink" title="6.Remove"></a>6.Remove</h3><p>Remove的方法思路和get差不多，只是最后是—size</p><pre><code class="lang-java">    /**     * Removes the mapping for the specified key from this map if present.     *     * @param  key key whose mapping is to be removed from the map     * @return the previous value associated with &lt;tt&gt;key&lt;/tt&gt;, or     *         &lt;tt&gt;null&lt;/tt&gt; if there was no mapping for &lt;tt&gt;key&lt;/tt&gt;.     *         (A &lt;tt&gt;null&lt;/tt&gt; return can also indicate that the map     *         previously associated &lt;tt&gt;null&lt;/tt&gt; with &lt;tt&gt;key&lt;/tt&gt;.)     */    public V remove(Object key) {        Node&lt;K,V&gt; e;        return (e = removeNode(hash(key), key, null, false, true)) == null ?            null : e.value;    }    /**     * Implements Map.remove and related methods     *     * @param hash hash for key     * @param key the key     * @param value the value to match if matchValue, else ignored     * @param matchValue if true only remove if value is equal     * @param movable if false do not move other nodes while removing     * @return the node, or null if none     */    final Node&lt;K,V&gt; removeNode(int hash, Object key, Object value,                               boolean matchValue, boolean movable) {        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; int n, index;        if ((tab = table) != null &amp;&amp; (n = tab.length) &gt; 0 &amp;&amp;            (p = tab[index = (n - 1) &amp; hash]) != null) {            Node&lt;K,V&gt; node = null, e; K k; V v;            if (p.hash == hash &amp;&amp;                ((k = p.key) == key || (key != null &amp;&amp; key.equals(k))))                node = p;            else if ((e = p.next) != null) {                if (p instanceof TreeNode)                    node = ((TreeNode&lt;K,V&gt;)p).getTreeNode(hash, key);                else {                    do {                        if (e.hash == hash &amp;&amp;                            ((k = e.key) == key ||                             (key != null &amp;&amp; key.equals(k)))) {                            node = e;                            break;                        }                        p = e;                    } while ((e = e.next) != null);                }            }            if (node != null &amp;&amp; (!matchValue || (v = node.value) == value ||                                 (value != null &amp;&amp; value.equals(v)))) {                if (node instanceof TreeNode)                    ((TreeNode&lt;K,V&gt;)node).removeTreeNode(this, tab, movable);                else if (node == p)                    tab[index] = node.next;                else                    p.next = node.next;                ++modCount;                --size;                afterNodeRemoval(node);                return node;            }        }        return null;    }</code></pre><h3 id="7-线程安全"><a href="#7-线程安全" class="headerlink" title="7.线程安全"></a>7.线程安全</h3><h4 id="Java-1-7"><a href="#Java-1-7" class="headerlink" title="Java 1.7"></a>Java 1.7</h4><p>在多线程使用场景中，应该尽量避免使用线程不安全的HashMap，而使用线程安全的ConcurrentHashMap。那么为什么说HashMap是线程不安全的，下面举例子说明在并发的多线程使用场景中使用HashMap可能造成死循环。代码例子如下(便于理解，仍然使用JDK1.7的环境)：</p><pre><code class="lang-java">public class HashMapInfiniteLoop {      private static HashMap&lt;Integer,String&gt; map = new HashMap&lt;Integer,String&gt;(2，0.75f);      public static void main(String[] args) {          map.put(5， &quot;C&quot;);          new Thread(&quot;Thread1&quot;) {              public void run() {                  map.put(7, &quot;B&quot;);                  System.out.println(map);              };          }.start();          new Thread(&quot;Thread2&quot;) {              public void run() {                  map.put(3, &quot;A);                  System.out.println(map);              };          }.start();            }  }</code></pre><p>其中，map初始化为一个长度为2的数组，loadFactor=0.75，threshold=2*0.75=1，也就是说当put第二个key的时候，map就需要进行resize。</p><p>通过设置断点让线程1和线程2同时debug到transfer方法(3.3小节代码块)的首行。注意此时两个线程已经成功添加数据。放开thread1的断点至transfer方法的“Entry next = e.next;” 这一行；然后放开线程2的的断点，让线程2进行resize。结果如下图。<br><img data-src="https://ws2.sinaimg.cn/large/006tKfTcly1g0l53xl5zbj30k0092757.jpg" alt="jdk1.7 hashMap死循环例图1"></p><p>注意，Thread1的 e 指向了key(3)，而next指向了key(7)，其在线程二rehash后，指向了线程二重组后的链表。</p><p>线程一被调度回来执行，先是执行 newTalbe[i] = e， 然后是e = next，导致了e指向了key(7)，而下一次循环的next = e.next导致了next指向了key(3)。</p><p><img data-src="https://ws2.sinaimg.cn/large/006tKfTcly1g0l54uk4i4j30k007k0t2.jpg" alt="jdk1.7 hashMap死循环例图2"></p><p>e.next = newTable[i] 导致 key(3).next 指向了 key(7)。注意：此时的key(7).next 已经指向了key(3)， 环形链表就这样出现了。<br><img data-src="https://ws4.sinaimg.cn/large/006tKfTcly1g0l57eeta1j30k006zwf4.jpg" alt="5f3cf5300f041c771a736b40590fd7b1_hd"></p><p>于是，当我们用线程一调用map.get(11)时，悲剧就出现了——Infinite Loop。</p><h4 id="Java1-8"><a href="#Java1-8" class="headerlink" title="Java1.8"></a>Java1.8</h4><p>java1.8在resize的时候不再使用重新计算每个Hash值的方法，那么而且，它是把新元素插在链表的尾部，尾部的next指向null，那么久不会出现所谓无线循环。但依然是线程不安全的。</p><p>详见：<em><a href="http://blog.csdn.net/u010412719/article/details/52049347" target="_blank" rel="noopener">http://blog.csdn.net/u010412719/article/details/52049347</a></em></p><p>但是除了Infinite Loop，HashMap依然存在另一个问题：</p><p><strong>fail-fast</strong></p><p>如果在使用迭代器的过程中有其他线程修改了map，那么将抛出ConcurrentModificationException，这就是所谓fail-fast策略。</p><p>这个异常意在提醒开发者及早意识到线程安全问题，具体原因请查看ConcurrentModificationException的原因以及解决措施</p><h3 id="8-红黑树"><a href="#8-红黑树" class="headerlink" title="8. 红黑树"></a>8. 红黑树</h3><p>红黑树部分等我学了红黑树再说吧。</p>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> java源码 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Java ModCount 和线程安全之间的关系</title>
      <link href="/2018/02/07/collection_modCount/"/>
      <url>/2018/02/07/collection_modCount/</url>
      
        <content type="html"><![CDATA[<h2 id="Java-ModCount-和线程安全之间的关系"><a href="#Java-ModCount-和线程安全之间的关系" class="headerlink" title="Java ModCount 和线程安全之间的关系"></a>Java ModCount 和线程安全之间的关系</h2><p>在ArrayList,LinkedList,HashMap等等的内部实现增，删，改中我们总能看到modCount的身影，modCount字面意思就是修改次数，但为什么要记录modCount的修改次数呢？<br>大家发现一个公共特点没有，所有使用modCount属性的全是线程不安全的，这是为什么呢？说明这个玩意肯定和线程安全有关系喽，那有什么关系呢</p><a id="more"></a><p>阅读源码，发现这玩意只有在本数据结构对应迭代器中才使用,以HashMap为例：</p><pre><code class="lang-java">    private abstract class HashIterator&lt;E&gt; implements Iterator&lt;E&gt; {        Entry&lt;K,V&gt; next;        // next entry to return        int expectedModCount;   // For fast-fail        int index;              // current slot        Entry&lt;K,V&gt; current;     // current entry        HashIterator() {            expectedModCount = modCount;            if (size &gt; 0) { // advance to first entry                Entry[] t = table;                while (index &lt; t.length &amp;&amp; (next = t[index++]) == null)                    ;            }        }        public final boolean hasNext() {            return next != null;        }        final Entry&lt;K,V&gt; nextEntry() {            if (modCount != expectedModCount)                throw new ConcurrentModificationException();            Entry&lt;K,V&gt; e = next;            if (e == null)                throw new NoSuchElementException();            if ((next = e.next) == null) {                Entry[] t = table;                while (index &lt; t.length &amp;&amp; (next = t[index++]) == null)                    ;            }            current = e;            return e;        }        public void remove() {            if (current == null)                throw new IllegalStateException();            if (modCount != expectedModCount)                throw new ConcurrentModificationException();            Object k = current.key;            current = null;            HashMap.this.removeEntryForKey(k);            expectedModCount = modCount;        }    }</code></pre><p>由以上代码可以看出，在一个迭代器初始的时候会赋予它调用这个迭代器的对象的mCount，如何在迭代器遍历的过程中，一旦发现这个对象的mcount和迭代器中存储的mcount不一样那就抛异常<br>好的，下面是这个的完整解释<br><strong>Fail-Fast 机制</strong><br>我们知道 java.util.HashMap 不是线程安全的，因此如果在使用迭代器的过程中有其他线程修改了map，那么将抛出ConcurrentModificationException，这就是所谓fail-fast策略。这一策略在源码中的实现是通过 modCount 域，modCount 顾名思义就是修改次数，对HashMap 内容的修改都将增加这个值，那么在迭代器初始化过程中会将这个值赋给迭代器的 expectedModCount。在迭代过程中，判断 modCount 跟 expectedModCount 是否相等，如果不相等就表示已经有其他线程修改了 Map：注意到 modCount 声明为 volatile，保证线程之间修改的可见性。</p><p>所以在这里和大家建议，当大家遍历那些非线程安全的数据结构时，尽量使用迭代器。</p>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> java源码 </tag>
            
            <tag> 线程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Java深拷贝和浅拷贝，克隆，对象引用</title>
      <link href="/2018/02/06/javaclone/"/>
      <url>/2018/02/06/javaclone/</url>
      
        <content type="html"><![CDATA[<h3 id="Java深拷贝和浅拷贝，克隆，对象引用"><a href="#Java深拷贝和浅拷贝，克隆，对象引用" class="headerlink" title="Java深拷贝和浅拷贝，克隆，对象引用"></a>Java深拷贝和浅拷贝，克隆，对象引用</h3><a id="more"></a><h4 id="1-克隆和对象引用"><a href="#1-克隆和对象引用" class="headerlink" title="1.克隆和对象引用"></a>1.克隆和对象引用</h4><pre><code class="lang-java">Person p = new Person(23, &quot;zhang&quot;);  Person p1 = p;  System.out.println(p);  System.out.println(p1);</code></pre><p>执行结果：</p><blockquote><p>com.pansoft.zhangjg.testclone.Person@2f9ee1ac<br>com.pansoft.zhangjg.testclone.Person@2f9ee1ac</p></blockquote><p><strong>赋值语句“=”是复制了对象的引用</strong></p><pre><code class="lang-java">Person p = new Person(23, &quot;zhang&quot;);  Person p1 = (Person) p.clone();  System.out.println(p);  System.out.println(p1);</code></pre><blockquote><p>com.pansoft.zhangjg.testclone.Person@2f9ee1ac<br>com.pansoft.zhangjg.testclone.Person@67f1fba0</p></blockquote><p>.clone()方法是克隆一个对象到新地址，可以克隆的对象继承了cloneable</p><h3 id="2-浅拷贝与深拷贝"><a href="#2-浅拷贝与深拷贝" class="headerlink" title="2.浅拷贝与深拷贝"></a>2.浅拷贝与深拷贝</h3><p>浅拷贝：</p><p>如果一个对象里有多个成员变量</p><pre><code class="lang-java">public class Person implements Cloneable{      private int age ;      private String name;      public Person(int age, String name) {          this.age = age;          this.name = name;      }      public Person() {}      public int getAge() {          return age;      }      public String getName() {          return name;      }      @Override      protected Object clone() throws CloneNotSupportedException {          return (Person)super.clone();      }  }</code></pre><p><img data-src="http://www.2cto.com/uploadfile/Collfiles/20140120/20140120090020287.jpg" alt> </p><p>要实现深拷贝：</p><p>覆盖.clone()方法，在内部实现对所有成员变量的复制，而且每个成员变量都必须实现cloneable接口：</p><pre><code class="lang-java">static class Body implements Cloneable{      public Head head;      public Body() {}      public Body(Head head) {this.head = head;}      @Override      protected Object clone() throws CloneNotSupportedException {          Body newBody =  (Body) super.clone();          newBody.head = (Head) head.clone();          return newBody;      }  }  static class Head implements Cloneable{      public  Face face;      public Head() {}      public Head(Face face){this.face = face;}      @Override      protected Object clone() throws CloneNotSupportedException {          return super.clone();      }  }</code></pre><p>但是如果多次继承，那每一个父类都需要实现cloneable接口，并复写.clone()，否则就会出现拷贝不彻底的情况。</p><p><img data-src="http://www.2cto.com/uploadfile/Collfiles/20140120/20140120090021288.jpg" alt></p><p>浅拷贝也可以用 <code>org.apache.commons.BeanUtils.copyProperties</code>实现</p><h3 id="彻底的深拷贝"><a href="#彻底的深拷贝" class="headerlink" title="彻底的深拷贝"></a>彻底的深拷贝</h3><ol><li><p>手动</p></li><li><p>利用序列化和反序列化的方式：</p><pre><code class="lang-java">public Object deepClone() throws Exception    {        // 序列化        ByteArrayOutputStream bos = new ByteArrayOutputStream();        ObjectOutputStream oos = new ObjectOutputStream(bos);        oos.writeObject(this);        // 反序列化        ByteArrayInputStream bis = new ByteArrayInputStream(bos.toByteArray());        ObjectInputStream ois = new ObjectInputStream(bis);        return ois.readObject();    }</code></pre></li><li><p>apache common类里的<code>SerializationUtils*.clone(T)</code></p><p>​</p><p>​</p></li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>手写单例模式,几种单例模式详解</title>
      <link href="/2017/12/05/singleton/"/>
      <url>/2017/12/05/singleton/</url>
      
        <content type="html"><![CDATA[<h1 id="手写单例模式"><a href="#手写单例模式" class="headerlink" title="手写单例模式"></a>手写单例模式</h1><p>单例模式算是设计模式中最容易理解，也是最容易手写代码的模式了吧。但是其中的坑却不少，所以也常作为面试题来考。本文主要对几种单例写法的整理，并分析其优缺点。很多都是一些老生常谈的问题，但如果你不知道如何创建一个线程安全的单例，不知道什么是双检锁，那这篇文章可能会帮助到你。<br><a id="more"></a></p><h3 id="1-懒加载-线程不安全"><a href="#1-懒加载-线程不安全" class="headerlink" title="1.懒加载 线程不安全"></a>1.懒加载 线程不安全</h3><p>当被问到要实现一个单例模式时，很多人的第一反应是写出如下的代码，包括教科书上也是这样教我们的。</p><pre><code class="lang-java">public class Singleton {    private static Singleton uniqueInstance;    private Singleton (){}    public static Singleton getInstance() {     if (uniqueInstance == null) {         uniqueInstance = new Singleton();     }     return uniqueInstance;    }}</code></pre><p>这段代码简单明了，而且使用了懒加载模式，但是却存在致命的问题。当有多个线程并行调用 getInstance() 的时候，就会创建多个实例。也就是说在多线程下不能正常工作。</p><h3 id="2-懒加载-线程安全"><a href="#2-懒加载-线程安全" class="headerlink" title="2.懒加载 线程安全"></a>2.懒加载 线程安全</h3><p>为了解决上面的问题，最简单的方法是将整个 getInstance() 方法设为同步（synchronized）。</p><pre><code class="lang-java">public static synchronized Singleton getInstance() {    if (uniqueInstance == null) {        uniqueInstance = new Singleton();    }    return uniqueInstance;}</code></pre><p>虽然做到了线程安全，并且解决了多实例的问题，但是它并不高效。因为在任何时候只能有一个线程调用 getInstance() 方法。但是同步操作只需要在第一次调用时才被需要，即第一次创建单例实例对象时。这就引出了双重检验锁。</p><h3 id="3-双重检查加锁-线程安全"><a href="#3-双重检查加锁-线程安全" class="headerlink" title="3.双重检查加锁 线程安全"></a>3.双重检查加锁 线程安全</h3><p>双重检验加锁模式（double checked locking pattern），是一种使用同步块加锁的方法。程序员称其为双重检查锁，因为会有两次检查 <code>uniqueInstance == null</code>，一次是在同步块外，一次是在同步块内。为什么在同步块内还要再检验一次？因为可能会有多个线程一起进入同步块外的 if，如果在同步块内不进行二次检验的话就会生成多个实例了。</p><pre><code class="lang-java">public static Singleton getSingleton() {    if (uniqueInstance == null) {                         //Single Checked        synchronized (Singleton.class) {            if (uniqueInstance == null) {                 //Double Checked                uniqueInstance = new Singleton();            }        }    }    return uniqueInstance;}</code></pre><p>这段代码看起来很完美，很可惜，它是有问题。主要在于uniqueInstance = new Singleton()这句，这并非是一个原子操作，事实上在 JVM 中这句话大概做了下面 3 件事情。</p><ol><li>给 uniqueInstance 分配内存</li><li>调用 Singleton 的构造函数来初始化成员变量</li><li>将uniqueInstance对象指向分配的内存空间（执行完这步 uniqueInstance 就为非 null 了）</li></ol><p>但是在 JVM 的即时编译器中存在指令重排序的优化。也就是说上面的第二步和第三步的顺序是不能保证的，最终的执行顺序可能是 1-2-3 也可能是 1-3-2。如果是后者，则在 3 执行完毕、2 未执行之前，被线程二抢占了，这时uniqueInstance已经是非 null 了（但却没有初始化），所以线程二会直接返回 uniqueInstance，然后使用，然后顺理成章地报错。</p><p>我们只需要将 uniqueInstance 变量声明成 volatile 就可以了。</p><pre><code class="lang-java">public class Singleton {    private volatile static Singleton uniqueInstance; //声明成 volatile    private Singleton (){}    public static Singleton getSingleton() {        if (uniqueInstance == null) {                                     synchronized (Singleton.class) {                if (uniqueInstance == null) {                           uniqueInstance = new Singleton();                }            }        }        return uniqueInstance;    }}</code></pre><p>有些人认为使用 volatile 的原因是可见性，也就是可以保证线程在本地不会存有 uniqueInstance 的副本，每次都是去主内存中读取。但其实是不对的。使用 volatile 的主要原因是其另一个特性：禁止指令重排序优化。也就是说，在 volatile 变量的赋值操作后面会有一个内存屏障（生成的汇编代码上），读操作不会被重排序到内存屏障之前。比如上面的例子，取操作必须在执行完 1-2-3 之后或者 1-3-2 之后，不存在执行到 1-3 然后取到值的情况。从「先行发生原则」的角度理解的话，就是对于一个 volatile 变量的写操作都先行发生于后面对这个变量的读操作（这里的“后面”是时间上的先后顺序）。</p><p>但是特别注意在 Java 5 以前的版本使用了 volatile 的双检锁还是有问题的。其原因是 Java 5 以前的 JMM （Java 内存模型）是存在缺陷的，即使将变量声明成 volatile 也不能完全避免重排序，主要是 volatile 变量前后的代码仍然存在重排序问题。这个 volatile 屏蔽重排序的问题在 Java 5 中才得以修复，所以在这之后才可以放心使用 volatile。</p><p>相信你不会喜欢这种复杂又隐含问题的方式，当然我们有更好的实现线程安全的单例模式的办法。</p><h3 id="4-急加载-static-final-field-线程安全"><a href="#4-急加载-static-final-field-线程安全" class="headerlink" title="4.急加载 static final field 线程安全"></a>4.急加载 static final field 线程安全</h3><p>这种方法非常简单，因为单例的实例被声明成 static 和 final 变量了，在第一次加载类到内存中时就会初始化，所以创建实例本身是线程安全的。</p><pre><code class="lang-java">public class Singleton{    //类加载时就初始化    private static final Singleton uniqueInstance = new Singleton();    private Singleton(){}    public static Singleton getInstance(){        return uniqueInstance;    }}</code></pre><p>这种写法如果完美的话，就没必要在啰嗦那么多双检锁的问题了。缺点是它不是一种懒加载模式（lazy initialization），单例会在加载类后一开始就被初始化，即使客户端没有调用 getInstance()方法。饿汉式的创建方式在一些场景中将无法使用：譬如 Singleton 实例的创建是依赖参数或者配置文件的，在 getInstance() 之前必须调用某个方法设置参数给它，那样这种单例写法就无法使用了。</p><h3 id="5-静态内部类-static-nested-class-线程安全"><a href="#5-静态内部类-static-nested-class-线程安全" class="headerlink" title="5.静态内部类 static nested class 线程安全"></a>5.静态内部类 static nested class 线程安全</h3><p>我比较倾向于使用静态内部类的方法，这种方法也是《Effective Java》上所推荐的。</p><pre><code class="lang-java">public class Singleton {      private static class SingletonHolder {          private static final Singleton uniqueInstance = new Singleton();      }      private Singleton (){}      public static final Singleton getInstance() {          return SingletonHolder.uniqueInstance;     }  }</code></pre><p>这种写法仍然使用JVM本身机制保证了线程安全问题；由于 SingletonHolder 是私有的，除了 getInstance() 之外没有办法访问它，因此它是懒加载的；同时读取实例的时候不会进行同步，没有性能缺陷；也不依赖 JDK 版本。</p><h3 id="枚举-Enum-线程安全"><a href="#枚举-Enum-线程安全" class="headerlink" title="枚举 Enum 线程安全"></a>枚举 Enum 线程安全</h3><p>用枚举写单例实在太简单了！这也是它最大的优点。下面这段代码就是声明枚举实例的通常做法。</p><pre><code class="lang-java">public enum EasySingleton{    INSTANCE;}</code></pre><p>我们可以通过EasySingleton.INSTANCE来访问实例，这比调用getInstance()方法简单多了。创建枚举默认就是线程安全的，所以不需要担心double checked locking，而且还能防止反序列化导致重新创建新的对象。但是还是很少看到有人这样写，可能是因为不太熟悉吧。</p><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>一般来说，单例模式有五种写法：懒加载、急加载、双重检查加锁锁、静态内部类、枚举。上述所说都是线程安全的实现，文章开头给出的第一种方法不算正确的写法。</p><p>就我个人而言，一般情况下直接使用急加载就好了，如果明确要求要懒加载（lazy initialization）会倾向于使用静态内部类，如果涉及到反序列化创建对象时会试着使用枚举的方式来实现单例。</p><h3 id="Read-More"><a href="#Read-More" class="headerlink" title="Read More"></a>Read More</h3><ul><li><a href="http://javarevisited.blogspot.sg/2014/05/double-checked-locking-on-singleton-in-java.html" target="_blank" rel="noopener">Double Checked Locking on Singleton Class in Java</a></li><li><a href="http://javarevisited.blogspot.sg/2012/07/why-enum-singleton-are-better-in-java.html" target="_blank" rel="noopener">http://javarevisited.blogspot.sg/2012/07/why-enum-singleton-are-better-in-java.html</a></li><li><a href="http://javarevisited.blogspot.com/2012/12/how-to-create-thread-safe-singleton-in-java-example.html" target="_blank" rel="noopener">How to create thread safe Singleton in Java</a></li><li><a href="http://javarevisited.blogspot.com/2011/03/10-interview-questions-on-singleton.html" target="_blank" rel="noopener">10 Singleton Pattern Interview questions in Java</a></li></ul>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> 设计模式 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>三种工厂模式</title>
      <link href="/2017/03/05/factory/"/>
      <url>/2017/03/05/factory/</url>
      
        <content type="html"><![CDATA[<h2 id="关于工厂模式的总结"><a href="#关于工厂模式的总结" class="headerlink" title="关于工厂模式的总结"></a>关于工厂模式的总结</h2><a id="more"></a><h3 id="1-简单工厂"><a href="#1-简单工厂" class="headerlink" title="1.简单工厂"></a>1.简单工厂</h3><ol><li><p><img data-src="/image/简单工厂.jpg" alt></p></li><li><p>角色：</p><ul><li>一个抽象产品类/接口</li><li>多个抽象产品类的实现类</li><li>一个工厂</li><li>客户端只请求工厂，工厂根据请求（switch）创建不同产品</li><li>改进，可以用xml</li></ul></li><li><p>主要优点：</p><pre><code>(1) 工厂类包含必要的判断逻辑，可以决定在什么时候创建哪一个产品类的实例，客户端可以免除直接创建产品对象的职责，而仅仅“消费”产品，简单工厂模式实现了对象创建和使用的分离。(2) 客户端无须知道所创建的具体产品类的类名，只需要知道具体产品类所对应的参数即可，对于一些复杂的类名，通过简单工厂模式可以在一定程度减少使用者的记忆量。(3) 通过引入配置文件，可以在不修改任何客户端代码的情况下更换和增加新的具体产品类，在一定程度上提高了系统的灵活性。</code></pre><p>​</p></li><li><p>主要缺点：</p><pre><code>(1) 由于工厂类集中了所有产品的创建逻辑，职责过重，一旦不能正常工作，整个系统都要受到影响。(2) 使用简单工厂模式势必会增加系统中类的个数（引入了新的工厂类），增加了系统的复杂度和理解难度。(3) 系统扩展困难，一旦添加新产品就不得不修改工厂逻辑，在产品类型较多时，有可能造成工厂逻辑过于复杂，不利于系统的扩展和维护。(4) 简单工厂模式由于使用了静态工厂方法，造成 工厂角色 无法形成基于继承的等级结构。</code></pre></li></ol><h3 id="2-工厂方法"><a href="#2-工厂方法" class="headerlink" title="2.工厂方法"></a>2.工厂方法</h3><p><img data-src="/image/工厂方法.jpg" alt></p><ol><li>与简单工厂的差别：工厂方法模式最重要的区别是引入了抽象工厂角色，抽象工厂可以是接口，也可以是抽象类或者具体类</li><li>客户端先实例化具体工厂，由具体工厂再实例化具体产品。</li><li>扩展：在客户端代码中将不再使用 new 关键字来创建工厂对象，而是将具体工厂类的类名存储在配置文件（如 XML 文件）中，通过读取配置文件获取类名字符串，再使用Java的反射机制，根据类名字符串生成对象。在整个实现过程中需要用到两个技术：Java 反射机制与配置文件读取。</li><li><p>主要优点：</p><ol><li>在工厂方法模式中，工厂方法用来创建客户所需要的产品，同时还向客户隐藏了哪种具体产品类将被实例化这一细节，<strong>用户只需要关心所需产品对应的工厂</strong>，无须关心创建细节，甚至无须知道具体产品类的类名。</li><li>基于工厂角色和产品角色的多态性设计是工厂方法模式的关键。它能够让工厂可以自主确定创建何种产品对象，而如何创建这个对象的细节则完全封装在具体工厂内部。工厂方法模式之所以又被称为<strong>多态工厂模式</strong>，就正是因为所有的具体工厂类都具有同一抽象父类。</li><li>使用工厂方法模式的另一个优点是在系统中加入新产品时，无须修改抽象工厂和抽象产品提供的接口，无须修改客户端，也无须修改其他的具体工厂和具体产品，<strong>而只要添加一个具体工厂和具体产品就可以了</strong>，这样，系统的可扩展性也就变得非常好，完全符合“开闭原则”。</li></ol></li><li><p>缺点：</p><ol><li>在添加新产品时，<strong>需要编写新的具体产品类，而且还要提供与之对应的具体工厂类，系统中类的个数将成对增加，</strong>在一定程度上增加了系统的复杂度，有更多的类需要编译和运行，会给系统带来一些额外的开销。</li><li>由于考虑到系统的可扩展性，需要引入抽象层，在客户端代码中均使用抽象层进行定义，增加了系统的抽象性和理解难度，且在实现时可能需要用到 DOM、反射等技术，增加了系统的实现难度。</li></ol></li><li>适用场景<ol><li>客户端不知道它所需要的对象的类。在工厂方法模式中，客户端不需要知道具体产品类的类名，只需要知道所对应的工厂即可，具体的产品对象由具体工厂类创建，可将具体工厂类的类名存储在配置文件或数据库中。</li><li>抽象工厂类通过其子类来指定创建哪个对象。在工厂方法模式中，对于抽象工厂类只需要提供一个创建产品的接口，而由其子类来确定具体要创建的对象，利用面向对象的多态性和里氏代换原则，在程序运行时，子类对象将覆盖父类对象，从而使得系统更容易扩展。</li></ol></li></ol><h3 id="3-抽象工厂"><a href="#3-抽象工厂" class="headerlink" title="3.抽象工厂"></a>3.抽象工厂</h3><p>抽象工厂模式是所有形式的工厂模式中最为抽象和最具一般性的一种形式<strong>。抽象工厂模式与工厂方法模式最大的区别在于，工厂方法模式针对的是一个产品等级结构，而抽象工厂模式需要面对多个产品等级结构，一个工厂等级结构可以负责多个不同产品等级结构中的产品对象的创建。</strong>当一个工厂等级结构可以创建出分属于不同产品等级结构的一个产品族中的所有对象时，抽象工厂模式比工厂方法模式更为简单、更有效率。</p><p>抽象工厂模式为创建一组对象提供了一种解决方案。与工厂方法模式相比，抽象工厂模式中的具体工厂不只是创建一种产品，它负责创建一族产品。</p><p><img data-src="/image/抽象工厂1.jpg" alt="cxgc1"></p><p><img data-src="/image/抽象工厂2.jpg" alt></p><ol><li><p>开闭原则的倾斜性：开闭原则的倾斜性：</p><p>(1) 增加产品族：对于增加新的产品族，抽象工厂模式很好地支持了“开闭原则”，只需要增加具体产品并对应增加一个新的具体工厂，对已有代码无须做任何修改。</p><p>(2) 增加新的产品等级结构：对于增加新的产品等级结构，需要修改所有的工厂角色，包括抽象工厂类，在所有的工厂类中都需要增加生产新产品的方法，违背了“开闭原则”。</p></li><li><p>抽象工厂把产品族的产品放在同一个工厂里生产，不同产品族用不同的工厂，新飞冰箱洗衣机空调，海尔冰箱洗衣机空调。</p></li><li><p>试用场景：</p><p>在以下情况下可以考虑使用抽象工厂模式：</p><ul><li>一个系统不应当依赖于产品类实例如何被创建、组合和表达的细节，这对于所有类型的工厂模式都是很重要的，用户无须关心对象的创建过程，将对象的创建和使用解耦。</li><li>系统中有多于一个的产品族，而每次只使用其中某一产品族。可以通过配置文件等方式来使得用户可以动态改变产品族，也可以很方便地增加新的产品族。</li><li>属于同一个产品族的产品将在一起使用，这一约束必须在系统的设计中体现出来。同一个产品族中的产品可以是没有任何关系的对象，但是它们都具有一些共同的约束，如同一操作系统下的按钮和文本框，按钮与文本框之间没有直接关系，但它们都是属于某一操作系统的，此时具有一个共同的约束条件：操作系统的类型。</li><li>产品等级结构稳定，设计完成之后，不会向系统中增加新的产品等级结构或者删除已有的产品等级结构。</li></ul></li></ol>]]></content>
      
      
      
        <tags>
            
            <tag> java </tag>
            
            <tag> 设计模式 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>刚刚开通了空间，来转转吧！</title>
      <link href="/2008/08/31/hello/"/>
      <url>/2008/08/31/hello/</url>
      
        <content type="html"><![CDATA[<p>刚刚开通了空间，来转转吧！<br>我今天终于有了自己的QQ空间，欢迎新老朋友为我的空间捧场！</p><p>虽然我认识的GGMM都有QQ空间，不过我相信我的空间一定是独一无二的！<br><a id="more"></a></p><p>我喜欢交朋友，如果你想和我多聊一些，给我留言，发小纸条或者直接加我好友吧！</p><p>另外，如果你在其他博客上有日记或者照片，可以用QQ空间搬家工具 (<em><a href="http://n.qzone.qq.com/move/move_login.htm" target="_blank" rel="noopener">http://n.qzone.qq.com/move/move_login.htm</a></em>) 把它们备份到QQ空间。让我们一起享受QQ空间的网络生活新体验！:)</p>]]></content>
      
      
      
        <tags>
            
            <tag> 迁移 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
    
    
    <entry>
      <title>About Me</title>
      <link href="/about/index.html"/>
      <url>/about/index.html</url>
      
        <content type="html"><![CDATA[<h3 id="Who-am-I"><a href="#Who-am-I" class="headerlink" title="Who am I?"></a>Who am I?</h3><ul><li>我是夜宵。You can call me Larry.</li></ul><p>​ </p><h3 id="Wanna-Find-Me"><a href="#Wanna-Find-Me" class="headerlink" title="Wanna Find Me ?"></a>Wanna Find Me ?</h3><ul><li><strong>email:</strong> nightsnackc@gmail.com </li><li><strong>Instagram:</strong> nightsnack_</li><li><strong>weibo:</strong> @吃夜宵</li><li><strong>github:</strong> <a href="https://www.github.com/nightsnack/" target="_blank" rel="noopener">https://www.github.com/nightsnack/</a></li></ul><p>​  </p><h3 id="Want-to-know-me"><a href="#Want-to-know-me" class="headerlink" title="Want to know me?"></a>Want to know me?</h3><p>一个无趣的人。<br>  ​</p><h3 id="Donate"><a href="#Donate" class="headerlink" title="Donate"></a>Donate</h3><p><img data-src="/image/qrcode.jpg" alt></p>]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>categories</title>
      <link href="/categories/index.html"/>
      <url>/categories/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>tags</title>
      <link href="/tags/index.html"/>
      <url>/tags/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
    
    <entry>
      <title>timeline</title>
      <link href="/timeline/index.html"/>
      <url>/timeline/index.html</url>
      
        <content type="html"><![CDATA[]]></content>
      
    </entry>
    
    
  
</search>
